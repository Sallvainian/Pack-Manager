# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local environment files (the documented template remains tracked)
.env
.env.*
!.env.example

# Playwright output
playwright-report/
test-results/
blob-report/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Rust / Tauri
src-tauri/target

# Agent tooling, installed per-machine rather than vendored (~24MB of skill
# files). Reinstallable, so it stays out of the repo. `.mcp.json` is tracked
# deliberately — it is machine-independent and shared, not an install artifact.
.agents/
.claude/
_bmad/

# BMAD UX working scratch — screenshots, Playwright CLI logs, mockup
# iterations, and memlogs. Regenerable exploration output, not deliverables;
# the DESIGN.md/EXPERIENCE.md/mockups and reports under _bmad-output are tracked.
**/.working/
**/.playwright-cli/
**/.memlog.md
.bmad-loop/runs/
.bmad-loop/cache/
.bmad-loop/policy.toml

# Kanban Pro overlay — auto-generated when this repo is opened in Kanban Pro
# (a separate app), not part of the Pack-Manager codebase. Kanban Pro also
# overwrites README.md / CLAUDE.md / AGENTS.md on project load; restore those
# from git if it does.
.kanban/
/MAPPING.md
/ORGANOGRAM.md
.github/copilot-instructions.md

# Local agent tooling — machine-specific paths and working data.
.bmad-loop/
.codex/
