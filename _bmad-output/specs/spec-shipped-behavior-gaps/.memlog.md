---
topic: D33 triage survivors: the seven live Epic 2/3/6 stories closing verified gaps in shipped behavior (2.2, 3.1, 3.2, 3.4, 3.5, 6.5, 6.6)
updated: 2026-08-18T17:18
---

- (direction) Second spec of the pair requested by the user: kernel for the seven live non-UX-PB stories so the whole remaining queue is covered by new-format specs
- (decision) Scope is the D33 triage survivors 2.2, 3.1, 3.2, 3.4, 3.5, 6.5 plus Story 6.6 (revived by D30/AD-30, hand-added to sprint-status 2026-08-11); the 31 archived stories are excluded - re-scheduling them violates the AD-1 behavior-present rule
- (decision) story-triage-2026-07-24.md listed under sources: its load-bearing outcome (which stories survived and why) is absorbed into this kernel and the rescope was already applied to epics.md on 2026-07-25, so downstream reads epics.md, not the triage
- (decision) Same adopted-companion set as spec-upgrade-plan-redesign since the same authorities bind these stories; capabilities map one-to-one to the seven stories so no spec-authored story map is needed
- (capability) CAP-1 refresh phase and timeout honesty: per-Manager phase order with the disabled metadata path omitting only that phase, per-Manager timeouts isolating failures, recovered parse output merging into the parsed inventory without dropping rows or un-pinning (Story 2.2, FR-3/17, AD-4/25)
- (capability) CAP-2 complete Package state and Manager detail: rows readable without color, uv executables expandable and searchable, npm self-state only in its Manager Card/Header where Update Manager stages and never executes (Story 3.1, FR-2/5/6/10/11/19)
- (capability) CAP-3 pinned and greedy eligibility enforcement: pinned rows inert with aria-disabled and the correct reason across every entry path and filter; greedy-only casks are the two-call set difference, collapsed, default-excluded, opt-in with disclosure (Story 3.2, FR-5/6/7, D7, D38)
- (capability) CAP-4 Settings and Environment Report trustworthiness: validate then persist atomically before activation with visible Saving/Saved/failure, skipUpgradePlanConfirmation first-class, autoOpenDrawer tolerated as inactive legacy, Environment Report complete with visible Copy outcomes (Story 3.4, FR-17, AD-19/27)
- (capability) CAP-5 exact batched membership and row plan actions: toggle, shift-range, tri-state header, Cmd-A, Space act directly on draft membership as one batched operation with concrete identities plus snapshot token; ineligible rows never enter; Cmd-A stays native on viewless surfaces; Esc is close-dialog only (Story 3.5, FR-6/10, AD-28, RP-2)
- (capability) CAP-6 exact native diagnostics with visible outcomes: timestamped ZIP with report.json, newest 3 logs, newest 25 transcripts, operations.jsonl, and raw plan-attempt record sets under the existing allowlist, proven through the AD-26 native harness; Export and Open Logs surface actionable success and failure (Story 6.5, FR-18, AD-26/29)
- (capability) CAP-7 quit guard for live child processes: user-initiated quit with Queued or Running work routes to the one enforcement point the app-update path already uses, with an explicit choice; OS-initiated shutdown gets no dialog and runs the awaited kill hook; children never outlive the app (Story 6.6, FR-14/21, AD-30)
- (constraint) Cross-spec sequencing: UX-PB.1d blocks 3.2, UX-PB.1a blocks 3.5, UX-PB.1e blocks 3.1, UX-PB.5b blocks 3.4, UX-PB.3d and 4d block 6.5; only 2.2 and 6.6 are buildable with no UX-PB dependency
- (constraint) Before treating any limb as new work, verify it against shipping code - the adversarial triage overturned 14 of 20 keeps for exactly that reason (AD-1, D33)
- (constraint) 6.5 needs the AD-26-compliant native harness: automation surface excluded from release bits at compile time, driving the production composition; adopting it is an AD-20 security-reviewed change
- (constraint) The quit-guard active set is Queued union Running and must stay identical to FR-21 app-update refusal - a change to either predicate is an AD-30 change, not a local one
- (constraint) Ineligible controls use aria-disabled, never native disabled, because a natively disabled control dispatches no mouse events and kills the pointer-reachable reason (D38, AD-16)
- (assumption) The 19 merge residuals from the triage were folded into the surviving story text when the rescope was applied to epics.md on 2026-07-25, so epics.md story text is complete as the AC authority
- (question) PRD section 9 Q4: should diagnostics export get a preview or redaction step before sharing? Non-blocking, resolves inside Story 6.5
- (note) Deliberately not listed as companions: sprint-status.yaml, docs/RELEASE-CHECKLIST.md, and process byproducts - same reasoning as the sibling spec
- (event) Self-validate pass 1 (coherence): PASS - 7 capabilities map one-to-one to the seven live stories, each with intent and success; constraints bend design (AD-26 harness, AD-30 predicate identity, aria-disabled rule); non-goals explicit; success signal demonstrable per story
- (event) Self-validate pass 2 (preservation): PASS - story-triage outcome absorbed (sources), acceptance criteria preserved via adopted epics.md, merge residuals confirmed folded into surviving story text per the triage header; companion and source paths verified to resolve on disk
- (decision) Owner closed PRD section 9 Q4 on 2026-08-18 as docs/DECISIONS.md D42: no diagnostics preview or redaction step - construction-time allowlist plus the visible ZIP path (already Story 6.5 AC) is the review affordance; open question removed and converted to a non-goal
- (event) Re-derived SPEC.md 2026-08-18: Open Questions section removed (empty after D42), preview/redaction non-goal added citing D42
- (decision) Story 2.5 (Offer Copyable Install Guidance for Absent Managers) added to Epic 2 on 2026-08-18 by sprint-change-proposal-2026-08-18.md implementing D40; joins this spec so the two kernels keep covering the whole queue - scope framing becomes seven survivors plus one owner-added story, eight total
- (capability) CAP-8 copyable install guidance for absent Managers: every absent Manager surface shows its copyable install command through the existing CopyableCommand paths, all-absent machines get a Dashboard guidance panel (Homebrew first, then Refresh All), copy-only - no Install button or execution affordance exists (Story 2.5, FR-1 install-hint limb, D40, D14)
- (event) Re-derived SPEC.md 2026-08-18 (second time today): CAP-8 added, Why and success signal updated to eight stories, buildable-now list gains 2.5
