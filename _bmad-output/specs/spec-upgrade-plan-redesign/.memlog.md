---
topic: Epic UX-PB: the D27-D30 Upgrade Plan redesign - persistent draft plan, durable plan attempt, verification-gated Results/History, separate confirmation gate
updated: 2026-08-18T19:12
---

- (direction) User asked for however many new-format bmad-spec kernels the remaining project work needs; existing planning files (epics.md, PRD, spine, UX docs, DECISIONS) are old-format inputs to distill, not to replace
- (decision) Two specs cut, not one or five: the remaining work is exactly two independent streams - Epic UX-PB (28 stories, D27-D30) and the seven D33-survivor stories; per-wave slicing would fragment one coherent domain whose per-story contracts already live in epics.md
- (decision) This spec is a kernel layer OVER the live authorities, adopted as companions, never absorbed: PRD wins on requirements, spine rev 12 wins on architecture invariants, epics.md carries binding per-story acceptance criteria, docs/DECISIONS.md is the decision log - the spec supersedes none of them
- (decision) Companions adopted: epics.md, prd.md+addendum.md, ARCHITECTURE-SPINE.md, DESIGN.md, EXPERIENCE.md, docs/SPEC.md, docs/DECISIONS.md; sources empty because nothing is fully absorbed - every input stays live downstream
- (decision) One spec-authored companion story-map.md maps CAP ids to UX-PB stories, waves, and governing decisions/ADs - a multi-row table that would bloat the kernel (Spec Law rule 8)
- (capability) CAP-1 persistent draft membership: every entry point (row checkbox, header checkbox, Manager header, Manager-wide, Update Everything) edits one persistent draft of canonical identities, Rust rebuilds exact commands, nothing executes (UX-PB.1a/1c, D27, AD-16/17/23/24/28)
- (capability) CAP-2 sidecar lifecycle: opens on first item, persists across navigation, closes on last removal, session-scoped - relaunch yields empty draft and nothing executes (UX-PB.1b)
- (capability) CAP-3 ineligible inertness with pointer-reachable reason: aria-disabled never native disabled, four plain-language reason strings, header denominator excludes ineligible (UX-PB.1d, D38, AD-16)
- (capability) CAP-4 standardized Manager workspace presentation: description/path/version/status/ownership/counts, IN PLAN plus Remove for staged self-update, last-good snapshot on refresh failure (UX-PB.1e, AD-25)
- (capability) CAP-5 distinct identities and atomic single-attempt admission: one-use preview planId vs durable planAttemptId as non-interchangeable types; execute_plan mints exactly one attempt atomically; second confirmation fails closed (UX-PB.2a/2b, D29, D30)
- (capability) CAP-6 durable attempt record and correlation: admission record at mint, terminal record on terminal transition, everything correlates by planAttemptId, attempt state derived never persisted (UX-PB.2c/2d, AD-18, AD-29)
- (capability) CAP-7 plan-level cancellation with two labeled scopes: Cancel plan skips unstarted work as Skipped and escalates process groups with no Cancelling state anywhere; Cancel operation reserved for op-scoped diagnostics (UX-PB.2e/3g, D30, AD-16)
- (capability) CAP-8 legacy honesty: records lacking planAttemptId stay labeled legacy Operations, never inferred into plans (UX-PB.2f/4e, AD-18/29)
- (capability) CAP-9 sidecar as live summary plus full Activity: confirmed sidecar transforms in place into the single live summary, full Activity is a deeper view of the same attempt, per-item honest states including Verifying, safety-critical state always visible never announcement-dependent (UX-PB.3a/3b/3c, AD-17)
- (capability) CAP-10 verification-gated Results with failure guidance: Results only after refresh verification; closed outcome taxonomy; What happened / What to do next before a secondary Retry (UX-PB.3d/3e, AD-16/25/29)
- (capability) CAP-11 trusted Interaction-required classifier: only a closed Manager-specific classifier or explicit native signal, everything else stays an ordinary stall (UX-PB.3f, D30)
- (capability) CAP-12 one immutable History row per attempt with read-only replay and live/replay coexistence: row is the fold of two records, replay executes nothing, live attempt stays primary (UX-PB.4a/4b/4c, AD-29)
- (capability) CAP-13 Retry as a new linked attempt: failed-item scope preview, derived RetryIntent never routes through the draft, retryOfPlanAttemptId lineage, original result immutable (UX-PB.4d, AD-24)
- (capability) CAP-14 separate confirmation gate with reversible skip: Confirm N Updates opens Proceed with Upgrade Plan?; opt-out lives only in the dialog, admit-then-persist rider, plan-inert setting; bypass auto-expands commands with persistent warning and Run N Updates while keeping rebuild and stale check (UX-PB.5a/5b/5c, D28, AD-21/22)
- (capability) CAP-15 safety surfaces at the size and zoom floors: 900x600, 150-200 percent zoom, high-zoom stacked mode below 720 usable px, persistent non-occludable indicator for safety-critical state (UX-PB.5d, NFR-3, AD-17)
- (capability) CAP-16 application-update separation: one restrained Pack-Manager Update Ready! badge plus the Settings card, never entering plans, Activity, Results, or History (UX-PB.5e)
- (constraint) No Cancelling state at any level and OpStatus gains no variant; any wire change is one atomic AD-3 change across the Rust enum, types.ts, guards, and dev/fixtures/ipc (prd FR-13, AD-16, D17)
- (constraint) Exactly two attempt-journal records: UX-PB.2c writes admission, UX-PB.2e owns the terminal write inside the transition critical section, UX-PB.4a is reader and folder only; appends gate nothing (AD-29, AD-18)
- (constraint) The draft is session-scoped, never written to disk, and has exactly one author; the frontend never authors or edits executable command text - Rust rebuilds from canonical intent (AD-16, AD-24, UX-PB.1b)
- (constraint) One confirmed attempt active at a time with cross-Manager concurrency inside it via the lock-set scheduler (D30, D4)
- (constraint) Membership mutations are batched: one operation per range or filter-wide interaction carrying concrete canonical identities plus the snapshot token, never a predicate (AD-28, NFR-3)
- (constraint) Focus is a real 2px outline in --color-focus-ring, never ring-* or outline-none - WKWebView drops box-shadow on native-appearance controls (AD-27, D35)
- (constraint) D37 removed keyboard-nav, VoiceOver, and announcement obligations as criteria: build none of them, report none as gaps, and strip no shipped ARIA or focus code
- (constraint) Sequencing follows epics.md dependencies; UX-PB.3a triggers on atomic admission from 2b, never on the 5a dialog, or the confirmation-off path is undefined
- (constraint) EXPERIENCE.md still carries Cancelling rows and the D37-removed keyboard sections - known-stale divergence, not authority; fixes route through a bmad-ux Update, never a hand edit
- (constraint) No sudo, shell, or password path; structured argv, constructed env, null stdin (FR-12, NFR-1)
- (assumption) ARCHITECTURE-SPINE.md artifact_revision 12 (read 2026-08-18) is current and its AD numbering governs; a later revision requires re-derive
- (assumption) sprint-status.yaml stays the tracking authority and epics.md the acceptance-criteria authority - this spec adds no story keys and restates no ACs; preservation is satisfied via the adopted companions
- (question) PRD section 9 Q3: can the user clear or delete History entries? Non-blocking, resolves inside the History stories (UX-PB.4a-4e)
- (note) Deliberately not listed as companions: sprint-status.yaml (tracker, named in the authority note), docs/RELEASE-CHECKLIST.md (release readiness, outside build scope), story-triage (sibling spec source), and all process byproducts (reviews, reconcile-*, DRIFT-NOTE, validation reports, memlogs)
- (event) Self-validate pass 1 (coherence): PASS - 16 capabilities each carry intent and success, constraints all rule something out, non-goals explicit, success signal demonstrable, CAP ids fresh and unique; noted that several intents restate decided mechanisms (Rust rebuild, aria-disabled, two-record journal) which is deliberate: they are the D27-D30/AD contract, not invented prescription
- (event) Self-validate pass 2 (preservation): PASS - all load-bearing claims live in the kernel or in adopted companions (epics.md carries the binding ACs verbatim); wrapper-only drops are the process byproducts named in the note above; companion and source paths verified to resolve on disk
- (decision) Owner closed PRD section 9 Q3 on 2026-08-18 as docs/DECISIONS.md D41: no user-initiated History deletion - no per-row delete, no Clear History, no retention knob; automatic compaction to newest 1000 stays the only pruning; open question removed from the kernel and converted to a non-goal
- (note) D40 (owner-modified Q2 closure) adds small adjacent scope owned by neither spec: copyable install hints on all six absent Manager cards plus the no-Managers panel copy; an executing Install button was refused (installer non-goal, sudo, no-shell). Needs a home - hand-added story key like the 6-6 precedent, or fold when UX-PB.1e is built; flagged to owner rather than invented here
- (event) Re-derived SPEC.md 2026-08-18: Open Questions section removed (empty after D41), History-deletion non-goal added citing D41
- (direction) User: "look in the specs folder and turn them into stories" - Story Breakdown run over both sibling specs, interactive
- (decision) Granularity: one stories.yaml entry per existing epics.md story key, not per CAP - a CAP-level merge would invent dispatch units with no sprint-status.yaml key and split dispatch from tracking (user chose from 3 options)
- (decision) ids are the bare story numbers 1a-5e; titles copied verbatim from the epics.md headings so every entry resolves to its binding AC block without restating it
- (decision) Execution order is epics.md order 1a->5e - it already satisfies every story-map gate (2a needs 1a-1e, 3a needs 2a-2f, 4a needs 3a-3g, 5a needs waves 1-2, 5e needs wave 4)
- (decision) Checkpoint policy (user-chosen): spec_checkpoint on wave openers 1a/2a/3a/4a/5a, done_checkpoint on wave enders 1e/2f/3g/4e/5e - the pause lands exactly where a wave gates on the prior wave completing
- (decision) invoke_dev_with carries prerequisite story keys plus the story-map non-stylistic ordering rules only - no AC restatement, dispatch notes only (Spec Law rule 7)
- (decision) Story 1a "Persistent draft domain with single-entry membership and Rust rebuild" (CAP-1, wave 1) - spec_checkpoint: wave opener; retires the live selection set in src/store/packages.ts; blocks 1b, 1c, and Story 3.5
- (decision) Story 1b "Sidecar lifecycle and navigation-persistent visibility" (CAP-2, wave 1) - presumes Story 6.6's quit guard, builds no second enforcement point (AD-30)
- (decision) Story 1c "Remaining draft entry points as independent removable items" (CAP-1, wave 1)
- (decision) Story 1d "Ineligible-control inertness with a pointer-reachable explanation" (CAP-3, wave 1) - blocks Story 3.2
- (decision) Story 1e "Standardized Manager workspace presentation" (CAP-4, wave 1) - done_checkpoint: wave ender, 2a gates on 1a-1e; blocks Story 3.1
- (decision) Story 2a "Distinct one-use preview planId and durable planAttemptId identity types" (CAP-5, wave 2) - spec_checkpoint: wave opener
- (decision) Story 2b "Atomic admission mints one planAttemptId and fails a second attempt closed" (CAP-5, wave 2) - its admission is the trigger 3a keys on
- (decision) Story 2c "Persist reviewed intent and the exact command snapshot durably" (CAP-6, wave 2) - writes the admission record only; 2e owns the terminal write
- (decision) Story 2d "Correlate every Operation, event, and durable record by planAttemptId" (CAP-6, wave 2)
- (decision) Story 2e "Plan-level cancellation that skips unstarted work and escalates running process groups" (CAP-7, wave 2) - owns the terminal-record append; ships in wave 2 because the no-Cancelling correction is wire surface
- (decision) Story 2f "Keep legacy Operations honest without inferred plan grouping" (CAP-8, wave 2) - done_checkpoint: wave ender, 3a gates on 2a-2f
- (decision) Story 3a "Confirmed sidecar as the single active plan summary" (CAP-9, wave 3) - spec_checkpoint: wave opener; triggers on 2b admission, never the 5a dialog
- (decision) Story 3b "Full Activity as detailed view of the same attempt" (CAP-9, wave 3)
- (decision) Story 3c "Per-item live progress states" (CAP-9, wave 3)
- (decision) Story 3d "Verification-gated Results with outcome taxonomy" (CAP-10, wave 3) - blocks Story 6.5
- (decision) Story 3e "Failure guidance and safe next step before Retry" (CAP-10, wave 3)
- (decision) Story 3f "Trusted Interaction-required classifier" (CAP-11, wave 3)
- (decision) Story 3g "Two labeled cancellation scopes" (CAP-7, wave 3) - done_checkpoint: wave ender, 4a gates on 3a-3g
- (decision) Story 4a "One immutable History row per confirmed attempt" (CAP-12, wave 4) - spec_checkpoint: wave opener; reads and folds the two records, never writes one
- (decision) Story 4b "Read-only Activity replay of a History row" (CAP-12, wave 4)
- (decision) Story 4c "Live and replay coexistence with the live attempt primary" (CAP-12, wave 4) - leaf of the UX-PB.4 spine
- (decision) Story 4d "Retry scope preview and linked new attempt" (CAP-13, wave 4) - blocks Story 6.5
- (decision) Story 4e "Legacy Operation History honest labeling" (CAP-8, wave 4) - done_checkpoint: wave ender, 5e gates on wave 4
- (decision) Story 5a "Separate final confirmation gate with the Confirm N Updates action and Proceed with Upgrade Plan? dialog" (CAP-14, wave 5) - spec_checkpoint: wave opener; gates on waves 1-2 only
- (decision) Story 5b "Dialog-only disable control with atomic skipUpgradePlanConfirmation persistence and Settings migration" (CAP-14, wave 5) - blocks Story 3.4
- (decision) Story 5c "Confirmation-disabled bypass with expanded commands and native rebuild/stale-validation-gated run" (CAP-14, wave 5) - leaf of the confirmation branch
- (decision) Story 5d "Responsiveness of the confirmation and safety surfaces at the size and zoom floors" (CAP-15, wave 5)
- (decision) Story 5e "Application-update presentation kept separate from Package plans and History" (CAP-16, wave 5) - done_checkpoint: wave ender; gates on wave 4, History must exist to assert separation
- (event) Story Breakdown check: PASS - stories.yaml holds 28 entries; schema rules 1-4 all pass (ids unique, prefix-free, quoted strings, no status field); all 28 titles match their epics.md heading verbatim; all 28 ids match a sprint-status.yaml ux-pb-<id>- key prefix; all 16 capabilities cited by at least one story
- (note) Topological check over both specs: the epics.md order used in this stories.yaml is a valid topological order (all 81 edges forward), but it is not critical-path-optimal - UX-PB.5a/5b/5c/5d gate on waves 1-2 only and can run alongside wave 3 instead of after wave 4. Left in epics.md order deliberately for sequential dispatch; a parallel schedule should float the confirmation branch earlier
