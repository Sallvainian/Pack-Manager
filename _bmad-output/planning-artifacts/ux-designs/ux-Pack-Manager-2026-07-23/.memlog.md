---
topic: Pack-Manager UX
updated: 2026-07-24T00:08
---

- (event) Resumed existing UX workspace
- (decision) Use all approved planning sources: PRD, PRD addendum, architecture spine, epics, and implementation-readiness report
- (decision) Use the coaching path for UX discovery
- (event) Approved-source extraction completed; the implementation-readiness report contains only a header and provides no UX findings
- (override) Treat DR-2 packaged accessibility method and DR-3 physical Intel requirement as approved per the later Architecture Spine, superseding their proposed state in the PRD addendum
- (event) Epics state that existing UX is complete but include no separate UX contract; this run must reconcile the new DESIGN.md and EXPERIENCE.md with finalized product authorities
- (decision) The UX should be visually pleasing and feel smooth as butter; Kanban Pro is the primary experience reference
- (decision) Use Kanban Pro as inspiration for atmosphere, hierarchy, glass depth, compact controls, and motion while creating an original Pack-Manager design without copying proprietary code, branding, or assets
- (decision) The opening Dashboard must provide an immediate overview of installed Managers and the update status of both each Manager and its managed Packages
- (decision) The primary success moment is a clearly confirmed successful update of one Package, one Manager, or a selected batch
- (event) Read-only Kanban Pro audit completed against installed 1.1.51 and DMG 1.1.53; reusable evidence includes layered dark surfaces, blue control plane, glass panels, compact icon tooling, 4/8-based spacing, visible focus, reduced-motion coverage, and a 120-400ms motion hierarchy
- (decision) Proceed with visual Pack-Manager design directions before choosing a final visual personality
- (decision) Refine Aurora Control Deck as the selected design direction while preserving the original mockup for comparison
- (decision) Remove the redundant per-Manager list from the sidebar; Manager detail remains available through the Managers destination and dashboard cards
- (decision) Summary cards use three lines: unchanged summary name, a smaller primary value, and lowercase supporting context on its own line
- (decision) Standardize Manager cards as name plus optional delegated-route chip, short description, executable path, Manager-specific status, Manager version state, Package update counts, and explicit refresh recovery when needed
- (decision) Manager status badges describe only the Manager itself; Package update counts are separate and delegated ownership such as npm via mise appears beside the Manager name
- (decision) Use a red-to-yellow-to-green package-health gauge with a position marker and text equivalent so health is not conveyed by color alone
- (decision) Upgrade Plan defaults to human-readable Package version changes grouped by Manager, colors the installed version amber and target version green, and offers a bold underlined command reveal
- (decision) Refresh failure cards say REFRESH FAILED, retain and timestamp the last-good snapshot, show a plain-language failure message, and offer Retry refresh
- (assumption) Preview a compact-confirmation preference instead of permanently disabling bulk command review; exact commands remain visible and Change Plan is neutral rather than destructive
- (event) User requested a new Aurora mockup reflecting the consolidated feedback
- (event) Refined Aurora mockup created and validated at 1440x900 and 900x600, including command reveal and final confirmation states
- (override) Manager status badges omit the word Manager and use NO UPDATES or UPDATE AVAILABLE; exceptional states such as REFRESH FAILED remain explicit
- (override) Package health is a conventional filled meter with dark remaining space: fill length represents the proportion of Packages current, the fill is one solid color selected along a red-to-yellow-to-green health scale, and no position marker is shown
- (decision) A Manager self-update is shown as a version delta in its Upgrade Plan group heading beside the Manager name and Package count, not as a Package row
- (event) Applied the second feedback pass to the refined Aurora mockup
- (event) Second Aurora refinement validated at 1440x900 and 900x600 with updated command reveal and confirmation states; Safari preview refreshed
- (override) Show each Package Manager installed version as small muted text immediately beside its name; remove the word Manager from version labels and remove Manager version metadata from the Package health area
- (decision) When a Package Manager self-update is available, show the installed-to-latest version delta directly below its UPDATE AVAILABLE badge, with installed amber and latest green
- (event) Applied the third feedback pass to the refined Aurora Manager cards
- (event) Third Aurora refinement validated at 1440x900 and 900x600; Safari preview refreshed
- (decision) Manager overview descriptions use short role-based phrases that fit without truncation: macOS package manager, Runtime version manager, JavaScript package manager, Python tool manager, Rust toolchain manager, and Mac App Store CLI manager
- (event) Applied the fourth feedback pass to shorten Aurora Manager descriptions
- (event) Short Manager descriptions validated without truncation at 900x600; Safari preview refreshed
- (override) Upgrade Plan has one blue Confirm # Updates button; Change Plan appears only inside the confirmation dialog
- (override) Pressing Confirm # Updates opens a dimmed Proceed with Upgrade Plan dialog containing exact commands, Change Plan, and a final Confirm # Updates action
- (override) Replace the compact-confirmation preference with Skip this confirmation for future upgrade plans; when enabled, future Upgrade Plans automatically expose exact commands before the blue button can execute immediately, preserving the approved command-visibility safety requirement
- (decision) Replace the ambiguous Prompts summary card with a Commands count that matches the command reveal control
- (event) Applied the fifth feedback pass to clarify the Upgrade Plan confirmation flow
- (event) Corrected confirmation flow validated at 1440x900 and 900x600; Safari preview refreshed to the normal Upgrade Plan state
- (override) Remove the Safe plan trust strip and diamond from the normal Upgrade Plan; before confirmation opens, the footer contains only the single blue Confirm # Updates button
- (decision) The future-confirmation preference checkbox appears only inside the dimmed confirmation dialog and is never visible in the base Upgrade Plan
- (event) Corrected the misplaced Safe plan affordance after user clarification
- (event) Verified no Safe plan strip or checkbox is visible in the base Upgrade Plan; the single preference checkbox appears only in the opened dialog at both target sizes; Safari refreshed
- (decision) Make the refined Aurora mockup confirmation flow genuinely interactive: the blue Confirm # Updates button opens the dialog; Change Plan, backdrop click, and Escape close it; focus moves into the dialog and returns to the trigger
- (decision) The mockup final Confirm # Updates action simulates confirmation with a non-destructive mockup-only notice and never executes a real command
- (event) User requested an interactive confirmation-dialog preview
- (event) Interactive dialog validated for open, checkbox toggle, Change Plan, Escape, backdrop dismissal, focus return, simulated confirmation, and 900x600 fit; Safari preview refreshed
- (decision) Reduce the primary value size in the three System summary cards so Ready, 12, and 76 fit comfortably while preserving the three-line hierarchy
- (event) Applied the sixth feedback pass to refine System summary typography
- (event) Reduced System summary values validated at 1440x900 and 900x600; Safari preview refreshed
- (override) System health reflects data reliability and Manager operating state, not merely whether saved data exists; a Manager refresh failure means the system is not Ready
- (decision) Use System health value Attention with supporting text 5 managers current · 1 refresh failed when one Manager retains a last-good snapshot after a failed refresh
- (decision) Package updates alone do not change System health from Ready because update availability is reported separately
- (event) Corrected the inconsistent System health state in the refined Aurora mockup
- (event) System health Attention state validated at 1440x900 and 900x600; interactive dialog remained functional; Safari preview refreshed
- (override) Use Warning rather than Attention for System health when a Manager refresh fails but a last-good snapshot keeps the system usable
- (event) Refined the System health warning vocabulary in the Aurora mockup
- (event) Changed the System health summary state from Attention to Warning and validated the refined Aurora mockup at 900x600 and 1440x900 with no horizontal overflow or console errors.
- (override) Use one cohesive set of outlined navigation icons throughout the sidebar. In Manager overview facts, show the managed Package total before the Package update count, for example 34 managed packages then 8 package updates.
- (event) Replaced sidebar text-symbol icons with consistent custom outline SVG icons, reordered every Manager card fact line, and validated the refreshed mockup at 900x600 and 1440x900 with no overflow or console errors.
- (override) Replace the terminal-dominant bottom Activity Drawer presentation with a two-level Activity experience: after confirmation, the right-side Upgrade Plan transforms into human-readable live Upgrade Activity, while the Activity sidebar destination opens a full Activity screen for deeper inspection; raw commands and terminal output remain secondary disclosures.
- (decision) Upgrade Activity shows exact overall and Manager completed-item counts plus per-Package queued, running, verified, failed, cancelled, or skipped states. Running Package rows use indeterminate motion unless a trustworthy percentage exists; a row fills green and collapses installed-to-latest into the single current version only after a refresh verifies the update.
- (override) The Managers item in the primary sidebar is a disclosure control rather than a separate all-Managers page. It is collapsed by default to avoid Dashboard redundancy; expanding it reveals the detected Manager list, and selecting a Manager replaces the Dashboard overview with that Manager's dedicated workspace.
- (override) Remove every immediate individual-Package execution path. Clicking a Package update action adds that Package to the visible Upgrade Plan; single-Package, selected, per-Manager, and Update Everything actions all flow through the same plan review and confirmation experience before any command executes.
- (decision) Treat the Upgrade Plan as Pack-Manager's primary visual organization and staging layer: it translates Package and Manager update intent into a human-readable grouped plan so users do not have to organize or reason through CLI commands.
- (override) The right-side Upgrade Plan sidecar is hidden whenever the draft plan is empty. It opens only after the first Package or Manager update is added, persists with all contents while the user navigates between Managers, closes again if the final item is removed, and transforms into Upgrade Activity after confirmation.
- (decision) At rest, Dashboard and Manager workspaces use the sidecar's width rather than reserving an empty plan panel. Update Everything and per-Manager bulk actions populate the same draft plan and reveal the same sidecar; they never execute directly.
- (decision) Package checkboxes directly control Upgrade Plan membership: checking an eligible Package immediately adds it, unchecking immediately removes it, and the header checkbox adds or removes all eligible visible Packages. Eliminate the separate temporary selection and Add Selected layer.
- (decision) Pinned, current, and excluded Packages cannot enter the Upgrade Plan. Pinned rows use muted treatment, retain a visible disabled checkbox for table alignment, and show an explicit PINNED status label plus an accessible explanation; the restriction never relies on gray styling alone.
- (decision) A pinned Package's visually disabled checkbox remains an explanatory interaction target. Hover, click, or keyboard focus reveals an anchored tooltip or popover stating: This Package is pinned and cannot be updated. Unpin it, then refresh Pack-Manager to make it selectable. Activating the control never changes Upgrade Plan membership.
- (decision) Manager workspaces show all managed Packages by default, ordered with actionable updates first, pinned or excluded Packages next, and current Packages afterward. Provide compact count-bearing filters for All Packages, Updates, and Pinned; remember each Manager's chosen filter for the current session.
- (override) Do not add a separate Manager-update row or card above the Package list. Keep a Manager's installed version, update status, installed-to-latest delta, and Update Manager action together in the existing Manager title header. Activating Update Manager adds the self-update to the persistent Upgrade Plan and changes the header action to an In Plan state; it never executes directly.
- (decision) When Upgrade Activity reaches terminal state, the right sidecar transforms into a persistent Results summary and remains until the user closes it. It reports the overall outcome and per-Manager and per-Package verified, failed, cancelled, or skipped results; successful rows show the single verified current version rather than the prior version delta.
- (decision) Declare an update successful in the Results summary only after Pack-Manager refreshes the affected Manager and verifies current state. The result is announced accessibly and may also raise a brief notification, but the notification never replaces the persistent summary.
- (decision) Failed Results retain a visible user-controlled Retry action, but Retry never substitutes for diagnosis. Each failure expands with What happened, What to do next, safe contextual actions, and optional technical evidence; Retry is secondary and rebuilds a fresh reviewable Upgrade Plan rather than executing immediately.
- (decision) When Pack-Manager recognizes the cause, show curated guidance for temporary versus action-required failures. If the same item fails again with the same cause, say so explicitly and emphasize resolving that cause before another Retry; do not automatically retry or hide the Retry control.
- (override) History is Upgrade-Plan-centric rather than Package- or command-centric. Each confirmed execution creates one History entry representing the entire plan; opening it reconstructs the original grouped Managers, Manager updates, Packages, version changes, commands, per-item outcomes, errors, timestamps, and available logs.
- (decision) A History list row summarizes one plan with execution date and time, overall result, updated-versus-total count, affected Manager count, and duration. Successful, partial, failed, cancelled, timed-out, and interrupted plans remain distinguishable; underlying Operation records stay nested evidence rather than top-level History rows.
- (override) Activity is an Upgrade Plan execution inspector, not a queue or completed-plan list. An unconfirmed draft plan exists only in the persistent Upgrade Plan sidecar. Activity opens for a confirmed plan when execution begins and otherwise shows no active upgrade rather than listing drafts or completed History entries.
- (decision) Opening a completed plan from History routes to Activity in read-only replay mode. Replay reconstructs the plan's command timeline and correlated Package results, making it clear which commands succeeded or failed and exposing specific errors and retained output inline; revealing the external log file remains a secondary diagnostic action.
- (decision) Use one scrollable Settings page grouped as Upgrade safety, Refresh behavior, Operation limits, Pack-Manager updates, and Diagnostics. Keep Operation limits and Diagnostics under a collapsed Advanced section rather than adding Settings tabs.
- (override) Remove the obsolete Auto-open Activity drawer preference. After confirmation the Upgrade Plan sidecar always becomes live Upgrade Activity; the full Activity screen opens only through deliberate navigation or History replay.
- (decision) Keep Pack-Manager application updates separate from Package Upgrade Plans, Activity, and History. Background checking and download use the application status area; installation requires an explicit Restart to Update action, refuses restart during active Package work, and presents manual-install or actionable error states when needed.
- (event) Distilled approved Discovery decisions into draft DESIGN.md and EXPERIENCE.md contracts for local coverage review
- (event) Local UX contract pass completed: frontmatter, canonical sections, 21-component parity, six acceptance journeys, source references, formatting, and palette contrast passed; visual mock coverage remains incomplete for Manager workspace, Activity, Results, History, and Settings
- (event) Corrected the automated component-parity matcher for Prettier table padding; all 21 named components match across DESIGN.md and EXPERIENCE.md and required text/action color pairs pass the 4.5-to-1 contrast floor
- (event) Focused usability and accessibility validation completed: no critical findings; reviewers reported nine high, seven medium, and two low findings, consolidated in validation-report.md and validation-report.html without changing the UX spines
- (decision) Every staged Package and Manager self-update is directly removable in the Upgrade Plan; IN PLAN is paired with a visible Remove action, and Change Plan returns to the editable draft.
- (decision) The Package bulk Checkbox applies to every eligible identity matching the active filter, including off-screen virtualized rows; it names the exact count, supports mixed state, and the Package Grid has a stable keyboard and VoiceOver model through 101-plus rows.
- (decision) When confirmation is disabled, commands auto-expand, a persistent Confirmation is off warning links to Settings, and the immediate action reads Run # updates; Confirm # updates remains reserved for the dialog path.
- (decision) Upgrade commands remain non-interactive with null input. A stalled Operation offers Keep waiting, Copy command, and Cancel; unexpected prompts become Interaction required with Copy command and Cancel, never inline input.
- (decision) Each confirmed execution attempt creates one immutable History entry. Retry scopes failed items, creates a new reviewable plan, and if confirmed produces a separate History entry linked to its source attempt.
- (decision) Sidecar Activity and full Activity share one live state. History replay stays secondary during live work with Back to live activity; Results persists until Done and deliberately transitions through retry scope before a new draft replaces it.
- (decision) Unavailable Package states receive plain-language reasons; delegated ownership uses Managed through Manager. Settings saves each control atomically with Saving, Saved, and revert-on-failure; application update details live in Settings with a restrained app-level badge.
- (decision) Accessibility fixes use a dedicated high-contrast focus ring, normative focus transitions, one throttled announcement channel, aria-disabled explanatory controls, and a high-zoom layout that replaces fixed panes at narrow effective widths.
- (event) Applied all focused usability and accessibility validation findings to the draft UX spines pending verification.
- (event) Validation resolution verified: both frontmatters parse, all token references resolve, 22 visual/behavior components match, focus contrast passes, all six journeys remain present, and every focused-review resolution has explicit contract coverage.
- (event) Applied all focused usability and accessibility findings to the UX spine pair; rendered and visually validated four authoritative key-screen mockups for Manager workspace, live Activity and Results, History replay, and Settings/app updates at 1440 x 900 and 900 x 600 with no horizontal overflow or browser-console errors; mock-coverage confirmation and finalization remain pending
- (decision) Application update presentation: the Settings card heading is simply Pack-Manager; its installed-to-target version delta remains on one line with the installed version in warning yellow and target version in success green; the title-area availability badge reads exactly Pack-Manager Update Ready!
- (decision) Advanced Settings values are editable: Stall threshold and Operation hard cap use validated duration controls; Application log level uses an explicit selector; each saves atomically with Saving, Saved, and revert-on-failure behavior
- (decision) Mock coverage confirmed for finalization: Manager workspace and Upgrade Plan, live Activity and Results, read-only Activity replay with concurrent live sidecar, and Settings/application updates are authoritative mockups; Dashboard, Confirmation Dialog, and unrendered state variants remain spine-only and are built from DESIGN.md and EXPERIENCE.md
- (change) Mock-coverage clarification: the History index itself remains spine-only; the promoted history-replay mock begins after a History entry routes into Activity, with Activity selected and the replay shown read-only beside any live Activity sidecar
- (event) Final mock distillation resolved editable Advanced controls, Activity-selected History replay, Retry scope cancellation, and compact-sidecar escalation; four validated mockups were promoted and linked from the UX spines
- (event) Structure and prose document standards completed: ten material prose refinements applied; mandatory DESIGN.md frontmatter, canonical section order, full journeys, and deliberate implementation cross-checks preserved
- (event) spines finalized
