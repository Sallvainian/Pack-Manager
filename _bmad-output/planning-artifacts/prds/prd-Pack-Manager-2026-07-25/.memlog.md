---
topic: Pack-Manager PRD (Phase 2 requirements restoration after D33 retired the prior PRD)
updated: 2026-07-25T12:15
---

- (decision) Intent=Create. No prior run under planning-artifacts/prds; clean workspace bound.
- (decision) Working mode = Fast path (user choice): batch gaps, draft full PRD with [ASSUMPTION] tags, iterate.
- (decision) FR scope = Target requirements stated normatively, each carrying a Shipping / Planned (D27-D30) status tag (user choice), mirroring project-context.md's Current-vs-Target convention so the downstream reconciliation can tell satisfied FRs from unbuilt ones.
- (change) Corrected the brief's count: epics.md carries 22 FR ids (FR-1..FR-22) plus RP-1/RP-2 and NFR-1..NFR-8, not 44 FRs. 44 came from grep -c 'FR-' also matching NFR- lines. Verified by grep -oE 'FR-[0-9]+' | sort -u.
- (change) Corrected the brief's decision range: DECISIONS.md runs D1-D37; D36 (bright fills use palette dark ink) sits between the two named. project-context.md:149 still says D1-D35 and is stale by two.
- (decision) Preserve existing FR/NFR numbering (FR-1..FR-22, RP-1, RP-2, NFR-1..NFR-8) rather than renumbering, because epics.md and ARCHITECTURE-SPINE.md already cite those ids; renumbering would break every downstream reference.
- (decision) D37 wins over epics.md FR-19 and NFR-6 (later decision). Restated without keyboard/VoiceOver operability and without announcement obligations. Kept: non-color status cues, 4.5:1 text contrast, reduced motion, 900x600, 150-200% zoom, 100+ Packages, display-only VersionDelta. Focus indicator explicitly retained per D37 (D35/AD-27, 31 sites, CI-asserted).
- (decision) Dropped NFR-6's 'deterministic dialog/sidecar focus restoration' limb along with the keyboard obligations: it is a keyboard-focus concern and is absent from the user's explicit keep-list. The visible focus indicator, which D37 does protect by name, is retained.
- (decision) Excluded all D33-retired gate machinery: 72-criterion P0 gate, 55 versioned scenario contracts, readiness-coverage-map, evidence-manifest and candidate-freeze process, coverage percentages, multi-host environment requirements. No archived file copied or moved into planning-artifacts.
- (override) Skipped the skill's subagent source-extraction default; read all sources directly instead. Session instructions forbid calling the Agent tool unless the user requests it, and direct reading serves verbatim grounding better than a digest.
- (override) Skipped the Discovery web-research subagents. This run restores requirements for an already-shipping personal utility from named internal sources; competitive-landscape research would not inform any FR.
- (event) Verified all 9 SPEC.md defects from the brief against code at HEAD: generate_handler! registers 20 commands vs SPEC 5.9's 17 (missing check_for_app_update, get_app_update_state, install_app_update); settings.rs Settings has 8 fields incl. auto_check_for_updates, absent from both SPEC F11 and SPEC 5.9; SPEC:128 lists 'notarized DMG' under P2 while SPEC:108 says delivery is signed, notarized and stapled. Also found a 10th: events.rs defines 6 events incl. appUpdate:status, SPEC 5.9's table lists 5.
- (event) Drafted prd.md (Fast path) and addendum.md. PRD covers FR-1..FR-22, RP-1/RP-2, NFR-1..NFR-8 with per-FR Shipping/Partial/Planned status tags, plus 0.1 recording the 10 verified docs/SPEC.md defects so no reader reconciles back to it.
- (decision) Mirrored EXPERIENCE.md's AJ-1..AJ-6 journey ids instead of minting a UJ-1..UJ-N set. The project already has three id namespaces (FR-, AD-, AJ-) and a fourth would be a liability; the PRD maps each AJ to the FRs that realize it.
- (decision) Verified shipping facts used in the PRD directly against HEAD 5972109 rather than trusting prose: 20 registered commands, 6 events, 8 Settings fields with defaults (auto_check_for_updates=true, auto_open_drawer=true), MAX_CONCURRENCY=4, ISSUED_PLAN_LIMIT=64, MAX_PLAN_SELECTIONS=2048, LOG_CAP=5000, minWidth 900 / minHeight 600, minimumSystemVersion 15.0, PackageTable VIRTUALIZE_ABOVE=100, LiveLogView VIRTUALIZE_ABOVE=200.
- (decision) Wrote no [ASSUMPTION] tags. Every requirement traces to a named source; where a source was wrong the PRD records the correction in 0.1, and where a source was silent it records the gap in 9 Open Questions rather than filling it. The two reviewer-visible judgment calls are logged in 10 instead.
- (decision) Addendum carries the excluded-mechanism map, the rejected-alternatives list most likely to be relitigated, the three-artifact reconciliation queue this PRD creates (epics.md / ARCHITECTURE-SPINE.md / EXPERIENCE.md, each workflow-owned and never hand-edited), and the D33/D37 scale evidence.
- (assumption) Open Question 1 (transient selection vs Upgrade Plan membership) is classified as the single phase-blocker: docs/SPEC.md F5 and EXPERIENCE.md:143 answer it oppositely, ARCHITECTURE-SPINE.md:1050 flags it OPEN and routes it to the owner, and FR-6 plus the D27 staging stories cannot be built coherently until it is closed. Items 2-6 are non-blocking and resolvable inside the epic that touches them.
- (decision) Q1 CLOSED by owner 2026-07-25: EXPERIENCE.md's model wins, docs/SPEC.md F5 is stale. Package checkboxes directly control Upgrade Plan membership; the transient selection and Add Selected layers are eliminated. Already decided in the UX run and recorded verbatim at ux-Pack-Manager-2026-07-23/.memlog.md:75; verified by reading that line this session.
- (change) Root cause of the surviving conflict identified and recorded in PRD 0.1 as an 11th SPEC defect: docs/SPEC.md 0.1's supersession list retires six behaviors and never added F5. src/store/packages.ts's live selection set is pre-D27 code, not a competing decision.
- (change) FR-6 rewritten: 'Control Upgrade Plan membership directly from eligible Packages'. Status moved from Partial to Planned - D27. Adds the header-checkbox filter-wide semantics with mixed state, Out of Scope for the transient selection and Add Selected layers, and the NEW batch requirement.
- (decision) Batch membership mutation stated in FR-6 as a requirement rather than left to architecture, per owner instruction. Cost verified verbatim at architecture reviews/review-reconcile-epics.md:166 - a per-row mapping makes a shift-range across 100 rows 100 Rust round-trips, against NFR-3's 'remain interactive with more than 100 Package rows'.
- (change) FR-5 vocabulary aligned to the direct-membership model: 'non-selectable' replaced with 'cannot enter the Upgrade Plan'. Added the pinned row's disabled-but-explanatory checkbox behavior and the never-gray-styling-alone rule, both from ux .memlog.md:76-77.
- (decision) NFR-6's dropped focus-restoration clause stays dropped - owner confirmed. Keeps the PRD consistent with D37 rather than carving an exception; 10 records it as a reviewer-visible judgment call.
- (change) 9 renumbered: Q1 removed from the open list and recorded under a new 9.1 Closed during this run. Remaining five open questions are all non-blocking; the PRD now carries no phase-blockers.
- (change) Addendum reconciliation queue updated: ARCHITECTURE-SPINE.md:1050 is to be RETIRED rather than re-answered in the bmad-architecture Update, and EXPERIENCE.md's membership model is marked correct and surviving so the bmad-ux Update touches only the accessibility limbs.
- (override) REVERSED by owner 2026-07-25: the no-subagent constraint logged earlier applies to Discovery only. Owner explicitly authorized the Reviewer Gate's subagents - 'its subagents are the skill's own process, which is exactly what you told me not to override' - so Finalize steps 2 and 3 run as the skill specifies.
- (event) Memlog audit (Finalize step 1) complete: 26 entries walked. All content entries captured in prd.md or addendum.md; intent/working-mode/override entries correctly remain memlog-only process record; line 23's phase-blocker assumption is superseded in place by line 24. Nothing orphaned.
- (decision) Finalize step 3 gate scope: rubric walker (skill default) + adversarial-general (validate.md's named default, since finalize_reviewers is empty) + 4 ad-hoc lenses judged warranted by this artifact - status-tag accuracy (the load-bearing new idea, and a wrong tag propagates into scheduled work), done-ness/testability (rubric dimension 4, feeds story creation directly), retired-scope residue (the apparatus came back once already via artifact globbing), and internal coherence/glossary discipline. Plus 7 reconcilers, one per user-supplied input. Every finding faces a refute-by-default verifier required to open the cited file.
- (decision) No validation-report.html rendered. references/validate.md:11 scopes the synthesis pipeline to the Validate intent and states the Finalize discipline pass during Create/Update 'does NOT render a report -- findings stay in-conversation'. This is Create intent.
- (event) GATE RUN 1 PARTIALLY FAILED. 73 agents, 39 killed by a session limit ~11:20-11:30. All 6 review lenses died, so the Reviewer Gate did not run and no review-*.md exists. All 7 reconcilers completed and wrote files; verification died wholesale for spine, ux and code.
- (change) HARNESS BUG found and fixed. A dead verifier made agent() return null and the script's .filter(Boolean) DELETED the finding rather than marking it unverified. Run 1 therefore reported '0 critical, 1 high' while 14 spine, 13 ux and 5 code findings were silently discarded. Gate run 2 substitutes an UNVERIFIED verdict for a null and surfaces the finding.
- (decision) Rather than trust run 1's headline, verified the six highest-value orphaned claims by hand against HEAD. All six confirmed - see the individual entries below. Reported the outage to the owner rather than relaying the misleading counts.
- (change) CONFIRMED F1 (critical): FR-14's quit-guard consequence reads as shipping but no CloseRequested/quit handler exists in src/ or src-tauri/src/. QuitGuardDialog.tsx exists and DialogHost.tsx:20 renders it, but its only opener is UpdateStatusItem.tsx:36 with reason 'update' - the app-update path is wired, the quit path is not.
- (change) CONFIRMED F2 (critical): FR-7's D28 opt-out drops all three compensating affordances EXPERIENCE.md:190 requires - auto-expanded commands, the persistent 'Confirmation is off' notice linking to Settings, and the relabel from Confirm to 'Run N updates'. grep -c 'Confirmation is off' prd.md = 0; grep -ciE 'Run (N|#) updates' prd.md = 0. This is the one FR where removing a safety gate was authorized and the price of that authorization did not come across.
- (change) CONFIRMED F3 (high): section 7.3 defers Health fixes that ship complete. HealthBanner.tsx:6 imports runHealthFix; healthBanner.test.tsx:37 asserts run_health_fix is invoked and :57 asserts the altered-suggestion case exposes no Run fix. This is the exact D33 habit the PRD warns about one sentence earlier.
- (change) CONFIRMED F4 (high): section 7.1 claims a shipping 'contrast guard'. git show HEAD:tests/e2e/browser-style-contract.spec.ts has no 4.5:1 assertion; the working tree has it at :226-320 and git status shows that file and Button.tsx as M. The guard is uncommitted, so the claim is false at the HEAD the PRD says it verified against.
- (change) CONFIRMED F5 (high): FR-6 names only ManagerPane.upgradeRow as an immediate-execution site. ManagerCard.tsx:128 and SelfUpdateCard.tsx:116 also call selfUpdateManager directly. Three sites, not one - so the D27 removal scope as written leaves two mutation paths alive, against the PRD's own SM-2.
- (change) CONFIRMED F6 (medium): FR-5 is tagged Shipping but 'Packages with updates sort first by default' is implemented nowhere - zero sort calls in packages.ts, ManagerPane.tsx or PackageTable.tsx. Only the outdatedOnly default filter ships (packages.ts:56-59). Sourced requirement per EXPERIENCE.md:411, just unbuilt.
- (event) CONVERGENCE SIGNAL: three independent reconcilers (decisions, spine F4, ux F5) each found that D30's 'only one confirmed attempt may be active at a time' has no FR home and lives only in the section 7.2 scope bullet. The one that survived verification was downgraded to a LOW placement nit; the two whose verifiers died called it HIGH. Treating the convergence as the stronger signal.
- (event) GATE RUN 2 CLEAN: 6 lenses all completed and wrote review-*.md. 62 findings raised, 53 refuted by the adversarial verifier, 9 survived (5 medium, 4 low, 0 unverified). Rubric walker wrote review-rubric.md but died on the StructuredOutput retry cap, so its verdict was read off disk and its findings were verified by hand instead.
- (event) RUBRIC VERDICT: decision-readiness strong, substance strong, strategic coherence strong, done-ness adequate, scope honesty strong, downstream usability THIN, shape fit strong. Grade Good per validate.md's derivation (<=1 thin dimension, no critical findings). The thin dimension is the handoff, not the thinking.
- (change) MY ERROR, corrected: prd.md and addendum.md carried '10, 3, and 4 mentions' copied verbatim from DECISIONS.md:556-558 instead of measured. grep -ciE 'voiceover|keyboard' gives 13, 8, 11 lines (21, 9, 13 occurrences). Violated my own rule that counts come from a command. Both files now scope the reconciliation runs by named section and state that counts are indicative only.
- (change) Applied C1: FR-7 gains the three D28 confirmation-off compensations EXPERIENCE.md:190 requires - auto-expanded commands, persistent 'Confirmation is off' notice linking to Settings, and the Confirm->'Run N updates' relabel, stated as the price of the opt-out rather than a nicety.
- (change) Applied C2: FR-14's quit-guard consequence re-marked 'Not yet built' with the reason (dialog exists and renders, no quit listener, only caller is the app-update path); Status line amended.
- (change) Applied H1: addendum section 3 rescoped by named heading. EXPERIENCE.md row now names four sections (Keyboard 272, Package Grid keyboard model 281, Focus transitions 290-303, Accessibility Floor 313-332) and cites EXPERIENCE.md:296 as the focus-restoration line the PRD records as dropped. Added a FOURTH artifact row: docs/RELEASE-CHECKLIST.md, which the PRD makes release authority while it still carries both D37-removed criteria at :86 and :89 - workflow-unowned, so a maintainer edit.
- (change) Applied H2 per owner correction: FR-6 gains keyboard consequences. Cmd+A re-points to the batch membership operation (same eligibility predicate, different sink). Esc collapses from a three-rung cascade to two - close-dialog and close-drawer survive, only clear-selection goes, stated explicitly so no story author deletes the handler. Cmd+U moved to Out of Scope (collapses into Cmd+Shift+U once scope is ambient). RP-2 extended to enumerate Cmd+R, Cmd+Shift+R, Cmd+Shift+U, Cmd+L, Cmd+F, Cmd+1-9 so FR-19's cross-reference resolves.
- (change) Applied H3/H4/H5/H6: health fixes moved from section 7.3 deferred to section 7.1 shipping with the error named as the exact D33 habit warned about one sentence earlier; section 7.1 now excludes the contrast guard explicitly and says why (uncommitted, absent from HEAD 5972109); FR-6 names all three immediate-execution sites not one; FR-15 gains the History browse/filter/search/reveal reachability consequence.
- (change) Applied mediums: section 9 Q1's app-update clause struck (FR-21 decides it normatively and ships); single-active-attempt invariant given an FR home under FR-9 tagged Planned - D30 with the independent-of-lock-overlap clause; FR-4 re-tagged Partial for the routed-Route npm note gap, echoed in FR-11; FR-5 re-tagged Partial for unbuilt outdated-first ordering; section 2.3 declares FR-12/FR-17/FR-19/RP-2 cross-cutting and warns the table is not a complete FR index.
- (decision) FR-5's pinned-checkbox contradiction resolved in favor of the adversarial lens over my own verifier, per owner. A natively disabled control cannot be the pointer-interaction target the same consequence demands, and the next bullet forbids gray-styling-alone which is what ships. FR-5 now requires a visually-disabled-but-not-native treatment with inert activation, tagged Planned.
- (change) Applied lows: PI-1 dangling archive citation removed from the Glossary; D33 quote corrected to '28 real product stories'; the phantom 'cancelling' state removed from FR-13 and its absence stated; FR-6's NFR-3 quote requoted from this PRD's own NFR-3 rather than epics.md's wording; prd.md:94 repointed from section 9 to addendum section 3 where the EXPERIENCE.md route actually lives; FR-8 gains the pending-rebuild-blocks-confirmation consequence.
- (change) Added addendum section 4 recording four SHIPPING defects surfaced by the reconciliation, none of which had a ticket: Cmd+A swallowed on Dashboard/History/Settings (preventDefault fires before the early return, blocking native select-all with no replacement - owner-found, and an Edit-menu action so D37 does not excuse it); the unwired quit guard; the npm routed-path warning gap; and the uncommitted contrast guard.
- (decision) Considered and NOT applied: the testability lens's observation that all 22 FRs carry 'Consequences (testable)' blocks while 0 of 8 NFRs and 0 of 2 RPs do. The verifier refuted it and the NFRs are deliberately prose paragraphs with embedded numeric bounds, which is a defensible shape for cross-cutting quality attributes. Recorded so the choice is visible rather than silent.
- (change) POLISH (Finalize step 5) applied via bmad-review lenses=structure,prose. Model: Strategic/Context (Pyramid). 12 recommendations - 6 applied, 2 returned as questions, 4 explicit PRESERVEs. Applied: section 10 retitled 'Review Record and Judgment Calls' (it was named Assumptions Index and opened by saying it held none); the Cmd+A defect diagnosis MERGED into addendum section 4 leaving the requirement plus a pointer in FR-6 (it was fully duplicated in both files); section 7.1's 101-word semicolon chain and FR-17's 56-word settings sentence converted to a list and a table; two 58-59 word sentences split; RP-1 given the Consequences block every other requirement has.
- (decision) PRESERVED against the structure lens's brevity bias, recorded so the choice is visible: section 7 Scope (restates the per-FR status tags in aggregate - different cut of the same data, the only way to answer 'what ships today' without reading all 22 FRs); section 9.1 (process record, but FR-6 cross-references it and it is the only record of why FR-6 has its shape); the 130 em-dashes (consistent voice shared with DECISIONS.md and project-context.md); and the hedging, of which there is exactly one instance and it is correct usage.
- (decision) RETURNED AS QUESTIONS, not applied: moving section 0.1's seven-row SPEC defect table to the addendum (Pyramid says evidence never leads, but section 0.1 exists precisely to stop readers reconciling back to SPEC and burying it weakens that - owner's call, ~250 words off the front); and RP-1's schema inconsistency, now partly addressed by giving it a Consequences block.
- (change) FINAL FINDING, found during close: NFR-6 was tagged Shipping while asserting a 4.5:1 contrast floor the build fails at HEAD. git grep 'text-white' HEAD -- src/ returns three sites (Button.tsx:7, :11, UpdateStatusItem.tsx:63) measuring 2.46:1, 2.30:1 and 2.15:1 per D36; the working tree has zero. NFR-6 re-tagged Partial with the measurements named, and FR-19's contrast consequence cross-references it. Same root cause as the section 7.1 contrast-guard fix but a distinct, load-bearing tag.
- (event) Open-item triage (Finalize step 4): 5 Open Questions, all non-blocking and each resolvable inside the epic that touches it - deferred to the owner with that revisit condition. Zero real [ASSUMPTION] tags (the single grep hit is the sentence stating none were needed). 2 [NOTE FOR PM] callouts, both deliberate. ZERO phase-blockers remain: the one that existed (FR-7's missing D28 compensations) was found by the gate and fixed.
- (event) Finalize step 6 skipped: external_handoffs and on_complete both resolve empty. PRD finalized - frontmatter status set to final, updated 2026-07-25.
