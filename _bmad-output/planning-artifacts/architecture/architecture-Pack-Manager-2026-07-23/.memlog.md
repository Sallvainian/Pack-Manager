---
scope: Cross-cutting invariants governing Pack-Manager's product architecture. (Revised 2026-07-25: the prior scope named 'all eight closure batches' and the '100% P0 product-and-release readiness initiative', both retired by docs/DECISIONS.md D33.)
purpose: Cross-cutting build substrate for Architecture, Development, QA, and Release
altitude: initiative
updated: 2026-08-06T02:57
---

- (constraint) The spine is the only deliverable; initiative altitude; audience Architecture, Development, QA, and Release; no product code, tests, CI, release workflow, or configuration changes are authorized.
- (constraint) Product behavior is inherited from docs/SPEC.md as amended by later docs/DECISIONS.md; current mechanics are bound only after verification against production registration, configuration, manifests, lockfiles, and workflows.
- (constraint) readiness-coverage-map.md is normative for exactly 72 P0 rows but remains final-pending-approval; the spine must not approve, alter, re-denominate, auto-promote, or treat the 14 baseline-FULL rows as candidate proof.
- (constraint) Product behavior, reusable test infrastructure, and candidate-specific release evidence are independent lanes; evidence from one lane cannot compensate for another.
- (constraint) DR-1 minimum macOS is OPEN; DR-2 packaged accessibility is PROPOSED; DR-3 physical Intel evidence is PROPOSED; DR-4 P0-specific gate policy is PROPOSED and primarily Product/QA governance. Their statuses remain unchanged without explicit approval.
- (decision) Existing reality adopts a single bundled Tauri application with Rust owning trusted orchestration and React owning presentation, connected only through typed local Tauri commands and events; this is a brownfield constraint, not a redesign. Binds all batches and prevents parallel implementations from adding a second backend or trust path.
- (constraint) Production registration currently exposes 20 Tauri commands in src-tauri/src/lib.rs and six events in src-tauri/src/events.rs; src/lib/ipc/bridge.ts is the sole frontend Tauri importer and App subscribes before hydration.
- (constraint) Current Vitest and Playwright paths replace the Tauri bridge; Rust command/event tests remain below the real JavaScript-to-Tauri-to-handler crossing. ASR-01 must preserve production registration and wire contracts while exercising the real crossing with isolated dependencies.
- (constraint) CommandRunner already centralizes structured argv, absolute executables, env_clear, null stdin, new process groups, output, cancellation, escalation, timeout, and stall behavior. Opener, reveal, restart, current executable, bundle-parent writability, and some path/time behavior remain direct OS calls and are not one controllable acceptance surface.
- (constraint) Production AppState initialization binds the Tauri AppHandle and default Application Support and Logs locations; component tests use hand-built states and tempdirs. ASR-03 therefore needs a production-equivalent composition mode with disposable roots and safe crash/relaunch control, never historical PGID signaling.
- (constraint) No Candidate Identity Manifest, append-only Evidence Index, or candidate invalidation controller exists in production/release files. The release workflow builds universal artifacts and emits updater metadata, but signing/notarization can degrade when credentials are absent, release-please publishes before candidate evidence, and asset upload uses clobber. Static workflow text is not candidate proof.
- (constraint) Current CI and browser workflows do not establish a fresh-checkout outbound-denied forced-offline run, a serialized provisioned target-Mac lane, or a candidate-bound release lane. Existing browser evidence is explicitly fake-Tauri and current ignored live tests are excluded until executed.
- (version) Verified lock/manifests: Tauri crate 2.11.5, JS API 2.11.1, CLI 2.11.4, updater plugin 2.10.1, opener 2.5.4, Tokio 1.53.1, React 19.2.8, TypeScript 5.8.3, Vite 7.3.6, Playwright 1.61.1; application version 0.2.7.
- (constraint) Eight-batch order is inherited: Batch 1; Batches 2/3/4 in parallel; Batches 5/6 after Batch 4; release preparation freezes one signed/notarized/stapled candidate and metadata; Batch 7 uses it; Batch 8 follows 1-7 against the unchanged candidate. Release preparation is not a ninth batch.
- (direction) Recommended paradigm: ports-and-adapters acceptance architecture at the existing Tauri composition root. Production and test modes must use the same registered commands, events, handlers, wire types, and React bridge; only external effects and root paths are replaceable.
- (direction) Recommended evidence model: provider-neutral content-addressed evidence set on existing GitHub workflow/release transport, with canonical immutable JSON Candidate Identity Manifest, append-only NDJSON Evidence Index, immutable attempt directories, hash-addressed artifacts, and no clobber semantics.
- (question) Approve, modify, or defer the ports-and-adapters acceptance architecture and provider-neutral content-addressed evidence contract.
- (question) DR-1 remains OPEN: choose a specific minimum supported macOS version or explicitly defer it; deferral blocks TIR-7 and RE-4/RE-7/RE-8 planning. Recommendation is defer the number rather than invent it.
- (question) DR-2 remains PROPOSED: approve, modify, or defer packaged keyboard/focus, automated 4.5:1 contrast, reduced-motion, and manual VoiceOver evidence. Recommendation is approve the proposed hybrid method without expanding to broader compliance.
- (question) DR-3 remains PROPOSED: approve, modify, or defer physical Intel fresh-install, Finder/Dock, and prior-version update evidence. Recommendation is approve because universal architecture inspection alone cannot prove the physical Intel promise.
- (question) DR-4 remains PROPOSED Product/QA governance: approve, modify, or defer the 72 P0 plus separate RP-1/RP-2 gate policy. Deferral blocks candidate validation but not infrastructure architecture.
- (question) Ratify role-level accountability from the finalized addendum: Architecture for ASR-01, Development/Platform for ASR-02, QA/Platform for ASR-03, Release for ASR-04, and QA/CI for ASR-05; otherwise any unassigned ASR remains an implementation-handoff blocker.
- (decision) [ADOPTED] The invariant is the shared production frontend-to-Tauri-to-Rust command/event boundary. The verified 20 commands and six events are a baseline, not permanent counts; every deliberate surface change updates production registration, Rust/TypeScript wire contracts and fixtures, frontend wrappers/subscriptions, and real native acceptance coverage in one change.
- (decision) [ADOPTED] Evidence identity uses a versioned schema with an exact cross-machine JSON canonicalization method, explicit hashing boundaries, single append authority, and validation rules; canonical JSON plus SHA-256 must be byte-reproducible across machines and runs.
- (decision) [ADOPTED] Exactly one accountable role per enabler: ASR-01 Architecture; ASR-02 Development with Platform as capability area; ASR-03 QA with Development/Platform support; ASR-04 Release; ASR-05 QA with CI as execution mechanism. Existing batch deadlines and acceptance boundaries remain binding.
- (decision) [ADOPTED] DR-2 approved as proposed: packaged keyboard/focus, automated 4.5:1 text contrast, reduced-motion, and manual VoiceOver focus-order/completion-announcement evidence; broader compliance remains out of scope.
- (decision) [ADOPTED] DR-3 approved as proposed: physical Intel fresh-install, Finder/Dock launch, and prior-public-version update evidence is mandatory for any 100% readiness claim; universal-binary inspection cannot substitute.
- (question) DR-1 remains OPEN. Product and Release must declare the minimum supported macOS version; until then TIR-7 and RE-4/RE-7/RE-8 implementation handoff remains blocked.
- (question) DR-4 remains PROPOSED and blocked on Product/QA governance. Architecture records that candidate validation cannot start until the P0-specific policy is approved, but does not approve or change the policy.
- (event) coaching decisions approved with DR-1 open and DR-4 proposed
- (decision by coach) Reconciliation ratified the verified brownfield gap: current unit/browser tests stop on one side of real Tauri transport, OS effects remain partly hardwired, current CI is not forced-offline, and the release workflow is mutable transport rather than candidate proof.
- (decision by coach) Manifest v1 requires strict Draft 2020-12 validation, prevalidated NFC strings, schema-defined array ordering, RFC 8785 JCS UTF-8 bytes, and lowercase prefixed SHA-256; immutable build run/attempt identity makes every rebuild a new candidate.
- (decision by coach) Evidence Record v1 hashes JCS payload bytes outside its own digest, stores one canonical envelope per LF-terminated NDJSON line, uses a Release-owned sole append identity with compare-and-append, and content-addresses raw evidence bytes.
- (decision by coach) Candidate/P0 collection disables automatic retries; manual retries append and retain the first failure. Ignored tests qualify only when explicitly selected, executed, and passed in a qualified lane.
- (decision by coach) Dependency DAG is Batch 1, then Batches 2/3/4, then Batches 5/6 after Batch 4, then freeze after Batches 1-6, Batch 7 on the frozen candidate, and Batch 8 after Batches 1-7.
- (event by coach) Authority and current-system reconciliation completed with no fatal conflict; draft tightened before independent reviewer gate.
- (event by coach) Deterministic architecture lint passed with zero findings; independent rubric, reality/currentness, and downstream-divergence reviewers started.
- (gap by coach) Independent gate initially failed: prose-level manifest/record schemas allowed nested-field divergence; no frozen criterion-slot profile; candidate subjects and result objects were conflated; retry aggregation and Registrar concurrency were underspecified.
- (decision by coach) ASR-01 now binds one strict versioned Tauri boundary catalog, set equality, one real round trip per command, one real delivery per event, and catalog-fixed native vectors; controlled adapters are non-distributable construction-time dependencies with no release runtime selector.
- (decision by coach) Readiness v1 contract authority is an exact locked byte set with immutable schema IDs, contract-lock digests, canonicalization vectors, closed property paths, and exact raw-byte boundaries for lockfiles, workflow, certificate DER, updater key, candidate artifacts, and evidence objects.
- (decision by coach) Criterion Acceptance Profile v1 materializes the still-unapproved DR-4 policy without changing the pending 72-row map; it fixes slots, lane/depth, scenarios, subjects, OS/architecture, physical/package requirements, and retry disposition for all 72 plus RP-1/RP-2.
- (decision by coach) Evidence records now separate subjectArtifacts from resultArtifacts and aggregate by deterministic slot attempt chains; automatic retries are disabled and DR-4 must choose fail-sticky or authorized-terminal semantics before validation.
- (decision by coach) Registrar append now requires one protected GitHub identity, candidate/profile-scoped lock or CAS, immutable publication, atomic single-head advance, deterministic idempotency, and fork rejection within the existing release framework.
- (event by coach) All clear independent-review fixes applied; deterministic lint reran with zero findings and all three reviewers were dispatched for re-review.
- (event by coach) Independent reviewer gate passed after remediation: rubric walker 0 findings; reality/currentness 0 critical/high; downstream-divergence 0 critical/high.
- (event by coach) spine finalized
- (event) Update run opened 2026-07-25, resumed from this memlog. Intent: update revision 3 in place. Mode: fast path. Deliverables: the spine plus a drift note. AD ids stay stable; AD-6..AD-10 and AD-13..AD-15 remain retired and are never reused.
- (constraint) docs/DECISIONS.md:326 records the retired gate ran 'against a contracts/ directory that does not exist and that no story creates'. Verified this run: 'ls -d contracts' returns 'No such file or directory'. project-context.md:132 states 'no scenario contract, no evidence manifest, and no contracts/ directory - do not build one'. Revision 3 nevertheless left AD-3's rule 'contracts/tauri-boundary/v1.json is the one versioned boundary catalog' binding.
- (version) Verified from package-lock.json and Cargo.lock this run: TypeScript 7.0.2 (declared ~7.0.2, registry-resolved), Vite 8.1.5, React/React DOM 19.2.8, Vitest 4.1.10, Playwright 1.61.1, Tailwind 4.3.3, Zustand 5.0.14, TanStack React Virtual 3.14.8, Tauri JS API 2.11.1, Tauri CLI 2.11.4, Tauri crate 2.11.5, tauri-plugin-updater 2.10.1, Tokio 1.53.1. Application version 1.0.0 in package.json:4 and src-tauri/Cargo.toml:3. Revision 3's Stack table carried TypeScript 5.8.3 and Vite 7.3.6; the memlog's earlier version entry carried application version 0.2.7.
- (decision) AD-3 amended in place. The versioned boundary catalog, its JCS object shape, per-schema sha256 digests, nativeVectors and scenarioContractSha256 are removed as retired-gate machinery. The boundary invariant survives and is re-anchored on the mechanism that already ships: src-tauri/src/ipc.rs:546 'Contract test (SPEC 7.4) - byte-equality against dev/fixtures/ipc/*.json', regenerated only with PM_UPDATE_CONTRACT=1, against 15 committed fixtures, with the TypeScript half asserting set equality with its guard map. 20 commands (lib.rs invoke_handler) and six events (events.rs:77-82) remain a verified baseline, not a fixed count.
- (decision) AD-1, AD-2, AD-4, AD-5 and AD-11 amended to drop evidence-lane vocabulary. AD-1's third concern 'Candidate-Specific Release Evidence' and its TIR-1/TIR-8/RE-1..RE-11 bindings are retired; what survives is the dependency direction (product code never depends on test infrastructure, CI, or release tooling) plus D33's surviving habit at DECISIONS.md:344 - verify behavior-present in shipping code before scheduling anything described as a test gap. Closes the 'AD-1..AD-5 wording cleanup' item revision 3 left Open.
- (decision) AD-4 gains the coordinator-first lock order as an explicit rule: any code reading detection, registry, queue.records(), settings, or tool_env must already hold state.plan_coordinator. Bound now because the 28 UX-PB stories all read plan-relevant state and could each take those locks independently.
- (decision) AD-17 (new) fixes draft-plan ownership. Story UX-PB.1b requires a draft to survive crash or force-quit - reconstructed into the sidecar, or returned to empty with no fabricated membership - while AD-16 said only that frontend draft state stores canonical PlanIntent. Frontend-only state cannot satisfy that, so UX-PB.1a and UX-PB.1b could build incompatibly. Rust owns the canonical draft; the Zustand store is a projection; the sidecar is a layout region rather than a ui.dialog kind or a DialogHost child.
- (decision) AD-18 (new) separates the durable plan-attempt store from the Operation journal. Story 6.5 requires the diagnostics ZIP to contain 'operations.jsonl, and the durable plan-attempt records' as distinct entries, while UX-PB.2c writes them and UX-PB.4a reads them for History. Without one owner those three stories could each pick a different file. A second append-only NDJSON journal beside operations.jsonl, under the same temp-file plus fsync plus rename compaction discipline; operations.jsonl keeps its shape and carries planAttemptId only when one exists.
- (decision) AD-19 (new) fixes persisted-schema evolution. Story 3.4 requires an old persisted autoOpenDrawer value to be tolerated during migration without ever becoming active, and UX-PB.5b adds skipUpgradePlanConfirmation. Ratifies shipping behavior at src-tauri/src/lib.rs:181 - a corrupt settings.json degrades to defaults with a visible notice - and extends it: unknown or retired fields are tolerated on read, never re-serialized as active, and never fail the app.
- (decision) AD-16's normative domain block reconciled against the UX-PB acceptance criteria it is supposed to serve. PlanIntent gains an explicit intent kind (Explicit or AllEligible) that story UX-PB.1c makes durable, and the global includeSelfUpdates control is named as removed rather than left implicit; the prose rule for intent kind existed but the domain block did not carry it.
- (constraint) Divergence recorded, not fixed: _bmad-output/planning-artifacts/epics.md lines 100-207 still carry the retired register - 72 P0 criteria, TIR-1..TIR-8, RE-1..RE-11, AD-6..AD-10 and AD-13..AD-15, Candidate Identity Manifest, Evidence Registrar, contracts/readiness/v1/contract-lock.json, and the batch waves - with only the DR rows and AD-15 patched post-D33. epics.md:150 still requires set equality against contracts/tauri-boundary/v1.json. This run was scoped to the spine plus a drift note, so the upstream reconciliation is offered rather than performed.
- (assumption) The draft plan is persisted durably rather than always failing to empty on relaunch. Story UX-PB.1b:437-439 allows either branch, and the durable reading is the primary one in its wording; the choice was inferred, not user-confirmed.
- (event) Reviewer gate, first return: reconcile-decisions (docs/DECISIONS.md D1-D33 + docs/SPEC.md). Verdict: substantially reconciled. Confirmed zero live readiness-gate residue in the rule set - the six matches for the retired vocabulary are all retirement records or negations - and zero inverted SPEC load-bearing invariants. Eight findings, all applied.
- (decision) AD-16's app-update rule corrected: 'surfaces only through its own badge and Settings card' excluded the macOS app menu 'Check for Updates...' item, which D25 names as a trigger and which ships at src-tauri/src/lib.rs:70. A builder enforcing the rule literally would have been entitled to delete it, taking the hand-rebuilt Edit/Window submenus and every clipboard shortcut with it. The rule now names all three surfaces and adds D25's convergence requirement - menu handler and IPC command share one code path behind UpdateSource/PendingRelease.
- (decision) AD-16 gains the distinction between item-level ineligibility and plan-composition exclusion. The inertness rule said an ineligible item 'can never enter a PlanIntent', which is wrong for rustDedup: mise tool:rust is legitimately stageable until rustup toolchains join the same plan, so the trigger is cross-item, not item-level. Under durable rebuilt membership that question is sharper than it was under the one-shot sheet. rustDedup and alreadyRunning stay in PlanIntent and surface in the preview's exclusions; removing the other side of the conflict restores the item.
- (decision) AD-4 gains three rules the spine had left to SPEC: the manager's own outdated verdict is the sole authority on outdatedness with frontend delta display-only; classification reads the RAW resolved path against mise's shims and installs directories before canonicalizing, because canonicalize-first misroutes npm and uv to brew; and the output side has a fidelity floor whose only exception is the runner's closed verbatim unterminated-notice list. Story 2.2 was bound to this area with no governing rule.
- (decision) AD-17 gains Activity's surface. D30 makes Activity a first-class navigation destination and supersedes D18's drawer-only model, but the spine settled that question for the sidecar only - so a UX-PB.3x and a UX-PB.4x story could still disagree on whether Activity is a route, a no-longer-auto-opening drawer, or a sidecar mode. Activity is an ActiveView destination; the ActivityDrawer surface retires with the autoOpenDrawer setting.
- (decision) AD-16 states admission atomicity explicitly. 'Returns the admitted Operation identities' implied but did not require all-or-none, and under the one-active-attempt model a partial admission would produce an attempt whose membership silently differs from the reviewed intent.
- (constraint) D31's residual restored to the spine. The status table recorded minimum supported macOS as RESOLVED with no residual, but DECISIONS.md:271-276 keeps one question OPEN in bold - whether notarytool accepts minos 15.0 against SDK 14.5 - and states 'Nothing here asserts that it does.' The spine was asserting a closure its cited source refuses to assert. Now recorded as RESOLVED (one residual) in the table and in AD-11.
- (event) Reviewer gate complete: six independent lenses returned - reconcile-epics, reconcile-decisions, reconcile-ux, rubric walker, reality/currentness, downstream-divergence. Rubric verdict was NOT READY on revision 4. Criticals and highs applied as revision 5; the spine grew from 505 to ~640 lines and gained AD-20.
- (gap) Rubric CRITICAL: AD-16's Prevents claimed 'executing from a row or Manager header' but not one of its nine rules forbade an entry point from executing. D27's headline invariant was asserted in a Prevents and stated nowhere. Five stories own five entry points and the shipping ManagerPane.upgradeRow still calls executePlan directly, so the divergence was the default rather than hypothetical. Now AD-16 rule 1.
- (gap) Divergence CRITICAL D-1: nothing forbade a canonical rebuild from ADDING membership. AllEligible read as a live predicate re-expands on rebuild; UX-PB.5c's confirmation-off path admits and runs with no dialog. A background refresh between staging and click would mutate packages the user never saw. AD-16 now forbids enlarging rebuilds and freezes AllEligible expansion.
- (gap) Divergence CRITICAL D-2: AD-16 required affected refreshes to 'complete' but never that they START after the mutating process exited, while AD-4 mandates duplicate-refresh coalescing. A refresh in flight at mutation time would satisfy verification from a pre-mutation snapshot - or fail a successful update. Verification refreshes are now exempt from coalescing against anything in flight at exit.
- (gap) Divergence CRITICAL D-3, also found independently by the rubric and epics lenses: the draft's fate at admission was unowned, and AD-17's 'visibility driven by draft non-emptiness' contradicted its own 'a confirmed attempt replaces the sidecar's content'. Both branches broke. Admission now transfers custody atomically and visibility is a three-way union of non-empty draft, non-terminal attempt, or undismissed Results.
- (decision) Operation status taxonomy fixed after the rubric found Verifying and Skipped stated only at attempt level while three stories require them per item. They are durable wire-level states, not React-derived: they are journaled, exported, and replayed, and a derived state cannot survive a crash or a replay. Skipped marks only never-started work; crash-reconstructed work stays Interrupted.
- (decision) AD-4 rule 1 rewritten. It asserted a runtime-port set in the present indicative that does not exist - five traits ship, and opener, reveal, restart, current-executable, writability and several path/time call sites are direct - so the rule declared six shipping call sites to be violations no story schedules, contradicting the spine's own baseline three sections earlier. Now: five real ports named, new effects the live queue introduces must be ported, and porting the rest is Deferred with no live consumer.
- (decision) AD-3's Prevents narrowed. It claimed to prevent broken event delivery, but the fixtures prove payload shape only and never dispatch through Tauri - the spine says so itself. Delivery coverage now explicitly waits on the deferred native harness, and no story may claim it from a fixture or the browser double.
- (decision) AD-19 split. 'A corrupt file degrades to defaults' bound all persistence, which would have let a journal be defaulted away. Degrade-to-defaults is now scoped to configuration; an unparseable journal line is skipped and counted, and a record is never replaced by a synthesized one.
- (decision) AD-20 (new): the webview trust boundary. csp is null and one capability grants the main window core:default, opener:default, and core:window:allow-start-dragging. Any remote content must ship a real CSP in the same change, and a permission, plugin, window, or capability addition is reviewed on its own rather than folded into a feature story. The rubric flagged this as a wholly silent dimension.
- (constraint) AD-11's accessibility rule corrected: it asserted automated 4.5:1 contrast and reduced-motion checks in the Playwright/Vitest lane as present tense. Neither exists in the repo. Restated as an obligation on whichever story adds them.
- (decision) AD-17 gains explicit launch ordering - subscribe, hydrate, reconstruct the draft against the first real detection report, then arm the launch refresh - because resolving against the pre-detection placeholder would empty every draft on relaunch and satisfy the durability rule vacuously. Also one polite announcement channel, and the below-720px presentation change, both from the UX lens.
- (decision) AD-18 gains disclosure and retention coupling: widening the diagnostics export does not widen disclosure, and the two journals may not compact into orphaned attempts or unresolvable references.
- (assumption) AD-17's durable-draft choice moved from an inline [ASSUMPTION] tag to an Open row in the decision table. The rubric was right that a tag reading 'this rule may be wrong' inside the rule cancels the rule; the question belongs in the open items, where the spine still commits to a behavior.
- (event) spine finalized at revision 5; deterministic lint zero findings; all critical and high gate findings applied; medium/low tail recorded in DRIFT-NOTE.md section 4 with pointers to the six review files
- (decision) Draft durability RESOLVED by the owner: fail-to-empty. The draft is session-scoped and never written to disk; every relaunch - clean quit, crash, or force-quit - starts with an empty draft and a hidden sidecar. This takes the second branch of UX-PB.1b's recovery criterion unconditionally, which epics.md:437-439 permits explicitly. Supersedes the assumption revision 5 carried.
- (decision) AD-17 amended in place for fail-to-empty (id unchanged). The durable-persistence rule and the launch-reconstruction ordering rule are replaced by a session-scope rule plus a closed-door rule: no draft file, no versioned draft schema, nothing for AD-19 to migrate, and a story wanting staging to survive a crash is proposing a new decision rather than implementing this one. The accepted cost is a lost draft after a crash.
- (decision) Ripple sites updated with AD-17: the Structural Seed no longer lists a draft file and states its absence positively; AD-4's new-effects rule drops the draft's filesystem need and keeps AD-18's journal; the Deferred row narrows to the plan-attempt file alone; and the capability map now says the draft persists across navigation, not across relaunch, because 'persistent' was about to be misread as disk persistence.
- (constraint) epics.md left untouched by owner instruction. It remains the recorded Open item in the spine's decision table and section 3 of DRIFT-NOTE.md; the owner will run correct-course for it separately.
- (event) Focused verification workflow on the fail-to-empty change: three lenses (story-AC breakage, cross-AD consistency, residual references), 15 findings raised, each sent to an independent refuter, 0 survived. Refuters correctly judged all 15 non-blocking; three were nonetheless fixed in DRIFT-NOTE.md as record-accuracy issues rather than builder-facing defects - a stale memlog entry count, a sentence reading as though the durable branch were required, and a changelog line naming the now-removed draft file.
- (event) Revision 7 run opened 2026-07-25. Intent: update, forwarded from bmad-correct-course. Sole goal: close the 'epics.md retired register' Open row that revision 6 carried, now that the upstream reconciliation entry 91 deferred to the owner has actually been performed.
- (decision) epics.md retired register CLOSED. The divergence recorded at entry 64 and deferred at entry 91 is resolved: _bmad-output/planning-artifacts/epics.md was reconciled on 2026-07-25 under the approved proposal at _bmad-output/planning-artifacts/sprint-change-proposal-2026-07-25.md. Its six retired sections (Readiness and Scope Controls, Test Infrastructure Readiness, Release Evidence Requirements, Architecture Invariants and ASR Enablers, Governance/Risks/Entry Blockers, Implementation-Entry Blocker Register) were replaced by retired-record stubs naming this spine as the single architecture authority. TIR-1..TIR-8, RE-1..RE-11, the register's own AD-1..AD-15 and ASR-01..ASR-05, the 72-criterion controls, the Candidate Identity Manifest, the Evidence Registrar, contracts/readiness/v1/contract-lock.json, and the contracts/tauri-boundary/v1.json set-equality requirement no longer appear as obligations. Binds: the spine's Decision Status table. Prevents: bmad-create-story reading a retired requirement out of epics.md and handing a story an instruction to build a directory three authorities say must not exist.
- (constraint) The AD-id collision was the sharpest half of the divergence and is the reason the reconciliation could not be deferred further. epics.md published its own AD-1..AD-15 under the same ids this spine uses but with different rules - its AD-1 was a readiness-concern rule against this spine's dependencies-point-inward; its AD-3/ASR-01 ordered the contracts/tauri-boundary/v1.json catalog against this spine's committed-fixture anchor; its AD-11 ended acceptance at an immutable candidate against this spine's checklist-plus-two-checks. epics.md now carries an explicit collision table recording those retired ids and names this spine as the only AD authority. Story text cites spine AD ids only.
- (constraint) Memlog frontmatter scope corrected for record accuracy. It still described this run as 'governing all eight closure batches for the Pack-Manager 100% P0 product-and-release readiness initiative' - the batches and the P0 readiness initiative are both retired by D33, and the spine's own frontmatter has said 'Cross-cutting invariants governing Pack-Manager's product architecture' since revision 4. Same class of fix as the stale entry count corrected at entry 92.
- (event) Reviewer gate revision 7, first return: currency/reality lens. Verdict: 47 of 49 checkable claims verify exactly against the tree, including the TypeScript 7.0.2 and Vite 8.1.5 rows most likely to be wrongly 'corrected' from stale memory. Two defects, both in the provenance layer rather than the invariants. Full review at reviews/review-currency-v6.md.
- (version) HIGH, independently confirmed: the application version is 1.0.1, not 1.0.0. Verified this run - package.json:4, src-tauri/tauri.conf.json:4, src-tauri/Cargo.toml:3, .release-please-manifest.json, and the package-lock root entry all read 1.0.1. Release commit 8a4cf6a 'chore(main): release 1.0.1 (#34)' landed 2026-07-25 02:46:52, about one minute before revision 6 was written, so the spine asserted 1.0.0 at ARCHITECTURE-SPINE.md:107 and :578 under a same-day 'Verified against package-lock.json' banner while the named authority already said otherwise. The digit is inert - no invariant depends on it - but the verification contract is what breaks, and AD-12 makes version drift a governed concern. Fix: stop restating a release-please-owned value the spine does not control.
- (constraint) MEDIUM, confirmed and self-inflicted: every epics.md line citation in this run folder went stale when epics.md was reconciled earlier today, because the rewrite shifted line numbers. Verified new targets - the UX-PB.1b recovery criterion moved from 437-439 to 521-523; Story 6.5's diagnostics-ZIP criterion from 1161 to 1245; Story 3.4's autoOpenDrawer-migration criterion from 1102 to 1189; and the AD-3/ASR-01 set-equality text formerly at :150 is gone entirely rather than moved. Five citations affected: ARCHITECTURE-SPINE.md:646 and DRIFT-NOTE.md:76, :87, :91, :165. The substance of each claim remains true; only the pointers rotted.
- (event) Reviewer gate revision 7, second return: adversarial epics.md reconciliation lens. Verdict: the Open row CANNOT be closed as written. Four items it names literally are discharged, but its operative claim - 'It contradicts this spine and docs/DECISIONS.md D33' - was still true through a residue the rewrite left and strengthened. Full review at reviews/review-reconcile-epics-v6.md.
- (decision) CRITICAL, upheld against my own earlier reasoning: the R-001..R-008 risk register was re-importing the entire retired obligation set by reference, and the correct-course run made it worse. Verified this run - the ids are defined ONLY under _bmad-output/archive/2026-07-24-scope-recalibration/ (grep -rln 'R-001' returns archive paths plus my own new text), and the archived table's 'Required mitigation' column IS the retired machinery: R-002 reads 'Deliver ASR-01 and AD-3 set-equality checks; round-trip every catalog command', which is the exact obligation the Open row cites, and R-007 requires the physical-Intel acceptance D32 dropped as undischargeable. HEAD read 'No mitigation is complete, waived, or accepted through planning'; the rewrite dropped 'through planning' and added bold 'are NOT retired', converting a scoped planning statement into a live claim that archived mitigations are still owed. My reasoning error was inferring survival from D33's retired list not naming risks, without ever reading where the ids were defined or what their mitigation column said. Resolution: the register and its ids are retired; the eight underlying engineering concerns are restated in epics.md as a table mapping each to the live spine AD that owns it, with no dependency on the archived mitigations.
- (decision) Citation hygiene fixed in epics.md. (a) Six 'AD-16 rule N' ordinals replaced with subject references - the ordinals silently broke when revision 5 inserted 'No entry point executes' as AD-16's first rule and shifted every later number, so a story citing 'rule 4' now resolved to a different rule than when written. (b) AD-17, AD-19, and AD-20 were cited zero times despite the spine binding all three to named stories, while epics.md's own new text claimed 'Stories cite its AD ids and no others'. AD-17 added to UX-PB.1a-1e and UX-PB.3a, AD-19 to UX-PB.5b, AD-18 to UX-PB.2a, each per the spine's Binds field; the six surviving Epic 1-6 stories gained a 'Governing invariants' contract line derived strictly from those same Binds fields. (c) AD-1, AD-2, AD-20 and the Determinism convention have no single owner, so they are stated once as binding every story regardless of citation, plus a standing instruction to cite by subject and never by rule ordinal. All twelve spine AD ids are now cited at least once.
- (event) Reviewer gate revision 7 complete: four lenses returned - currency/reality, adversarial epics.md reconciliation, adversarial downstream divergence, rubric walker. Rubric verdict READY WITH FIXES (1 CRITICAL, 4 HIGH, 9 MEDIUM, 3 LOW). Divergence returned 3 CRITICAL, 5 HIGH, 4 MEDIUM, 2 LOW. Reconciliation returned NO on the Open row until CRITICAL-1 was fixed, then YES. Currency: 47 of 49 checkable claims verified exactly, 1 HIGH, 1 MEDIUM.
- (decision) Revision 7 applied the corrections where the spine asserted something false, and deliberately did NOT attempt the new architecture. Applied: the epics.md Open row closed as RESOLVED with the UX-PB.1b residual named; the application version removed as a restated value and pointed at .release-please-manifest.json, because AD-12 makes version drift governed and the spine was contradicting the very file it named as authority; the porting row changed from 'Deferred, no live consumer' to 'OPEN, owner Story 6.5' because epics.md:1290-1292 requires controlled opener success/failure while both reveal paths are un-ported direct calls in commands.rs; the crash/relaunch row given its four real consumers; AD-4's falsified 'with no live consumer' clause removed; AD-17 and AD-18 Binds lists widened to include UX-PB.4d and UX-PB.3d; and all epics.md citations repointed after the reconciliation shifted line numbers.
- (question) NOT resolved in revision 7 and recorded as six Open rows rather than silently carried: the settings-write vs revision-drift collision that makes UX-PB.5b's opt-out fail its own run (divergence C-1); PlanIntent needing per-member provenance rather than one kind scalar, which UX-PB.1a and UX-PB.1c cannot resolve identically (C-2); Retry and the accumulating draft as two owners of the draft's next state (C-3); per-Manager failure isolation and Last-good Snapshot retention as a silent dimension AD-16 references as though defined (rubric H2); the app-update safety guard having no stated enforcement point while the shipping guard is frontend-only (rubric H4); and the remaining 5 HIGH / 19 MEDIUM tail. Closing these means writing new invariants, which is new architecture rather than the reconciliation this run was authorized for - so they go to the owner as a decision, not applied unilaterally.
- (event) spine finalized at revision 7; deterministic lint zero findings; epics.md register Open row closed as RESOLVED; six new Open rows recorded for the unresolved architecture findings; DRIFT-NOTE.md section 2d written and its header corrected from 'revision 3 to 6'
- (decision) Line-number citations into epics.md abandoned in favour of story-id plus quoted-content citations. They rotted twice inside one session: first when the reconciliation shifted epics.md from 1166 to 1250 lines, then again when this run's own CRITICAL-1 and citation fixes took it to 1293, so the numbers the reviewers reported were already stale by the time they were applied. Same failure mode as AD-16's rule ordinals fixed earlier this run - a positional reference into a document under active edit. All citations in the spine and DRIFT-NOTE now name the story and quote the criterion; the single remaining epics.md:150 in DRIFT-NOTE section 3 is explicitly labelled a pre-reconciliation line number that does not resolve against the current file.
- (event) revision 7 closed; deterministic lint zero findings after all edits
- (constraint) Reviewer-gate finding counts recomputed rather than estimated: 44 total across the four v6 lenses - 5 CRITICAL, 14 HIGH, 18 MEDIUM, 7 LOW (rubric 1/4/9/3, divergence 3/5/4/2, reconcile-epics 1/4/4/0, currency 0/1/1/2). Revision 7 resolved 12 and promoted 5 to dedicated Open rows, leaving a tail of 7 HIGH, 15 MEDIUM, 5 LOW. An earlier hand-tallied figure of 11 HIGH / 19 MEDIUM / 5 LOW was wrong and had propagated into the spine's tail row before correction.
- (event) Revision 8 run opened 2026-07-25, resumed from this memlog. Intent: update. Scope set by the owner: close the three CRITICAL Open rows revision 7 recorded but did not fix (UX-PB.5b vs UX-PB.2b settings-write/revision-drift; UX-PB.1a vs UX-PB.1c PlanIntent member provenance; UX-PB.4d vs UX-PB.1c Retry vs accumulating draft), each needing a NEW invariant rather than a reworded one; correct AD-11's accessibility rule, whose 'neither exists yet' is false for reduced motion; and judge whether the two named HIGHs (Story 6.5 not buildable from the spine, and Last-good Snapshot / per-Manager failure isolation as a silent dimension) are the same shape of hole. epics.md is OUT OF SCOPE and must not be edited - further divergence is recorded as Open rows for a later bmad-correct-course run. AD ids stay stable; AD-6..AD-10 and AD-13..AD-15 remain retired and are never reused.
- (constraint) AD-11 accessibility correction VERIFIED against the tree this run, and the owner's report is exactly right. Reduced motion IS automated and DOES run in CI: tests/e2e/browser-style-contract.spec.ts:46 'await page.emulateMedia({ reducedMotion: "reduce" });' and :93 'await test.step("And reduced motion removes CSS transitions and animations"' asserting transitionDuration/animationDuration '0s' at :111-112; .github/workflows/test.yml runs 'npm run test:e2e' at :90-94 under 'on: push: branches: [main]' / 'pull_request: branches: [main]' (:8-12). The product side ships too - src/styles/theme.css:50-51 '/* prefers-reduced-motion disables all transitions (default 150ms ease). */ @media (prefers-reduced-motion: reduce) {'. Contrast genuinely does NOT exist: 'grep -rni contrast tests src' returns exactly one hit, the disclaimer at tests/e2e/browser-style-contract.spec.ts:116-117 '// This is a browser DOM/CSS contract only. It does not claim measured / contrast compliance or validate the native Tauri package.' So AD-11's 'neither exists yet' is false for half its subject; the obligation is contrast only.
- (constraint) C-1 is not hypothetical - the mechanism ships today and I verified both halves. src-tauri/src/commands.rs:649 'coordinator.bump_revision();' sits inside set_settings_core AFTER the persist and publish, unconditionally, for every key of every settings patch. src-tauri/src/commands.rs:372-373 'if issued.revision != coordinator.revision() { return Err(plan_stale("canonical state revision changed after preview")); }'. So ANY settings write of ANY key already invalidates any live preview. AD-19's ordering rule was ratifying real behavior (commands.rs:644-645 comment 'Persist before publishing: a failed write leaves both the in-memory settings and the canonical plan revision unchanged'), but it inherited the unconditional bump with it. The Settings struct at settings.rs:28-39 has eight fields and exactly one - include_greedy_by_default - can change plan membership; run_brew_update_on_refresh, stall_after_secs and upgrade_hard_cap_mins shape execution rather than composition; auto_refresh_on_launch, log_level, auto_open_drawer and auto_check_for_updates cannot touch a plan at all.
- (decision) AD-21 (new) - only plan-determining inputs invalidate a reviewed plan. Binds UX-PB.2b, UX-PB.5b, UX-PB.5c and all settings work. Prevents a settings write anywhere in the app falsely invalidating a reviewed plan, and specifically UX-PB.5b's opt-out deterministically failing the admission it rides on. The canonical revision AD-16 checks for drift advances only on a change to the closed plan-determining set - detection, registry, queue records, tool_env, and the plan-affecting subset of settings. Fail-closed default: a persisted key is plan-determining unless explicitly declared plan-inert with a stated reason where it is defined, because forgetting to classify costs one unnecessary re-review while the reverse executes an unreviewed plan. skipUpgradePlanConfirmation is declared plan-inert - it selects whether a modal renders and cannot alter membership, exclusions, or argv. Splitting the revision does not split the lock: settings stays read under state.plan_coordinator per AD-4.
- (decision) AD-22 (new) - a confirming action is one critical section, and a safety-reducing rider never outlives a failed action. Binds UX-PB.5b, UX-PB.2b, UX-PB.5c. Prevents the two halves of one confirming click landing independently. Three rules: the coordinator lock is held once, unbroken, across validation, admission and any rider, because release-and-reacquire is the window that re-opens drift; ordering is fixed as validate, admit and mint planAttemptId, then persist the rider, so a rider never precedes the admission it rides on; and a rider that REDUCES a safety default commits only if the action succeeded - rejected admission persists nothing and activates nothing while the dialog keeps the user's selection, successful admission with a failed rider save leaves the attempt standing and the prior preference active and persisted with an inline error. The asymmetry is deliberate and is the whole point: an unsaved opt-out costs one extra confirmation, a saved opt-out on a refused run removes the gate from a run the user never got. Compatible with UX-PB.5b's own save-failure criterion, which already says 'the prior preference is retained as both active and persisted state, an inline error is shown, and no partial or legacy value becomes active'.
- (decision) AD-23 (new) - PlanIntent membership carries per-member provenance; the intent-level kind scalar is removed. Binds UX-PB.1a, UX-PB.1c, UX-PB.1d, UX-PB.2a, Story 3.2. Prevents UX-PB.1a emitting a whole-intent kind while UX-PB.1c needs per-member and per-scope provenance - two stories that cannot produce the same wire shape for one fixture-backed model under AD-3. Every member of packageUpdates and managerUpdates carries origin: Explicit or Bulk{scope}. Explicit dominates - a bulk expansion covering an already-Explicit member leaves it Explicit, which is the only enforceable reading of 'never silently absorbed into a later bulk action'; explicitly staging a Bulk member promotes it, never the reverse. scope is a descriptive token recorded at the creating mutation and never re-evaluated, because AD-16 already freezes expansion into concrete members. Removal records a tombstone on the intent - a later bulk expansion of any scope does not re-add a tombstoned ref, which is the only home the 'stays removed' guarantee can have, since a member list cannot record an absence; explicitly re-staging clears the tombstone because a deliberate reversal is not a silent re-add. kind becomes derived for display only. The 'a removal converts AllEligible -> Explicit' clause is deleted rather than reworded: with per-member origin there is no whole-intent value left to convert.
- (decision) AD-24 (new) - the draft has exactly one author, and a derived intent never routes through it. Binds UX-PB.4d, UX-PB.1a, UX-PB.1c, UX-PB.4b. Prevents Retry and the accumulating draft both writing the draft's next state - one discarding membership staged during the attempt, the other inflating the reviewed retry scope and falsifying the retryOfPlanAttemptId lineage. The one persistent draft is written only by a user staging or removal action resolved through the Rust canonical rebuild; admission empties it as custody transfer and a rebuild may narrow it, and nothing else touches it. Retry composes a DERIVED intent in Rust from the failed attempt's reviewed intent restricted to its failed members, canonically rebuilt for current eligibility and argv, and takes it straight to preview and confirmation - a separate reviewable object that never merges with the persistent draft in either direction. Because no draft membership can leak in, the lineage claim stays true by construction rather than by a story remembering to filter. Rejected alternatives: Retry replaces the draft (needs a second holding area for the accumulated membership - a new entity), and Retry refused while the draft is non-empty (punishes the user for staging during a run).
- (decision) AD-17's sidecar visibility rule amended in place with AD-24 (id unchanged). The three-way union - non-empty draft, non-terminal attempt, undismissed Results - had no state for an open retry review, which is reachable from a History entry when all three are false. Now a four-way union with explicit precedence: non-terminal attempt, then open retry review, then undismissed Results, then non-empty draft. This keeps AD-17's 'exactly one instance exists' intact - the retry review is a fourth content state of the same single region, not a second surface - and keeps the accumulated draft alive behind it.
- (decision) HIGH #2 (rubric H2) JUDGED THE SAME SHAPE OF HOLE and taken. AD-16's verification rule says Results distinguish mutation from verification failure 'while preserving the Last-good Snapshot rules' - and the spine defines no such rules, so the reference dangles in a status:final document, which reads as though the rule exists. It is the same shape because two AD-compliant units diverge concretely: project-context.md:121 names the exact trap - 'parse_recovery must MERGE the recovered overlay into the inventory already parsed from the successful refresh outputs - replacing the snapshot with an outdated-only overlay makes every up-to-date package vanish. Merging never un-pins a row.' Story 2.2's refresh-failure path and UX-PB.3d's verification-refresh-failure path will each re-derive merge-vs-replace and can pick differently. The invariant is load-bearing in three driving sources: docs/SPEC.md:17 under 'Load-bearing invariants (violations are bugs)' - '5. One failing manager never blanks the others. Per-manager refresh isolation: independent operations, timeouts, error cards; prior snapshots are retained on failure.'; epics.md:105 NFR-2 'without blanking another Manager or destroying a Last-good Snapshot'; EXPERIENCE.md:173 'On refresh failure, retain the last-good snapshot, show its timestamp, set System health to Warning, and provide Retry refresh plus the specific known failure.' Given as its own AD-25 rather than an AD-4 rule so AD-16 has a stable citation target - memlog entry 102 already recorded that rule ordinals rot under edit.
- (decision) AD-25 (new) - a Manager failure is contained and never destroys a Last-good Snapshot. Binds Story 2.2, UX-PB.1e, UX-PB.3d, UX-PB.2b; closes AD-16's dangling reference. Failure in one Manager's detection, refresh, parse, network, update, cancellation, timeout or persistence path is contained to that Manager; peers keep running and keep rendering. A Manager that has ever produced a successful snapshot retains it on failure, labeled with its own timestamp and the exact failure, with a Retry affordance - never replaced by an empty snapshot and never by a partial overlay, because recovered-parse output MERGES into the inventory already parsed from the successful outputs and merging never un-pins a row. A verification refresh is bound by the same rule: a failed or timed-out verification marks the attempt's verification failed and leaves the snapshot in place, because a failed verification must not destroy the inventory that would prove what happened. Health and staleness presentation derive from the snapshot's real timestamp; no invented value is ever substituted.
- (version) Tauri E2E platform support verified on the web this run rather than asserted, because the whole of HIGH #1 turns on it. Official Tauri v2 docs, tauri.app/llms-full.txt: 'For end-to-end testing, Tauri utilizes the WebDriver protocol, compatible with both desktop and mobile platforms, with the exception of macOS.' and 'Tauri supports WebDriver through a cross-platform wrapper called tauri-driver, which utilizes the native platform's WebDriver server. Support is available on Windows and Linux, but not macOS due to the lack of a WKWebView driver tool.' The v2.tauri.app WebDriver page confirms: 'Driven directly, only Windows and Linux are supported on desktop, as macOS has no WKWebView driver tool available', and the macOS route is the WebdriverIO service where 'By default the service runs an embedded WebDriver server inside your app, so no external driver is needed on any platform - and this is how macOS is supported.' CrabNebula's tauri-driver fork likewise requires tauri-plugin-automation installed in the app. Pack-Manager is macOS-only in practice (tauri.conf.json:48 minimumSystemVersion 15.0). No automation, webdriver, tauri-driver or wdio dependency exists in package.json or Cargo.toml today.
- (decision) HIGH #1 (rubric H1) JUDGED: not the same shape as the three CRITICALs as the rubric framed it, but the web check turned it into that shape, so it is taken. The rubric framed it as an undecided-and-unowned choice - one story unbuildable, not two stories diverging. That framing is correct but incomplete. Because every macOS-viable native-E2E route embeds an automation surface INSIDE the application (an embedded WebDriver server, or tauri-plugin-automation), the real hole is a genuine divergence pair: a builder satisfying Story 6.5 can add the automation plugin to the shipping app - a release-bits and trust-boundary regression - or gate it to a non-distributable target. AD-2 forbids the first ('Release builds contain no feature flag, environment variable, CLI option, hidden command, or runtime selector that can activate a controlled adapter') and AD-20 makes adding a plugin a security-sensitive change reviewed on its own terms, but neither AD mentions test automation, so neither reads as governing this and a builder will not connect them. Made explicit as AD-26.
- (decision) AD-26 (new) - a native automation surface never reaches release bits. Binds Story 6.5, any native E2E harness adoption, AD-2, AD-3, AD-20. Prevents Story 6.5's required test level being satisfied by shipping an automation plugin or an embedded WebDriver server in the distributed app. Rules: Tauri's own WebDriver route does not cover macOS, so every macOS-viable route embeds an automation surface in the application, which makes harness adoption a trust-boundary decision under AD-20 and a release-bits decision under AD-2 rather than a test-tooling detail. If a harness is adopted its automation surface is a construction-time dependency of a non-distributable target only - no release build carries an automation plugin, an embedded driver, a capability granting it, or any flag, variable or argument that could activate one - and a harness that cannot meet this is not adopted. Until such a composition exists and is verified, no story may claim native-transport coverage; AD-3 already closed the fixture and browser-double routes, and this closes the third one, claiming it from an automation build that would never ship. The remedy for Story 6.5 is a decision at this altitude or a renegotiated test level, never a widened trust boundary in the shipping app.
- (decision) AD-11's accessibility rule corrected. It read 'Automated 4.5:1 text-contrast and reduced-motion checks belong in the Playwright/Vitest lane - neither exists yet, so this is an obligation on whichever story adds them, not a description of current coverage.' The 'neither' is false: reduced motion is automated and runs in CI. The rule now states reduced motion as shipping coverage, cites the spec and the CI lane, and narrows the standing obligation to contrast alone. Note this is the SECOND time this rule has been wrong in opposite directions - memlog entry 83 records revision 5 correcting it from a present-tense claim that both checks existed. Overcorrecting a false present-tense claim into a false absence is the same failure with the sign flipped, so the rule now carries its evidence inline rather than a bare assertion either way.
- (constraint) epics.md divergences found this run and RECORDED rather than edited, per owner instruction, as one Decision Status Open row for the single bmad-correct-course run that follows revision 8. (a) UX-PB.1b - the crash/relaunch criterion still permits the draft-reconstruction branch AD-17 forbids, carried from revision 7. (b) UX-PB.1c - VERBATIM from epics.md: 'Given a draft seeded by Update Everything as an AllEligible intent When I remove any item Then the draft converts to an Explicit intent of the surviving PackageRefs and Manager self-update identities and rebuilds the authenticated preview from the backend, never from edited display text.' AD-23 removes the whole-intent kind, so the stated MECHANISM no longer exists, though the observable outcome survives and is strengthened - the surviving members are what rebuilds and nothing re-expands. Restate as member removal plus a tombstone. (c) UX-PB.4d - VERBATIM: 'Create new plan rebuilds current canonical intent into a new reviewable draft'. Under AD-24 that must be the derived RetryIntent, not the one persistent draft; the indefinite article is currently carrying the entire distinction, which is too thin for a builder. (d) Story 6.5 - its 'Required test level: Real native Tauri E2E plus artifact inspection' plus the DEFERRED register row that still cites only AD-2 and AD-3 both need AD-26. (e) UX-PB.5b - its criteria cover successful admission and failed save but never a REJECTED admission carrying the opt-out rider; AD-22 decides it and the criterion should say so.
- (constraint) Ripple and hygiene checks run before the gate, all verified rather than assumed. Both mermaid diagrams re-rendered with @mermaid-js/mermaid-cli 11.16.0 after the state-diagram edit - d0 flowchart and d1 stateDiagram-v2 both produce SVG, and RetryReview appears in the rendered output; a semicolon inside the new transition label was replaced with a comma before rendering. Deterministic lint_spine.py: 0 findings, re-run after every edit batch. Residual-reference sweep: 'AllEligible' now appears exactly once in the spine, inside the Open row that deliberately quotes epics.md; the two remaining 'kind:' hits are the unrelated ui.dialog { kind: "upgradePlan" } baseline description and the kind:name package-id convention. AD heading inventory is 1-5, 11, 12, 16-26 with zero headings for the retired 6-10 and 13-15. Stack table re-verified against package-lock.json and src-tauri/Cargo.lock: TypeScript 7.0.2, Vite 8.1.5, React 19.2.8, Zustand 5.0.14, Vitest 4.1.10, Playwright 1.61.1, Tailwind 4.3.3, Tauri JS API 2.11.1, Tauri CLI 2.11.4, TanStack React Virtual 3.14.8, tauri 2.11.5, tokio 1.53.1, tauri-plugin-updater 2.10.1, tauri-plugin-opener 2.5.4 - every row exact, no edit needed.
- (decision) AD-25 marked [ADOPTED] rather than left as a new call, because it ratifies existing reality on both halves. docs/SPEC.md declares containment a load-bearing invariant ('violations are bugs'): '5. One failing manager never blanks the others. Per-manager refresh isolation: independent operations, timeouts, error cards; prior snapshots are retained on failure.' And the merge seam already ships - ManagerAdapter::parse_recovery in src-tauri/src/managers/mod.rs takes 'refresh_outputs: &[CommandOutput]' alongside the failed command's own output, which exists precisely so the recovered overlay can merge into what already parsed. Discarding that argument is the defect the rule names, so the AD ratifies a real signature rather than inventing one. AD-21..AD-24 and AD-26 stay untagged: they are new calls, not adopted reality.
- (constraint) CRITICAL, self-inflicted, caught by the currency lens and confirmed by me against raw bytes: AD-26's load-bearing quote was FABRICATED. I wrote 'Support is available on Windows and Linux, but not macOS due to the lack of a WKWebView driver tool' and stamped it 'tauri.app, verified 2026-07-25'. That sentence does not exist in the source - 'curl -sL https://tauri.app/llms-full.txt | grep -c' returns 0. It was Context7's SUMMARY of the docs, which I treated as verbatim because the tool labelled it 'Source: https://tauri.app/llms-full.txt'. A retrieval tool's paraphrase is not a quote, and stamping it 'verified' made an unverified claim look checked. The real text at llms-full.txt:9532 is 'Driven directly, only Windows and Linux are supported on desktop, as macOS has no WKWebView driver tool available (use the service's embedded WebDriver server for macOS)' - which is about tauri-driver driven DIRECTLY, not about Tauri's WebDriver support as a whole. My derived claim 'Tauri's own WebDriver route does not cover macOS' therefore inverted the fact: llms-full.txt:9483 says the recommended route '@wdio/tauri-service' 'works on Windows, Linux, and macOS'.
- (decision) AD-26 rewritten on verified source, and the correction makes it STRONGER rather than weaker. Verbatim, all confirmed by grepping the raw file this run: llms-full.txt:9483 'The recommended way to use it with Tauri is WebdriverIO and the @wdio/tauri-service, which works on Windows, Linux, and macOS.'; :9485 'By default the service runs an embedded WebDriver server inside your app, so no external driver is needed on any platform - and this is how macOS is supported.'; :9511 'tauri-plugin-wdio-webdriver runs the embedded WebDriver server. It's required for the embedded provider (the default)'; :9532 the tauri-driver-direct exclusion. The WebdriverIO docs give the canonical registration as '#[cfg(debug_assertions)] let builder = builder.plugin(tauri_plugin_wdio_webdriver::init());' - COMPILE-TIME exclusion, which is exactly AD-2's test and stronger than the runtime selector AD-2 forbids. Verified src-tauri/Cargo.toml declares no [profile.release], so debug-assertions is off in release and the gate holds today. AD-26 now names the route, the reference gating shape, the profile dependency that gating rests on, and the production-composition requirement, instead of resting on a paraphrase.
- (decision) HIGH #1's CONCLUSION REVERSED on evidence. Revision 7 recorded, and I initially agreed, that Story 6.5 is not buildable from this spine. That is wrong: a compliant composition exists and is the documented default. The Deferred/Open row moves from 'OPEN - blocks Story 6.5 ... what remains undecided is whether a compliant non-distributable composition exists at all' to 'OPEN - owner Story 6.5; shape named, not yet adopted'. What genuinely remains is the adoption decision - a new plugin is an AD-20 security-reviewed change - plus one procurement fact worth recording because it constrains one option: llms-full.txt:9485 says the CrabNebula cross-platform tauri-driver fork works 'on all platforms (a paid API key is required for macOS)'. Consequently epics.md divergence item (d) narrows: Story 6.5's 'Real native Tauri E2E plus artifact inspection' test level needs NO renegotiation, only the register row's AD citation.
- (constraint) HIGH, out of this run's scope but recorded because the spine asserts it: macos-14 is on a dated retirement clock and it is release-blocking. Verified at actions/runner-images issue 13518 - 'Deprecation will begin on July 6th, 2026 and the images will be fully unsupported by November 2nd, 2026', after which 'Workflows using the macos-14, macos-14-large, macos-14-xlarge image labels will be terminated with an error'. Deprecation began nineteen days before this revision. Verified pins: .github/workflows/ci.yml:25, ci.yml:67, and .github/workflows/release.yml:62. After 2026-11-02 Pack-Manager cannot cut a signed notarized release at all. AD-11's rule previously asserted 'CI stays on macos-14' flatly; it now states the clock. Not fixed here - the runner move is workflow work and this run was scoped to invariants - and it perturbs the still-open notarytool minos 15.0 residual, because a newer image ships a newer SDK. Recorded as its own dated Open row.
- (decision) AD-22 rules 1-2 REWRITTEN after two independent lenses found the unbroken-critical-section rule unbuildable, and I verified every claim in the tree myself. Facts: src-tauri/src/state.rs:8 'use std::sync::{Arc, Mutex, RwLock};' and :212 'pub plan_coordinator: Arc<Mutex<PlanCoordinator>>' - non-reentrant, guard not Send. commands.rs:351-353 documents the constraint outright: 'Consumes and re-validates a backend-issued plan against one canonical revision, then asks the scheduler to atomically re-check that revision and enqueue the complete batch. No synchronous guard crosses an await.' The guard's block ends at commands.rs:460 '(subs, coordinator.revision())' and the admission is commands.rs:462-465 'state.queue.submit_plan_batch(subs, expected_revision).await'. queue.rs:1010-1011 re-acquires the SAME lock in handle_plan_batch and re-checks 'coordinator.revision() != expected_revision'. So my rule would not compile, and would deadlock if it did. My justification sentence was also wrong in context: set_settings_core does hold the lock across save_to (true in isolation), but the ADMISSION path deliberately drops the guard, so citing shipping practice to justify holding across admission inverted what shipping practice actually does. Corrected invariant: the confirming action is atomic against the canonical REVISION, not one mutex hold - the scheduler's expected_revision re-check IS the critical section, and it already ships. Added an explicit prohibition on holding the coordinator across admission or persisting under a held guard (settings.rs:128-146 save_to does create_dir_all, File::create, write_all, sync_all fsync, rename).
- (decision) CRITICAL caught by the reconcile lens: AD-24 and AD-17 defeated each other on the exact path both were written for. AD-17 said 'The draft is emptied atomically with the mint of planAttemptId' unqualified, in four places including the normative state diagram, and AD-24's own rule 1 repeated it. A confirmed retry mints a planAttemptId, so it emptied the draft - destroying verbatim the accumulated membership AD-24's Prevents clause exists to protect. Fix: custody transfer is scoped to WHAT WAS ADMITTED, not to the mint. Admitting the draft's own preview empties the draft; admitting a derived intent consumes that intent and leaves the draft and its tombstones untouched, because the draft was never its source. Qualified in all four places - AD-17's custody rule, AD-24 rule 1, AD-23's tombstone-lifetime rule, and the state diagram, which now routes RetryScope -> RetryPreview -> Admitted with its own 'draft NOT emptied' arrow instead of inheriting the draft-emptying Preview arrow. Diagram re-rendered with mermaid-cli; RetryScope and RetryPreview both present.
- (decision) AD-17's four-way visibility precedence REVERTED to the three-way union; it was over-engineered and contradicted every input. Both lenses found it independently. EXPERIENCE.md:231 'It first reveals the failed-item retry scope INSIDE Results with Cancel and Create new plan; Cancel closes the scope and returns focus to the Retry action.' DESIGN.md:216 lists 'retry scope' as a STATE of the Results Summary component. epics.md UX-PB.4d says 'reveals the proposed failed-item scope inline'. My precedence made the retry review outrank and hide Results, which deletes the Retry action that Cancel must return focus to and removes the failure detail the user is deciding against; it also hid a retry opened from History behind a live attempt, breaking UX-PB.4c's live-plus-replay case, and made staging an item during a retry review render two different things under UX-PB.1b. Fix: the retry scope is not a member of the union at all - it is a content state inside the surface Retry was invoked from (Results, or a read-only History replay). Simpler, matches every input, and eliminates all three reachable bad combinations rather than patching them.
- (decision) AD-21's plan-determining test widened after the divergence lens showed it excluded execution parameters. The three-part test (membership, exclusions, argv) classified upgrade_hard_cap_mins and stall_after_secs as inert, while the fail-closed default classified them as determining - the test and the default pulled opposite ways, so two builders reading the same AD would classify the same key differently. The test now reads 'membership, its exclusions, the argv Pack-Manager would construct, or any execution parameter the reviewed snapshot records - timeouts and stall thresholds included, because the user reviewed a plan that runs under them.' Test and default now agree.
- (constraint) epics.md batch row corrected and extended from five items to six after the reconcile lens showed three of the five understated their finding. (b) UX-PB.1c: both halves of the criterion need work, not just the conversion - the SEED clause treats AllEligible as a live-predicate intent, which AD-16's frozen-expansion rule already forbade, so 'the observable outcome survives' was true only of the conversion half. (d) four epics.md locations still frame the native harness as simply Deferred, not two; the worst attributes the deferral to THIS spine - 'The native Tauri harness is Deferred there, with Story 6.5 as its only live consumer' - and Story 6.5's test level needs no renegotiation now that AD-26 names a compliant shape. (e) UX-PB.5b is not a missing case but a direct CONTRADICTION: its criterion states persist-activate-admit and AD-22 fixes admit-then-persist, so the spine deliberately overrides the story and AD-22 now says so in its own Rule rather than leaving the override implicit. NEW (f): epics.md's D33 restatement carries the identical false claim AD-11 just fixed - 'Neither automated check exists yet, so this is an obligation on whichever story adds them, not a description of current coverage.' Verified: reduced motion exists and runs in CI; only contrast is outstanding.
- (decision) Rubric H1 applied: AD-16's durable-vs-derived answer extended to Cancelling and Interaction required. Verified src-tauri/src/ipc.rs:99-107 - OpStatus ships exactly seven variants (Queued, Running, Succeeded, Failed, Cancelled, TimedOut, Interrupted) with no Cancelling, Verifying or Skipped. The spine had decided the durability question for two of the four states its own bound stories need, and the asymmetry made silence on the other two read as deliberate. Interaction required was especially exposed: the spine said it is 'emitted', which points at an event, and the shipping precedent is an event with no status variant (events.rs EVENT_OP_STALLED with no Stalled in OpStatus) - so UX-PB.3f would reasonably have built a transient flag that UX-PB.4b's replay then cannot reconstruct. Rule added rather than a new AD, because it completes a decision AD-16 already half-made.
- (question) Rubric H2 RECORDED as an Open row rather than decided, because it is a product call and outside this run's authorized scope. Verified conflict in the tree: src/styles/theme.css:7 '--color-bg-base: #0B0E14' and :21 '--color-accent: #4F8CFF' with zero focusRing tokens (grep -c returns 0), and tests/e2e/browser-style-contract.spec.ts:66-67 asserts backgroundColor 'rgb(11, 14, 20)' = #0B0E14 on every push and PR to main. Against that, DESIGN.md:111 background #090C13, :123 accent #65A7FF, :119 a dedicated focusRing #F4F7FB, and EXPERIENCE.md:318 makes the separate focus ring normative while docs/SPEC.md:197 specifies a 2px --color-accent ring. UX-PB.1e and UX-PB.5d are both bound to build from DESIGN.md/EXPERIENCE.md, so whichever lands first either rewrites the tokens and breaks the CI style contract - the same lane AD-11 now depends on for reduced motion - or ships accent focus rings EXPERIENCE.md forbids. Passes the spine's inclusion test cleanly, but choosing between the shipping palette and DESIGN's is a design decision the owner makes, not one architecture should make unilaterally.
- (event) Reviewer gate revision 8 complete: four lenses - rubric walker, currency/reality, adversarial divergence, input reconciliation. Rubric verdict READY WITH FIXES. Counts taken from each lens's own tally rather than estimated: rubric 1 CRITICAL / 3 HIGH / 5 MEDIUM / 3 LOW, currency 1/3/2/2, divergence 4/4/4/2, reconcile 2/5/6/3 = 8 CRITICAL, 15 HIGH, 17 MEDIUM, 10 LOW, 50 total. Heavy overlap at the top: three of the eight CRITICALs are the same AD-22 defect found independently by three lenses, each proposing the same remedy. Currency checked 82 claims and verified 80 exactly. Every CRITICAL was in revision 8's NEW material rather than in the inputs. All eight resolved: AD-26's fabricated quote, AD-22's unbuildable critical section (x3), AD-22's ordering override left implicit, AD-21's execution-parameter gap, AD-24/AD-17 custody collision, and one stale tombstone finding already closed mid-review. Two HIGHs applied (Cancelling/Interaction required durable states; macos-14 clock), one HIGH recorded as an owner decision (canonical design-token set). Reconcile re-read after the fixes and records 'AD-21 and the rewritten AD-22 and AD-26 are clean'.
- (event) spine finalized at revision 8; deterministic lint zero findings after every edit batch including the final one; both mermaid diagrams re-rendered valid after the lifecycle diagram gained the RetryScope/RetryPreview path; three CRITICAL Open rows from revision 7 closed by AD-21 through AD-24, rubric H2 closed by AD-25, rubric H1 closed by AD-26 with its premise corrected; AD-11 accessibility corrected; epics.md untouched with six divergences recorded for one bmad-correct-course run; three new Open rows added that this run did not resolve - canonical design-token set, macos-14 runner retirement, and the pre-existing app-update guard enforcement point.
- (decision) Verification pass on the gate's own fixes found one HIGH I had introduced, and it is a good lesson about over-correcting. Reverting AD-17's visibility to three-way removed the retry SCOPE from the union correctly, but silently dropped the retry PREVIEW with it - the old four-way member 'an open retry review' had been covering both by accident. RetryPreview is not the draft (AD-24 forbids merging), not yet an attempt, and EXPERIENCE.md says Create new plan REPLACES Results in three separate places (:156 'deliberately replaces Results with a draft', :231 'deliberately replaces the sidecar', :430 'replaces Results with a fresh reviewable draft') - so all three union members go false and the derived plan the user is being asked to confirm renders nowhere. Fix: four-way union again, but with the RIGHT member - 'a derived intent under review', ranked above Results because Create new plan is meant to replace Results, with View previous result as the way back. H-1's original objection does not apply: it objected to the retry SCOPE outranking Results, and the scope stays inside Results. The scope and the preview are now explicitly distinguished.
- (decision) Three more verification-pass fixes applied. (1) AD-22 rule 2's persist ban was unqualified - 'Nothing may be persisted under a held coordinator guard' - which condemned the exact shipping sequence AD-19 holds up as the reference at set_settings_core, where taking the guard and then saving under it is correct. That is the mirror image of the original C-1 error: the first version cited set_settings_core to license too much, this one banned the pattern too broadly. Scoped to 'No confirming action may persist under a held coordinator guard' and said so explicitly. (2) AD-17's announcement channel was fixed as POLITE, dropping EXPERIENCE.md:322's escalation - 'It uses polite priority by default and assertive priority only for an immediate safety action'. The stall handoff and Interaction required are exactly such actions, and a polite region is announced only when the user is otherwise idle, so a VoiceOver user working elsewhere misses a safety prompt. Taken rather than deferred because the failure mode is silent - the neighbouring sentence 'Two live regions narrating one attempt is a defect' actively pushes an implementer away from the correct escalation, so a wrong build looks compliant. Brief-notification suppression added from EXPERIENCE.md:323. (3) Batch items (d) and (f) each enumerated fewer locations than they claimed; verified and completed - epics.md:280's Governance row for (d), epics.md:304's DR-2 register row for (f).
- (constraint) NEW epics.md divergence recorded as batch item (g): UX-PB.4b and UX-PB.4d contradict each other on Retry from History. UX-PB.4d offers Retry from 'a terminal Results or History entry with failed items'; UX-PB.4b's replay criterion says 'no control in the replay can mutate, re-run, or execute anything'. The UX sources side with 4b - DESIGN.md gives Results Summary the retry-scope state while History Plan Row gets only an optional source-retry link and a retry-linked state, a lineage pointer rather than an action. This spine takes UX-PB.4d's side on the ground that revealing a scope executes nothing and any execution still goes through the ordinary preview-and-confirmation path, so UX-PB.4b needs an explicit carve-out for the non-executing affordance or UX-PB.4d's History origin has to go. Recorded rather than resolved silently: taking a side is the spine's job, recording it is too.
- (decision) Divergence lens verification pass returned five findings against the pre-fix file; W-1 (HIGH - precedence declaration and the never-destroys guarantee deleted along with the fourth member) was already closed by the AD-17 four-way restoration before its report landed. Four applied. W-2: because the retry scope now lives INSIDE Results, Done could discard a scope the user was reviewing - dismissal is now withheld while a scope is open until the user resolves it with Cancel or Create new plan. W-3: two Decision Status rows still advertised the repudiated wording - one described AD-22 as 'one unbroken critical section', the exact phrasing AD-22 now says would not compile, and the other described AD-17's fourth state imprecisely; both restated. W-4: AD-21's argv clause was unscoped, and run_brew_update_on_refresh builds refresh argv, so Story 2.2 and Story 3.4 would classify it oppositely - scoped to 'the argv of the operations this plan would run, its verification refreshes included, so a key that shapes refresh argv is plan-determining'. W-5: AD-16's older rule said Interaction required is 'emitted', which after the new durable-state rule literally described the defect that rule names; changed to 'entered', with any event accompanying the state rather than standing in for it.
- (event) Revision 9 run opened 2026-07-25, resumed from this memlog. Intent: update. Scope set by the owner: (1) close the 'epics.md divergence batch for bmad-correct-course' Open row as RESOLVED - all seven items (a)-(g) applied plus three follow-ups, recorded at _bmad-output/planning-artifacts/sprint-change-proposal-2026-07-25-spine-rev8.md; (2) drop the now-false UX-PB.1b residual sentence from the 'epics.md retired register' row; (3) fold in three repo landings the spine states differently - macos-14 runner retirement closed by commit 419dc32 / DECISIONS D34 (which also settles the D31 notarytool minos-15.0-vs-SDK-14.5 residual), canonical design-token set closed by commit be1f0e6 / DECISIONS D35, and two pre-existing AD-11 accessibility defects logged to deferred-work.md. epics.md is OUT OF SCOPE and must not be edited; further divergence is recorded as Open rows. Nothing may be committed or staged - another agent is working in this tree. AD ids stay stable; AD-6..AD-10 and AD-13..AD-15 remain retired and are never reused.
- (constraint) macos-14 retirement VERIFIED CLOSED against the tree this run, not taken on the owner's word. All three pins now read macos-15: .github/workflows/ci.yml:28 'runs-on: macos-15' (rust job), ci.yml:70 'runs-on: macos-15' (build-smoke), .github/workflows/release.yml:63 'runs-on: macos-15' (build). grep for macos-14 across .github/workflows/ returns only two explanatory comments - release.yml:61 and ci.yml:10 - and zero runs-on pins. Recorded as docs/DECISIONS.md:367 'D34. CI and release build on macos-15; the macos-14 pin is retired', which cites actions/runner-images#13518 for the same 2026-07-06 / 2026-11-02 dates the spine carried. Commit 419dc32 'ci: move CI and release builds off the deprecated macos-14 runner' also updated docs/SPEC.md 7.6, which now reads 'rust (macos-15)' and 'build-smoke (macos-15, main only)'. Its commit body states signing and notarization on the new image are proven by a manual Release run.
- (constraint) The D31 notarytool residual is settled BY D34 rather than by a separate test, and the closure is the source's own words rather than my inference. docs/DECISIONS.md:381-385 verbatim: 'This also closes the question D31 left open. D31 recorded that a deployment target above the build SDK is a floor annotation rather than an SDK requirement, and that whether notarytool accepts minos 15.0 against SDK 14.5 was OPEN. On macos-15 the build SDK is no longer behind the declared 15.0 floor, so the mismatch that question was about no longer exists.' Note D31's own text at DECISIONS.md:269 still reads 'CI therefore stays on macos-14' and its OPEN paragraph at :271-276 is unedited - D34 supersedes rather than rewrites it, which is this repo's documented supersedence order, so the spine cites D34 for the closure and must not cite D31 alone for the residual any more.
- (constraint) Canonical design-token set VERIFIED CLOSED. The exact conflict the Open row named is gone: src/styles/theme.css:8 now reads '--color-bg-base: #090C13;' and :27 '--color-accent: #65A7FF;' - DESIGN.md's values, not the #0B0E14 / #4F8CFF stubs - and the missing token exists at :19 '--color-focus-ring: #F4F7FB;'. Recorded as docs/DECISIONS.md:392 'D35. The approved design palette is adopted and focus gets its own ring', which resolves the focus half against EXPERIENCE.md:318 verbatim: 'Every interactive element uses a separate {colors.focusRing} indicator that is at least 2px wide and visible against every surface.' docs/SPEC.md:206 was moved with it and now reads 'Focus: 2px --color-focus-ring ring, offset against surface, on every interactive element - a dedicated indicator, never --color-accent.' D35 also records the CI assertion moving in the same change 'including a negative guard that the focus ring is not the accent', and one deliberate survivor at src/components/manager/PackageRow.tsx:85, a navigation highlight with no focus-visible prefix. Commit be1f0e6.
- (constraint) The two accessibility defects verified by count rather than accepted. 'grep -rn focus-visible src --include=*.tsx | wc -l' returns 22 and 'grep -rn ring-offset src --include=*.tsx | wc -l' returns 6, so 16 of 22 focus sites carry no ring-offset - the owner's figure is exact. The three no-indicator controls carry no focus-visible class at all: src/components/manager/PackageToolbar.tsx:72 is a checkbox className 'h-4 w-4 rounded-[4px] border border-border-strong bg-bg-raised accent-accent', src/components/activity/ActivityDrawer.tsx:83 a button with only 'hover:bg-bg-raised', src/components/shell/StatusBar.tsx:53 the reveal-logs button. Both are logged in _bmad-output/implementation-artifacts/deferred-work.md against source spec spec-adopt-design-tokens-and-focus-ring.md, the second recording they were 'Measured in a real browser by walking the Tab order and reading computed style: each reports :focus-visible true with boxShadow: none'. Both predate D35, which retargeted the colour of rings that already existed and did not add rings.
- (constraint) epics.md divergence batch VERIFIED item-by-item against the working-tree file by an independent lens, not accepted on the owner's report. All seven items landed with the offending text gone rather than merely annotated. (a) UX-PB.1b:564-567 now reads 'it starts with an empty draft and a hidden sidecar - the draft is session-scoped and never written to disk, so membership is never reconstructed, never partially restored, and never fabricated'; grep -cF 'reconstructed into the sidecar' = 0, so AD-17's forbidden branch is gone, not softened. (b) UX-PB.1c:588-590 restates the seed as frozen ('whose expansion was frozen into concrete members at the moment I invoked it - each carrying Bulk { scope: Everything } provenance that is never re-evaluated') and the conversion as removal plus tombstone, ending 'no whole-intent kind is stored or converted'; AllEligible now appears exactly once in epics.md, as that negation. (c) UX-PB.4d:984 names 'a derived RetryIntent in Rust ... without ever writing to, merging with, or emptying the one persistent draft'; 'new reviewable draft' = 0 hits. (d) all four native-harness locations cite AD-26 (epics.md:171-176, :285, :314, :1294-1296); 'awaiting the deferred native harness' and 'harness is Deferred' both 0. (e) UX-PB.5b:1058 states 'validate, admit through the scheduler's revision-checked transaction, then persist the rider' and gains the rejected-admission case at :1061-1063. (f) both accessibility places corrected (:265, :310); 'Neither automated check exists yet' = 0. (g) UX-PB.4b:948 carries the explicit carve-out 'with exactly one carve-out: the non-executing Retry affordance UX-PB.4d offers from a History entry'. Follow-ups also landed: AD-25 cited 4x (:622, :664, :827, :1159) against 0 in HEAD, Story 3.2 restored to the surviving list at :351-352, and the design-token blocker row added at :308. All 18 live AD ids are cited at least once; AD-7/8/9/14 have zero hits and AD-6/10/13/15 appear only inside the retired-id collision block at :208-219.
- (constraint) TWO NEW epics.md residuals found by the verification lens, to be recorded as an Open row rather than edited, per owner instruction. (1) UX-PB.3d cites AD-25 at epics.md:827 but its verification-failure criterion at :843-845 carries no snapshot-preservation clause - it reads only 'the item does not declare success - it stays Verifying until it resolves, then reports verification failure with its evidence, and is never colored successful on the strength of the exit code alone'. AD-25's rule that a failed or timed-out verification 'leaves the snapshot in place' is therefore cited but not stated where the story would be built from. The correct-course proposal discloses this itself at its own section 7d, so it is a known residual rather than a missed item. (2) AD-21 is the one new invariant whose substance never reaches criterion text: it exists only as the parenthetical gloss on UX-PB.5b's Dependencies line at :1045, 'AD-21 (skipUpgradePlanConfirmation is declared plan-inert)', while AD-22's and AD-23's substance IS restated in criterion prose. Not an overstatement - the proposal claimed only a Dependencies addition for it - but it means a builder reading UX-PB.5b's criteria alone never learns that the plan-inert declaration is what stops the opt-out invalidating its own admission, which is the exact failure AD-21 exists to prevent.
- (decision) Runner rows closed and AD-11's macOS rule amended in place (id unchanged). The 'macos-14 runner retirement' Open row is RESOLVED and the 'Minimum supported macOS' row drops its residual, both citing D34 rather than D31 alone, with an explicit note that D31's own text still says 'CI therefore stays on macos-14' and is superseded rather than rewritten - otherwise a later reader checking the cited authority finds it contradicting the spine, which is the exact failure the currency lens caught twice already. AD-11's rule was rewritten to carry the INVARIANT rather than the changelog: the runner is a named stable image and never macos-latest, because a floating label moves the signing and notarization environment without a commit. That is the durable call; macos-15 is the current value of it. The floor-annotation reasoning is kept but explicitly denied as a licence to let the image drift behind the declared floor, which is what let the old pin sit under an open notarytool question for a month. Structural Seed's CI runner row corrected to macos-15 with the three job names.
- (decision) Canonical design-token Open row closed as RESOLVED, and the closure was pushed down into the Styling convention rather than left in the status table. The status row records WHAT was decided (D35, commit be1f0e6); the convention now carries the rule a builder actually reads: 'The values are DESIGN.md's, adopted under docs/DECISIONS.md D35 - a story proposing different ones is proposing a new decision, not implementing this one. Focus resolves --color-focus-ring and never --color-accent, so selection and focus stay distinguishable.' Without that, the row's own complaint - 'The Styling convention fixes where tokens live and never says which' - would still be true of the convention after the status table said RESOLVED, and UX-PB.1e and UX-PB.5d read conventions, not the status table. This is the same divergence pair the row named; closing it in the table alone would have left it open where it bites.
- (decision) AD-11's accessibility rule amended in place (id unchanged) and given two new rules, and this is the THIRD time the rule has been touched - revision 5 corrected a false present-tense claim that both automated checks existed, revision 8 corrected the overcorrection that neither did, so the deciding constraint this run was to write something that cannot go stale again. It therefore states the INVARIANT rather than the current CSS: 'A focus indicator must be drawn by a mechanism the shipping engine actually paints, and proven in WebKit rather than Chromium alone.' outline plus outline-offset is named as what satisfies it today, with the requirement explicitly being that focus be painted where the user runs it rather than that any particular property is used. Written this way deliberately because the remediation is in the working tree and uncommitted as I write, so any rule phrased as 'focus is an outline' would have been a false present tense the moment it was written - the exact failure mode of the two prior corrections. The WebKit constraint was judged invariant-worthy rather than deferred hygiene: it is not merely a bug but a coverage-sample hazard of the AD-3 shape, and the divergence pair is real - a story copying the existing sites ships an invisible focus state on form controls while a story following DESIGN.md ships a visible one.
- (constraint) The WebKit focus-paint defect VERIFIED from the committed tree rather than accepted from the report, and the verification is what settled the invariant question. tests/e2e/browser-style-contract.spec.ts at HEAD focuses a BUTTON - 'const refreshAll = page.getByRole("button", ...)' then 'await refreshAll.focus()' - and asserts 'expect(focusTreatment.boxShadow).not.toBe("none")' plus the token colour rgb(244, 247, 251) and the negative guard against accent rgb(101, 167, 255). A button is not a native-appearance form control, so WKWebView paints its box-shadow and the assertion passes on both engines. Meanwhile six native checkboxes ship - Checkbox.tsx:29, PackageRow.tsx:90, PackageTable.tsx:100, PackageToolbar.tsx:68, UpgradePlanSheet.tsx:218 and :229 - and Tailwind ring-* compiles to box-shadow, which WKWebView does not paint on those. So the suite was green on both engines while the plan-membership checkbox had no visible focus at all. That is why this is an invariant and not deferred hygiene: the lane's sample did not represent its population, which is precisely the limit AD-3 already states for the contract fixtures and the browser double, and nothing generalized it to the style lane.
- (decision) Three more rows closed and two opened. App-update safety guard enforcement point CLOSED (rubric H4, open since revision 6) - verified at src-tauri/src/commands.rs:810 'refuse_app_update_while_busy(&state.queue.records())?' called before install, the helper at :772 refusing on OpStatus::Queued | OpStatus::Running, and the frontend predicate it must match at src/store/operations.ts:137 filtering 'o.status === "queued" || o.status === "running"' - identical sets, which is the point, since the defect was a frontend-only convention that could drift from what actually holds. Tests at commands.rs:858-890 cover empty, all five terminal statuses, both active, and mixed. Reuse of ErrorCode::SelfUpdateUnavailable is correct under AD-3 rather than lazy: a new variant is an atomic boundary change across Rust, TypeScript, the guard map and the committed fixtures. epics.md divergence batch CLOSED as RESOLVED citing commit 8d36cdf - the working-tree qualifier I had planned is unnecessary because the owner committed epics.md and the proposal together while this run was in progress, and git status on both is now clean. epics.md retired-register row loses the UX-PB.1b residual sentence, which item (a) falsified, and loses its 'all twelve live AD ids' count, which revision 8 falsified by adding AD-21..AD-26 - a bare number in a status row rots the same way the rule ordinals and line citations did, so it is replaced with 'every live AD id' rather than corrected to eighteen. Two new Open rows: the UX-PB.3d and AD-21 epics.md residuals, and the focus-indicator remediation.
- (decision) AD-11's focus-paint rule tightened after the owner corrected the discriminator, and the correction makes the rule usable rather than merely narrower. My first phrasing said WKWebView 'does not paint box-shadow on native-appearance form controls' and cited the button counter-example, which was already scoped correctly but left a builder to infer the boundary from two data points. tokens-dev isolated the real discriminator as APPEARANCE - not the element and not the property - so a button paints box-shadow under the identical mechanism, and a checkbox stripped to 'appearance: none' would too. The rule now says that outright and names both escape routes: outline plus outline-offset, which works everywhere, or appearance:none plus full restyling, which is the only other way to earn box-shadow. Recording the discriminator matters more than recording the symptom, because a rule pinned to 'checkbox, radio, select' silently fails on the next native-appearance control someone adds, and a rule pinned to 'box-shadow' would forbid the button ring that demonstrably works. Chose not to wait for the in-flight outline conversion to land before writing the rule: the invariant is 'painted where the user runs it', which is true before and after that commit, so waiting would only have narrowed the rule to a snapshot - the exact failure the two prior AD-11 rewrites made.
- (constraint) Focus-indicator remediation landed as commit 22ed41e while revision 9 was being written, and the row moved from OPEN to RESOLVED rather than being deleted, because it records why AD-11's focus-paint rule exists. Verified against the committed tree rather than the report: 31 focus sites now carry focus-visible:outline, with grep returning 0 for ring-focus-ring, 0 for ring-offset, and exactly 1 ring-accent - PackageRow.tsx:85, the navigation highlight D35 deliberately preserved. The count is itself the evidence for the owner's correction that NINE controls rendered no indicator rather than three: 22 ring sites at 8d36cdf plus 9 additions equals the 31 outline sites now. The six extra were invisible to grep because the defect is the ABSENCE of a class, and no text search finds an absence - they surfaced only in a runtime audit walking the real tab order. This is the sample-versus-population failure AD-11's coverage-limit rule names, arriving a second time by an independent route, which is a stronger argument for that rule than the one that motivated it.
- (gap) Reviewer gate revision 9: FOUR lenses independently returned the same CRITICAL, and it was my error. AD-11 reads 'Binds: release' and the Capability Map routed it only to the release row, so the focus-paint and coverage-limit rules I folded into it bound nobody who could violate them - UX-PB.1e and UX-PB.5d, the two stories revision 9 declared unblocked, are bound to AD-4/16/17/19/21/22/23 and never to AD-11. Divergence lens confirmed by grep that AD-11 is cited by zero stories in epics.md. I had flagged this risk to the owner before the gate and then let amend-in-place stand anyway; the owner ratified amend-in-place for the COVERAGE-CLAIM half, which was correct, and I over-applied it to the mechanism half, which is a frontend invariant with no business under a release AD. All four lenses proposed the same remedy independently: a new AD with real Binds. Taken as AD-27, which also reuses no retired id.
- (gap) Second CRITICAL, found by three lenses, and the sharper of the two because it inverted the rule's purpose. My rule said the requirement is 'that focus be painted where the user runs it, not that any particular property is used', and offered 'appearance: none plus full restyling' as an escape route. Verified against the source this run: docs/DECISIONS.md D35 mandates 'One mechanism, outline + outline-offset, everywhere' and REJECTS appearance:none by name because it 'destroys the native checkmark, so it is not the fix'; docs/SPEC.md:208 says 'Use outline-*, not ring-*' and 'Never add outline-none to a focusable element'. D35's own stated reason for uniformity is the one I argued past: 'a scoped rule ... is a trap - the next person adding a checkbox copies the ring- from its neighbour.' So I wrote a permissive rule in the name of durability and thereby licensed the exact two-mechanism divergence D35 had just closed, without declaring an override. The lesson is that durability and convergence are not the same axis: pinning a rule to a SNAPSHOT of the tree is what went wrong in revisions 5 and 8, but pinning it to a DECISION is not the same mistake, and D35 is a decision. AD-27 now mandates the single mechanism and cites D35 and SPEC 4.1 as the floor.
- (decision) Third gate finding applied, raised by all four lenses: 'proven in WebKit' does not mean what I wrote. Verified - .github/workflows/test.yml runs every job on ubuntu-latest, so its Playwright 'webkit' project is Playwright's LINUX WebKit build, not WKWebView, and native-appearance form-control painting is precisely where WebKit ports diverge. AD-3 and AD-26 already refuse exactly this class of substitution for the browser double and the fixtures, and my new rule licensed it. AD-27 now states it outright: the Linux WebKit project is the closest available proxy and it did catch this defect, but it is not evidence about the packaged macOS app, and real-WKWebView verification stays the manual release-checklist pass until a native harness exists under AD-26. Also folded in the currency lens's MEDIUM-2, which strengthens the mechanism claim rather than weakening it: WebKit DISCARDS the box-shadow declaration at style resolution (RenderTheme supportsBoxShadow() returning false) rather than merely failing to paint it, so computed style reads the literal 'none' and the defect IS catchable by a computed-style assertion - which is what makes AD-27's named-sample rule enforceable rather than aspirational.
- (constraint) Remaining gate findings dispositioned. APPLIED: AD-26's paraphrase-in-quotes corrected - the spine read 'requires a paid API key for macOS' where llms-full.txt says 'a paid API key is required for macOS'; re-verified this run with curl piped to grep -c returning 1 for the source wording. This is the same failure class the currency lens caught in revision 8 and it recurred in a low-stakes clause, which is the argument for checking every quotation rather than only load-bearing ones. APPLIED: the epics.md residuals row grew from two items to four, and the two new ones are a different and more urgent class - this revision's own closures made epics.md:308 ('Canonical design-token set OPEN ... Blocks UX-PB.1e and UX-PB.5d') and epics.md:309 (the notarytool question OPEN) contradict rows the spine now marks RESOLVED. A closure that silently invalidates a downstream document is a divergence the closing run creates, not one it inherits, so recording it is part of closing. Both also cite the spine by LINE NUMBER and have already drifted - the third time positional references have rotted in this run folder, after AD-16's rule ordinals and the epics.md line citations. RECORDED not applied, as new architecture outside this run's authorized scope: transient selection has no owning invariant (SPEC F5 and EXPERIENCE.md answer oppositely and a live selection set ships that PlanIntent cannot represent), and AD-18 names the journal's home but neither its writer nor its per-attempt record cardinality.
- (decision) AD-27 created rather than AD-11 widened, and the id is the next free one - AD-6..AD-10 and AD-13..AD-15 stay retired and were not reused, so the live set is now AD-1..5, 11, 12, 16..27 (19 ADs, verified by heading inventory with zero duplicates). Chose a new AD over widening AD-11's title on the rubric lens's reasoning, which I checked and agree with: widening the title still leaves Binds and the Capability Map to fix, and AD-11's single Prevents clause - 'a release shipping without the checks whose failure is silent' - cannot honestly cover a paint rule. AD-11 keeps the release-side claim and now carries an explicit pointer to AD-27 for the mechanism. AD-27 is routed through the Capability Map on all six UI rows plus a dedicated row of its own, which is the fix for the actual defect: the map, not the AD text, is what a story author reads to find their governing invariants. Caught while verifying: my own batch row asserted 'eighteen live AD ids' and creating AD-27 falsified it mid-run - a bare count rotting inside the same revision that wrote it, which is the fourth positional or numeric reference to rot in this run folder. Replaced with a non-numeric formulation, and AD-27's zero citations in epics.md recorded as residual item (5) rather than papered over.
- (event) spine finalized at revision 9. Deterministic lint zero findings after every edit batch including the final one. Reviewer gate: four lenses, rubric verdict READY WITH FIXES, 9 CRITICAL / 20 HIGH / 28 MEDIUM / 15 LOW by each lens's own tally; currency checked 138 claims and verified 130 exactly. Every CRITICAL was in revision 9's own new material rather than in the inputs, matching revision 8's pattern. Six Decision Status rows closed - epics.md divergence batch (8d36cdf), epics.md retired-register residual, macos-14 retirement and the minimum-macOS notarytool residual (419dc32 / D34), canonical design-token set (be1f0e6 / D35), app-update safety guard enforcement point (7cc7b5f, open since revision 6), and focus-indicator remediation (22ed41e). One new invariant, AD-27, carrying the focus mechanism the gate showed AD-11 could not bind. Four Open rows recorded rather than resolved: the epics.md residuals now at five items, transient selection ownership, and plan-attempt journal writer/cardinality - the last two being new architecture outside this run's authorized scope. AD ids stable; AD-6..AD-10 and AD-13..AD-15 remain retired and unreused; live set is AD-1..5, 11, 12, 16..27. epics.md untouched throughout, per owner instruction. Nothing committed or staged - the tree is shared with two other agents.
- (event) Revision 10 run opened 2026-07-25, resumed from this memlog. Intent: update. New source of truth: _bmad-output/planning-artifacts/prds/prd-Pack-Manager-2026-07-25/prd.md (status final) plus addendum.md - Phase 2 exists again as a separable artifact carrying FR-1..FR-22, RP-1/RP-2, NFR-1..NFR-8, each tagged Shipping / Partial / Planned, where requirements previously lived inside epics.md. Scope set by the owner: (1) close the OPEN row 'Transient selection has no owning invariant' with a NEW invariant specifying a BATCHED range operation, then retire the row; (2) close the OPEN row 'Plan-attempt journal: writer identity and record cardinality' with one record at admission and one at terminal, not per transition; (3) apply D37, which removes keyboard navigation and screen-reader support as release criteria and supersedes D33's VoiceOver clause while preserving the focus indicator, the Cmd-X/C/V/A map and the contrast floor by name; (4) fold in D36 so AD-27 and the design-token rows reflect that the on-fill ink now has consumers and a CI guard (commit a201fb0). Rows are referenced by TITLE, never by line number - this run folder has now had positional references rot four times. epics.md is OUT OF SCOPE and must not be edited; the bmad-correct-course run that follows needs the membership invariant to exist here first. AD ids stay stable; AD-6..AD-10 and AD-13..AD-15 remain retired and are never reused.
- (constraint) Scope measured, not estimated, because the owner flagged the counts in docs/DECISIONS.md:556-558 as copied rather than measured. My own measurement of the spine agrees with the PRD's re-measurement exactly: 'grep -ciE (voiceover|keyboard) ARCHITECTURE-SPINE.md' returns 8 lines and the -o form returns 9 raw occurrences, against addendum.md:50's re-measured figure of 8 lines / 9 raw occurrences for this file. The eight lines are :332, :526, :614, :899, :941, :1015, :1018, :1050. Two of the eight are things D37 PRESERVES by name - the AD-27 heading at :899 and the focus-indicator Capability Map row at :1018 - so scoping by count alone would have deleted protected material. Scoped by heading instead, per the owner instruction.
- (constraint) CRITICAL correction to an input, verified against HEAD rather than accepted: addendum.md:57 says docs/RELEASE-CHECKLIST.md 'at HEAD it still carries both criteria D37 removed by name', citing :86 and :89. That was true at HEAD 5972109 when the PRD was written and is FALSE now - commit 5c8996f 'docs: record D36 and D37, and rescope the release checklist' landed the maintainer edit. Verified verbatim at docs/RELEASE-CHECKLIST.md:102-106: 'Keyboard navigation and screen-reader support are explicitly not release criteria. ... The former Tab and arrow navigation reach every control pass and the VoiceOver pass over the Upgrade Plan were removed deliberately on 2026-07-25 - they are not oversights, and a future regeneration or review should not reinstate them. Do not report their absence as a gap.' And :120-121: 'No by-eye contrast sweep is required at release.' This matters because two spine rules cite that pass as a live mechanism, so both are now false rather than merely stale.
- (constraint) D36's guard VERIFIED PRESENT at HEAD, so AD-11's 'Automated 4.5:1 text contrast does not exist' is false. tests/e2e/browser-style-contract.spec.ts:226 is a P0 case 'paints bright accent fills with ink that clears the 4.5:1 contrast floor'; :289-304 computes 'WCAG 2.1 relative luminance, then the (L1+0.05)/(L2+0.05) ratio' from the rendered element's own computed color and backgroundColor; :320 asserts toBeGreaterThanOrEqual(4.5). Consumers exist too: grep -rn text-white src/ returns 0 lines, and text-on-accent is used at src/components/primitives/Button.tsx:7 (primary), :13 (danger) and src/components/shell/UpdateStatusItem.tsx:63. Commit a201fb0, 3 files, 103 insertions. addendum.md:70's 'the contrast guard and its on-fill ink tokens are uncommitted' is stale the same way - written against HEAD 5972109, and a201fb0 landed after it.
- (constraint) AD-11's accessibility rule has now been wrong in three successive revisions and this is the fourth touch: revision 5 corrected a false present-tense claim that both automated checks existed (entry 83), revision 8 corrected the overcorrection that neither did (entry 111/123), revision 9 restated it with evidence inline (entry 153) - and revision 10 finds the contrast half false again, because the check landed. The pattern is not carelessness about the facts; it is that the rule states COVERAGE, and coverage is the kind of claim that goes stale on every commit. Fix accordingly: the rule states what a release may CLAIM from the lanes and names the lane as the inventory, rather than restating the inventory.
- (constraint) Membership-invariant groundwork verified in the tree, because the owner's brief names three consumers and I checked each. (1) src/hooks/useKeyboard.ts:35-53 visibleSelectableIds already applies the full predicate - search over name and meta.executables, the outdatedOnly default derived from anyOutdated, the caskGreedy exclusion, and isSelectable - so the owner is right that Cmd+A changes its SINK, not its predicate. Its sink today is :90 setSelection. (2) The predicate is DUPLICATED, which the owner's brief does not mention and which is the sharper hazard: useKeyboard.ts:34 says outright '/** Visible + selectable package ids for a manager (mirrors ManagerPane filters). */' and src/components/manager/ManagerPane.tsx:92-107 re-implements the same thing inline (anyOutdated, outdatedOnly, matchesSearch, then visibleMain.filter(isSelectable).map(p => p.id) at :107). Two copies of the set that decides what a batch stages is a drift hazard the invariant must name. (3) src/store/packages.ts ships selection at :17 plus anchor at :19, and the anchor is NOT membership - :87-92 rangeTo falls back to a single selection when the anchor is missing or no longer visible. Fourteen files reference selection today. (4) isSelectable is src/store/packages.ts:125-127 'pkg.outdated && !pkg.pinned && pkg.kind !== "caskGreedy"'.
- (constraint) The Cmd+A swallow defect confirmed in the tree exactly as addendum.md:67 describes. src/hooks/useKeyboard.ts:160-163 is 'case "a": e.preventDefault(); selectAllVisible(); break;' and selectAllVisible at :87-91 opens with 'if (ui.view.kind !== "manager") return;'. So preventDefault runs unconditionally while the action early-returns, and on Dashboard, History and Settings the app blocks the native select-all and does nothing in its place. The search field escapes only because :142 'if (isEditable(e.target)) return;' bails first. D37 does not excuse it: docs/DECISIONS.md:543 keeps 'Cmd+X / Cmd+C / Cmd+V / Cmd+A and the Cmd+R/A/L keyboard map' as a functional copy/paste concern, and prd.md RP-2 makes it a release prerequisite.
- (decision) AD-28 (new) - a Package checkbox IS Upgrade Plan membership, and a range or filter-wide interaction is ONE batched operation. Closes the OPEN row 'Transient selection has no owning invariant' (review-divergence-v9.md C-1). Binds Story 3.5, Story 3.2, UX-PB.1a, UX-PB.1c, UX-PB.1d and the RP-2 accelerator map. Prevents Story 3.5 building a transient selection set while UX-PB.1a builds direct membership - two stories that obey every existing AD and ship opposite models. EXPERIENCE.md:143 wins verbatim: 'On eligible Package rows, selection immediately adds/removes Upgrade Plan membership.' The owner confirmed it 2026-07-25 and it was already recorded at ux-Pack-Manager-2026-07-23/.memlog.md:75. docs/SPEC.md F5 is the stale side and stays stale by design - it is hand-written and workflow-unowned, and prd.md 0.1 records that F5 was never added to SPEC 0.1's supersession list, which is the mechanical reason the conflict survived to become a spine OPEN row.
- (decision) AD-28's batching rule is a REQUIREMENT with a recorded cost, not an optimization. prd.md FR-6: 'A range or filter-wide interaction submits one membership operation covering every affected Package identity, not one per row.' The cost is recorded at reviews/review-reconcile-epics.md:166 - 'If a builder maps selection onto draft membership to satisfy AD-17, then a shift-range across 100 rows becomes 100 Rust round-trips' - against NFR-3's 'The interface stays interactive beyond 100 Packages, with correct actions reachable at 101 rows.' Consequences fixed: the batch carries concrete canonical identities computed from the snapshot the user is looking at, never a predicate for Rust to re-expand, because a predicate re-expanded against a newer Rust snapshot could enlarge membership past what the user saw - the exact failure AD-16's frozen-expansion rule and its enlarging-rebuild prohibition exist to stop (memlog entry 75). One batch is one canonical rebuild and is all-or-none: a rejected rebuild leaves the prior coherent draft unchanged per AD-16, so a 100-row range can never half-apply.
- (decision) AD-28 fixes the four questions C-1 said would divide the two builders, and each answer traces to a named source rather than to my preference. (1) Esc never touches membership: prd.md FR-6 'The Escape cascade is close-dialog -> clear-selection -> close-drawer. This FR removes the middle rung only; Esc keeps close-dialog and close-drawer.' So the tombstone catastrophe C-1 named - 'one Esc can permanently poison a draft against Update Everything' - cannot occur, because the rung that would have written those tombstones is deleted rather than re-pointed. (2) Cmd+U is dropped, not re-pointed: prd.md FR-6 Out of Scope, 'Once selection is membership, its scope becomes ambient and it collapses into Cmd+Shift+U'; RP-2's enumerated accelerators do not include Cmd+U, and its handler upgradeSelected at useKeyboard.ts:93-104 retires with the selection set. (3) Range members carry Explicit provenance under AD-23, not a new Bulk scope: a range names concrete visible rows rather than a predicate, AD-23's scope enum has no token for a contiguous span, and adding one would be a fixture-backed AD-3 surface change bought for nothing - while filing a deliberate span as Bulk would let a later bulk of the same scope absorb it. The header checkbox and Cmd+A are the predicate-shaped entry points and stay Bulk { scope: FilteredView }. (4) The tri-state denominator is the eligible set matching the active filter, not every rendered row - unchecked when none of it is staged, mixed when some, checked when all - because 'only some are staged' in EXPERIENCE.md:143 is ambiguous about the denominator and the two stories would pick differently.
- (decision) AD-28 also fixes tombstone symmetry, which neither the owner's brief nor C-1 raised and which is the one place the naive reading breaks a user's mental model. Under AD-23 every removal writes a tombstone that no bulk expansion re-adds. Applied literally to a filter-wide UNCHECK, that turns one click into N session-permanent vetoes, so a user who unchecks the header and then invokes Update Everything gets nothing - and would reasonably call that broken. Rule: a bulk membership operation's remove verb is the inverse of its add verb - it clears membership for the refs in its scope and clears their tombstones - while an individual single-ref removal writes a tombstone. The justification is the purpose of a tombstone: it exists so a NARROWER deliberate act survives a WIDER automatic one, and when the user's own act is the wider one there is nothing to protect, because the wider act is the newer expression of intent. AD-23 already licenses this shape - 'Explicitly re-staging a tombstoned ref clears its tombstone: a user reversing themselves deliberately is not a silent re-add' - so this extends an existing clearing rule rather than inventing an exception. AD-23's addition-side guarantee is untouched: a bulk ADDITION still never re-adds a surviving tombstone.
- (decision) AD-28 carries the one-predicate rule and the native-default rule. One predicate: exactly one eligibility-and-visibility predicate serves the row, the header checkbox, Cmd+A and the batch payload, because two copies already ship and useKeyboard.ts:34 documents the duplication in a comment ('mirrors ManagerPane filters') rather than fixing it - and a batch computed from a drifted copy stages a set the user was not shown. Native default: an accelerator that shadows a standard Edit-menu action suppresses the native default only on surfaces where it performs its own action. Stated generally rather than as a Cmd+A patch so the next accelerator does not repeat it, and bound because the shipping handler violates it today and prd.md FR-6 says explicitly 'this FR must not inherit it'.
- (constraint) The Operation journal's shipping shape verified before writing the attempt journal's, because the attempt journal should ratify the discipline one level down rather than invent one. src-tauri/src/journal.rs:1-7 verbatim: 'Crash-safe journal operations.jsonl (SPEC 5.7, F8, DECISIONS D12) - implemented by U5. One line at op start, one at finish, flushed each write. Start-without-finish renders Interrupted on the next launch. Recorded pgids are NEVER signaled on startup (pid reuse). Compacted to the newest 1000 operations at startup. This one file is both the crash journal and the History source.' Two record structs, StartRecord at :26 and FinishRecord at :42, discriminated by an untagged Line enum at :50-55. And :73-74 'IO errors are logged and dropped - journal failures never fail an operation.' So exactly-two-records-per-subject with start-without-finish reading Interrupted is the SHIPPING pattern, and the owner's 'one record at admission and one at terminal' mirrors it precisely.
- (constraint) The audit-record asymmetry verified, because a builder could easily read NFR-4 as governing the attempt journal and block admission on a journal write - which would let a full disk stop all package work. The transcript DOES gate the spawn: src-tauri/src/queue.rs:1499-1503 'let transcript = match Transcript::create(&op.log_path) { Ok(t) => ..., Err(e) => { tracing::error!(op = %op_id, error = %e, "transcript create failed"); let _ = tx.send(Msg::Finished{...' - the operation finishes without spawning, matching prd.md NFR-4 'Failure to create an Operation transcript blocks the spawn - an unaudited command never starts. Later non-critical logging failures never hang Package work.' The journal does NOT gate anything (journal.rs:73-74 above, ratified by AD-18's nonfatal rule). Both positions are correct and they do not conflict; the asymmetry just has to be stated so nobody generalizes the wrong one.
- (decision) AD-29 (new) - the plan-attempt journal has one writer and exactly two records per attempt, and an attempt is a fold over them. Closes the OPEN row 'Plan-attempt journal: writer identity and record cardinality' (review-divergence-v9.md C-6). Binds UX-PB.2c, UX-PB.3d, UX-PB.4a, UX-PB.4b, UX-PB.4e, Story 6.5. Prevents UX-PB.3d and UX-PB.4a each owning a terminal durable write - one double-recording the attempt, the other leaving UX-PB.4a's own write-failure criterion with no code to attach to. Owner decision, taken verbatim: one record at admission and one at terminal, NOT per transition, because operations.jsonl already carries per-step detail tagged with the same planAttemptId, so per-transition attempt records would be exactly the duplication AD-18 exists to prevent.
- (decision) AD-29's rules. (1) One writer: the Rust plan-attempt store, at exactly two moments. History, Activity replay and the diagnostics export are READERS and never append - which is the sentence that resolves C-6's three-writer problem, since UX-PB.2c defines the record, UX-PB.3d transforms the sidecar into Results as presentation, and UX-PB.4a reads. (2) Ordering: mint and admit first, then append the admission record - nothing precedes the admission it rides on, by the same reasoning AD-22 fixes for the confirmation rider - and the append is nonfatal per AD-18, so a crash in that window leaves Operations pointing at an attempt that does not resolve, which AD-18 already dispositions as 'A record that loses its counterpart reads as legacy, never as corrupt.' (3) The asymmetry is explicit: the per-Operation transcript gates its spawn (NFR-4, queue.rs:1499) and the attempt-journal append gates nothing, so no story may make admission depend on a journal write. (4) Verification outcomes ride the terminal record - AD-16's PlanAttempt already carries verificationResults and resultSummary - so there is no verifying record and UX-PB.3d's Results are served by one read.
- (decision) AD-29's fold rule, which is the half C-6 said AD-18 lacked and the half the owner's decision does not itself supply. An attempt is a fold over its records, resolved in exactly one direction: admission plus terminal yields the terminal record's outcome; admission with no terminal record yields Interrupted once no live attempt owns it, which is journal.rs's shipping start-without-finish semantics and AD-5's reconstruction rule; a second terminal record for one attempt is duplicate evidence and never a state change, because AD-16 makes terminal states durable and an attempt cannot leave one - and that is what makes UX-PB.4a's 'no attempt ever yields more than one row' true by construction rather than by a story remembering to deduplicate. No completed outcome is ever synthesized (AD-19). And the limit is stated rather than papered over: an unparseable NDJSON line carries no readable planAttemptId, so a skipped line cannot be attributed to an attempt at all. C-6's sharp case - 'a skipped terminal line silently turns a completed attempt into an interrupted one' - is therefore answered by surfacing the read's skipped-line count at the journal level, so an Interrupted verdict drawn from a known-incomplete read is not presented identically to one drawn from a clean read. Inventing per-attempt attribution for an unparseable line would be fabrication.
- (decision) D37 applied by heading, and the sharpest finding is that one of the eight matching lines was ACTIVELY WRONG rather than merely stale. AD-16's ineligible-item inertness rule read 'its control is non-interactive to pointer and keyboard, it carries a stated reason for assistive technology' - and the pointer half contradicts both driving sources. prd.md FR-5: the pinned row's checkbox is 'rendered in a visually disabled treatment that must not be the native disabled state - a natively disabled control cannot receive the pointer interaction this consequence requires. Activation stays inert and never changes membership, while the control remains an explanatory target'. EXPERIENCE.md:143: 'An explanatory-disabled control never uses native disabled: expose aria-disabled=true, attach its persistent reason as an accessible description, keep activation inert, and preserve focus for its supplemental Tooltip/Popover.' Verified the shipping row is the defect prd.md names: src/components/manager/PackageRow.tsx:92 'disabled={checkboxDisabled}' with :100 'disabled:cursor-not-allowed disabled:opacity-40'. So a builder obeying the spine literally would have kept native disabled and shipped a row that can never explain itself. Rule rewritten: pointer-interactive but INERT, never native disabled, reason reachable by pointer. The assistive-technology limb drops as a criterion per D37, with the explicit note that D37 'is not a licence to strip shipped behavior' and rejects 'deleting the shipped focus and ARIA affordances from src/'.
- (decision) D37 applied to AD-17's announcement channel, and the transformation is obligation-to-convergence rather than deletion. The rule mandated one channel at polite priority with assertive escalation for the stall handoff and Interaction required, justified because 'a polite region is announced only when the user is otherwise idle and a VoiceOver user working elsewhere would miss the prompt'. prd.md FR-19 drops 'live-region announcements of plan progress/verification/cancellation/failure/completion' by name, so the obligation and its priority machinery go. Two things survive and one is NEW: the convergence half stays as a conditional - if a story adds an announcement channel there is exactly one, because two regions narrating one attempt is a defect rather than additive coverage - and the safety half has to be stated for the first time, because the deleted rule was the only thing that made a stall handoff or Interaction required reach a user who was looking elsewhere. It reached them through speech. With speech out of scope, the reachability of a safety-critical state must not depend on the announcement channel at all, and that now has to be said rather than assumed. This is D37 removing a criterion and thereby exposing an invariant that was hiding behind it.
- (decision) D37 leaves a real hole in AD-27 and the honest fix is to state the coverage floor, not to invent a replacement gate. AD-27's last rule read 'Verification in the real WKWebView remains the manual VoiceOver-and-keyboard pass on docs/RELEASE-CHECKLIST.md until a native harness exists under AD-26.' D37 deleted that pass - verified at docs/RELEASE-CHECKLIST.md:102-106 - so the rule now cites a mechanism that does not exist. I considered attaching the check to the surviving packaged-app manual steps and rejected it: those steps are mouse-driven, and :focus-visible is precisely the state a mouse click does not reliably produce on a checkbox, so the step would look like coverage and provide none. What replaces it: the per-story runtime verification AD-27 already mandates, performed in a macOS Tauri build, because that is the only engine where the appearance discriminator behaves as it ships. And the consequence worth recording is that the CI proxy has been PROMOTED by D37 - Playwright's Linux WebKit was a supplement to a manual pass, and with the pass gone it is the only automated guard standing between a ring- regression on a native control and a release. That raises rather than lowers the cost of weakening AD-27's named-sample rule, which D37 does not touch.
- (decision) D36 folded in, and it corrects AD-11 in the same direction the rule has now been wrong four times running - so the rule's SHAPE changed rather than only its facts. Verified: the automated contrast check exists (tests/e2e/browser-style-contract.spec.ts:226 P0 case, luminance computed at :289-304, asserted at :320) and the on-fill ink tokens have three consumers (Button.tsx:7 primary, :13 danger, UpdateStatusItem.tsx:63), with zero text-white left in src/. So AD-11's 'Automated 4.5:1 text contrast does not exist' and 'One manual VoiceOver pass and a by-eye contrast check sit on the release checklist' are both false, the latter on both halves - docs/RELEASE-CHECKLIST.md:120-121 now says 'No by-eye contrast sweep is required at release.' Rather than correct the inventory a fifth time, the rule now states what a release may CLAIM from the lanes and names the spec file as the inventory. The token rule itself is durable and goes to the Styling convention where a builder reads it: bright accent, accent-hover and danger fills take the palette's dark ink, never white, and the guard measures the rendered pair rather than trusting the token names.
- (constraint) prd.md is now the requirements authority and the spine's sources must say so. prd.md:16 verbatim: 'This PRD is the requirements authority. ARCHITECTURE-SPINE.md and epics.md are reconciled against it, not the reverse.' The spine cited no FR or NFR id anywhere before this run - 'grep -oE (FR|NFR)-[0-9]+' returned zero matches - so there is nothing to repoint, only the frontmatter to extend with prd.md and addendum.md. epics.md stays in sources as the story authority, which is what the spine actually cites it for.
- (constraint) Caught while writing, before the gate: AD-28's Binds listed Stories 3.2 and 3.5 while the Capability Map row it owns named 3.1, 3.2 and 3.5. That is precisely the defect four lenses independently returned as revision 9's CRITICAL - a rule filed where the stories that could violate it never read it (entry 158). Story 3.1 at epics.md:1190 is 'Present Complete Package State and Manager Detail', wanting 'complete Package rows, expandable details, self-update separation, and non-color status cues', so it renders the row whose checkbox IS the membership control. Binds corrected to 3.1, 3.2, 3.5 and the reason recorded inline so the next amendment does not re-split them.
- (question) OBSERVED, not applied, and offered to the owner rather than taken unilaterally: Cmd+L has the same shape of problem AD-28 fixes for Cmd+A, and no invariant owns it. prd.md RP-2 requires Cmd+L survive as 'toggle the activity surface', and the shipping handler at src/hooks/useKeyboard.ts:166 calls useUiStore.getState().toggleDrawer(). But AD-17 already retires that surface - 'The existing ActivityDrawer surface retires with the autoOpenDrawer setting; no story keeps it alive as a second home for attempt status' - and makes Activity an ActiveView destination. So Cmd+L's sink changes exactly the way Cmd+A's does, and the divergence pair is real: the UX-PB.3x story that retires the drawer and whoever owns the accelerator map could each leave Cmd+L a dead toggle or delete it outright, and RP-2 forbids both. It is a one-clause consequence of an invariant the spine already carries rather than new architecture, but it is outside the scope the owner set, so it is recorded here and offered rather than edited in. If the gate confirms it, the fix is a clause on AD-17's Activity rule naming Cmd+L's sink.
- (constraint) CRITICAL caught by reading epics.md before the gate rather than after, and it would have falsified a story the owner told me not to touch. My first AD-29 draft said 'One writer: the Rust plan-attempt store. History, Activity replay, and the diagnostics export are readers and never append ... UX-PB.4a reads.' epics.md:849 says the opposite and says it explicitly: 'This story renders and announces Results; it never writes a durable record itself. An attempt accumulates several append-only records - UX-PB.2c writes the admission record at mint - but exactly one terminal record exists per planAttemptId and UX-PB.4a writes it, folding the attempt's records into the single immutable History row. A second terminal write here would append a duplicate with no rule for which record is authoritative.' And epics.md:930 confirms UX-PB.4a persists: 'When its single immutable History row cannot be persisted Then the write failure is surfaced honestly'. So C-6's premise that 'UX-PB.3d and UX-PB.4a both own a terminal durable write' was already half-reconciled by commit 8d36cdf, and my rule would have contradicted the reconciled text while claiming to fix it.
- (decision) AD-29's writer rule reformulated as ONE APPEND AUTHORITY rather than one story, which is what I actually meant and what makes it compatible with epics.md. The store is the only thing that opens the journal for writing; UX-PB.2c appends at admission and UX-PB.4a appends at terminal, both through it. UX-PB.4a being a reader of the journal AND the author of exactly one append to it is not a contradiction - it is the distinction epics.md:849 already draws, and the spine now ratifies the assignment instead of inventing a rival one. What AD-29 genuinely adds on top: it tightens 'several append-only records' to exactly two, and it supplies the rule epics.md:849 itself names as missing - 'no rule for which record is authoritative.'
- (decision) AD-29 states one deliberate override, explicitly, per this run folder's standing practice that an override left implicit is a defect (entry 135). epics.md:693 has UX-PB.2c's ADMISSION record store 'the reviewed Manager/Package scope, Manager self-update identities, exact command snapshot, version evidence, timestamps, and result/verification state as immutable plan-admission metadata'. Result and verification state cannot be in an admission record: at mint the result does not exist, so the field is either a placeholder History must then tell apart from a real outcome, or it needs the in-place update an append-only journal forbids. AD-29 puts result and verification on the TERMINAL record and says it is overriding that clause, keeping the rest of UX-PB.2c's list on the admission record - which is also what that story's own crash criterion depends on, 'reconstructs the attempt only from durable plan-admission metadata that actually persisted'. Recorded as a divergence item for the correct-course run rather than edited into epics.md.
- (event) Found and read reviews/VALIDATION-REPORT-2026-07-25.md mid-run. It is a full Validate pass against revision 9, sitting in this run folder, and the owner's brief did not mention it - but its verdict is 'The spine needs another Update run' and five of its twelve findings are inside this revision's scope, including both rows I was sent to close. Ignoring a same-folder gate report while closing the exact rows it critiques would have been negligent, so it was folded in. Every citation I acted on was re-verified against the tree rather than accepted from the report. F1, F2, F3, F7 and F8 resolved this revision; F10, F11 and F12 recorded as rows rather than fixed, F10 and F12 because they are outside the scope the owner set.
- (constraint) F3 CONFIRMED by my own reading, and it invalidated something I had already written. All five items in the epics.md residuals row were CLEARED by commit 5972109 'Clear the epics.md residual pile (#36)', which landed after revision 9 was written. Verified individually against the committed file: epics.md:845 now states AD-25's rule in criterion prose ('a verification refresh that errors or times out never replaces or clears the snapshot it failed to refresh'); :1066 states AD-21's substance ('skipUpgradePlanConfirmation is plan-inert (AD-21) - it is not a plan-determining input, so writing it never advances the canonical revision'); :308 reads 'Canonical design-token set | CLOSED - D35 | Resolved 2026-07-25 | Nothing blocked.'; :309 records the notarytool question CLOSED by D34; grep -c AD-27 returns 32; and grep -c for spine line-number citations returns 0. I had extended that row with new items on the assumption its five were still open, which would have sent the correct-course run to re-fix five discharged items. Row rewritten: the five are recorded as cleared with the evidence, and only revision 10's own residuals remain.
- (constraint) F2 CONFIRMED and it made my AD-29 fold rule BETTER rather than merely restating it. epics.md:936-937 already carries a fold rule for UX-PB.4a, verbatim: 'Given an attempt whose admission record is present but whose terminal record is absent, unparseable, or skipped by the read (AD-19) When History folds that planAttemptId's records into its row Then the row is presented as Interrupted only when the absence is genuine - a terminal record that exists but failed to parse is reported as unreadable evidence rather than silently reclassifying a finished attempt as unfinished, and the fold states which it was.' My draft had claimed an unparseable line can never be attributed, which is only true of a line that fails JSON parsing outright; a line that parses as JSON but fails the record schema still yields its planAttemptId, so attribution IS possible and epics.md is right. AD-29 now carries both cases separately and ratifies the story's rule instead of contradicting it.
- (constraint) F1's escalation VERIFIED in the tree and folded into AD-29, and it changes three things. (1) The candidate terminal writers are FOUR, not two: UX-PB.2e drives an attempt terminal through cancellation and UX-PB.3g is equally exposed, and neither is named in epics.md's writer assignment - so AD-29's Binds names 2c, 2d, 2e, 3d, 3g, 4a, 4b, 4e, and AD-18 carries only a cross-reference. Three of those cite AD-18 nowhere today (UX-PB.2c at epics.md:684, UX-PB.2d at :711, UX-PB.2e at :731), which is why a bare AD-18 amendment would have been unenforceable. (2) The shipping fold is NOT idempotent - src-tauri/src/journal.rs:131-134 'Ok(Line::Start(s)) => { index.insert(s.op_id.clone(), entries.len()); entries.push((s, None)); }' pushes a new entry for every start-shaped line while only the finish half dedups, and :154 'None => (OpStatus::Interrupted, None, None)' makes an unmatched duplicate read Interrupted. So AD-29 requires the attempt fold to be idempotent and keyed, explicitly refusing to inherit that behavior. (3) The worst consequence is not presentational: a crash-interrupted mid-flight record read as current makes the active-attempt lookup resolve a dead attempt as live, and AD-16's one-active-attempt rule then fails EVERY subsequent confirmation closed - wedging admission permanently. AD-29 now states that a journal record is history and never liveness.
- (constraint) F1's knock-on verified too: the 'Plan-attempt file name and serde shape' Deferred row is downstream of cardinality rather than orthogonal to it. src-tauri/src/journal.rs:50-55 discriminates start from finish with '#[serde(untagged)]', which resolves by trial deserialization and cannot separate two record shapes that overlap in their fields. The row now says UX-PB.2c may copy journal.rs's file discipline but not its discriminator.
- (constraint) F8 CONFIRMED and fixed because I was already amending AD-27. Its Binds read 'every story that renders an interactive control - all of Epic UX-PB, Stories 3.1, 3.2, 3.5', and a prose preamble followed by a closed enumeration resolves as the enumeration - so Story 3.4 (Settings controls) and Story 6.5 (the diagnostics action) fell outside a Rule that says 'on every interactive element'. epics.md mirrored the defective list faithfully, which is what makes it a spine fix. Binds now names 3.1, 3.2, 3.4, 3.5 and 6.5, and the Capability Map's Story 3.4 and Story 6.5 rows gained AD-27 so the two agree - the same Binds-versus-map split that was revision 9's CRITICAL.
- (decision) F12 taken as a Deferred row with a revisit condition rather than as new architecture, which is what the finding itself proposes. The post-publish envelope was wholly silent through revision 9 - I confirmed 'grep -ciE rollback|retract|rotat|unpublish' returned 0 before my edits - and the good-spine checklist counts a silent dimension as a finding on its own. Four undecided questions named: a bad release after latest.json is live, artifact immutability, rotating the updater pubkey every installed client embeds, and the artifact host as a durable dependency. Deferred deliberately and with the reason stated: at one user and one machine the recovery path is 'publish another release', and a retraction protocol would be architecture bought for a scale D33 says this product does not have. The trigger is the first bad published release or any updater-key change, not a date.
- (question) F10 and F11 RECORDED as rows, not fixed, because they are outside the scope the owner set. F10 is real and I verified it: AD-12 says seven files are 'never hand-edited', but release-please-config.json scopes ownership to FIELDS - extra-files pins $.version in tauri.conf.json, $.package.version in Cargo.toml, and the pack-manager entry in Cargo.lock - while AD-11's bundle.macOS.minimumSystemVersion, AD-26's [profile.release] reasoning, and the updater pubkey at tauri.conf.json:30 all live inside those same files. Read literally, AD-12 leaves key rotation with no legal mechanism and makes AD-11 unimplementable. The fix is a one-clause narrowing to the version fields the rule actually means, but it is release-side and not in this run's scope, so it goes to the owner. F11 is answered by widening the gate-tail row to cover revisions 8, 9 and this validation pass.
- (constraint) Found while auditing the spine's SPEC citations: docs/SPEC.md 4.11 is a SECOND stale side alongside F5, and no artifact records it. Verified verbatim at docs/SPEC.md:288 - 'Cmd+R refresh current manager (Dashboard: all) . Cmd+Shift+R refresh all . Cmd+U upgrade selected (opens sheet) . Cmd+Shift+U Update Everything (sheet) . Cmd+A select all visible selectable rows . Space toggle focused row . Esc clear selection / close sheet / close drawer . Cmd+L toggle drawer . Cmd+F focus search . Cmd+1..9 sidebar jump. Roving tabindex in tables; live region announces op completions; all color states carry text/icon equivalents; text contrast >=4.5:1 on its surface.' AD-28 removes Cmd+U and the Esc clear-selection rung; D37 removes the roving tabindex and the completion-announcing live region; the contrast floor survives. prd.md 0.1 catalogues SPEC's defects against 5.9, F11, 1 P2, the 0.1 supersession list and F1-F17, but never reaches 4.11 - so the spine's own AD-28 rule is where it gets recorded. Not edited: SPEC is hand-written and workflow-unowned, so this is a maintainer decision like the F5 omission.
- (gap) Reviewer gate, PRD reconciler: CRITICAL and it is a REVERSAL of a decision revision 8 made deliberately. The spine mandated 'Cancelling and Interaction required are durable wire-level states on OpStatus as well', added by revision 8's rubric H1 (entry 136) reasoning by symmetry with Verifying and Skipped. prd.md:344 FR-13 forbids the state by name: 'There is no distinct cancelling state: cancellation moves an Operation to its terminal state, and the 5-second SIGTERM grace window is not surfaced as its own status.' Verified independently: src-tauri/src/ipc.rs:99-107 ships exactly seven OpStatus variants with no Cancelling, and grep -rni cancelling over src/ and src-tauri/src/ returns three hits, all comments or a tracing string. So the spine was instructing a builder to ADD a variant the requirements authority forbids, as one atomic AD-3 change across the enum, types.ts, the guards and the committed fixtures - a hard break, not a cosmetic one. Worse, AD-16 already contained the opposite model two bullets earlier, whose cancellation rule has unstarted work become Skipped and running work escalate with no intermediate state.
- (decision) Cancelling REMOVED from AD-16, and the PRD is followed rather than overridden. Under prd.md:16 the PRD wins over an older artifact, and this is a question about whether a user-visible state exists at all - which is requirements, not architecture, so the spine has no standing to override it here even though it does override story-level clauses elsewhere (AD-22 over UX-PB.5b, AD-29 over UX-PB.2c). The PRD's answer is also the better one: removing the state means the durability question never arises, and it makes AD-16 agree with its own cancellation rule instead of carrying both models twenty lines apart. Interaction required KEEPS its durable-state rule - the PRD requires that state (FR-14). Recorded as a divergence rather than edited: epics.md:740 and :903 both say 'changes still-running Operations bound to that planAttemptId to Cancelling', and EXPERIENCE.md:216 says Cancel plan 'changes running work to Cancelling'.
- (decision) Three more PRD-reconciler HIGHs applied to AD-16, all cases where the spine's rule was true but read as EXHAUSTIVE while the PRD adds required limbs. (1) The no-entry-point-executes rule named one shipping call site; prd.md FR-6 names three - the Package row action AND both direct Manager self-update paths, the Dashboard Manager card and the Manager-workspace self-update card - and says scoping the work to the row action alone leaves two unstaged mutation paths alive, which breaches SM-2. (2) The confirmation-opt-out rule said only what the opt-out may SKIP; prd.md FR-7 makes three compensations mandatory when it is active - auto-expanded exact commands before the action enables, a persistent 'Confirmation is off' notice linking to Settings, and the primary action relabelled from Confirm to 'Run N updates' - because without them the button still reads Confirm and executes immediately with commands collapsed, which EXPERIENCE.md names as an anti-pattern. (3) The draft-mutation convergence rule preserved the prior preview on a failed rebuild, which keeps the draft coherent but left the preview CONFIRMABLE; prd.md FR-8 makes confirmation unavailable while a rebuild is in flight and after one fails, and the reason is sharp - during a pending rebuild the displayed commands belong to the previous options, so a confirmation in that window executes something other than what is on screen while passing the revision check, the one-use planId and the lock-set test.
- (decision) PRD-reconciler H-4 applied as an explicit supersession rather than a silent divergence. prd.md says in three places - 7.1, NFR-6, and addendum 4 - that the contrast guard and on-fill ink tokens are uncommitted and absent from HEAD, and that neither FR-19 nor NFR-6 may be read as CI-guaranteed on that axis. Commit a201fb0 landed them after the PRD was written, and D36 records it, so under the PRD's own precedence rule (a decision later than 2026-07-25 supersedes it) the spine follows D36. AD-11 now states the supersession and names the three stale locations, because the reviewer's objection was not that the spine was wrong but that it never recorded overriding its own authority - the same defect the Cancelling finding punished.
- (decision) PRD reconciler's two MEDIUMs and three LOWs dispositioned. RECORDED as rows rather than invented into invariants: RP-2's Edit/Window submenu re-declaration obligation, which has no AD - app.set_menu replaces Tauri's default menu wholesale so the submenus must be rebuilt or Cmd-X/C/V/A die silently (D25a) - noted on the accelerator Capability Map row, because it is a requirements prerequisite validated by the checklist rather than an architectural divergence; and the quit-with-work-in-flight enforcement point, whose sibling app-update refusal got a two-layer invariant while the quit case has none, given its own Open row with the note that prd.md 9 Q1 leaves the queued-quit and OS-shutdown cases open, so writing an invariant over an undecided requirement would be premature. APPLIED, all three LOWs: the mise tool:rust entry is named as the excluded side of rustDedup (verified at src-tauri/src/queue.rs:559-563, which pushes ExcludedPackage with manager_id Mise, package_id tool:rust, reason RustDedup), because the spine said 'removing the other side of the conflict restores the item' with 'the item' undefined; reduced motion added to the Styling convention, since grep returned only two hits and both were revision-history prose - it was the one FR-19 limb with neither a rule nor a convention entry, and D37 at DECISIONS.md:533 says 'D33's reduced-motion and contrast positions are unaffected', so its absence was not a decision; and AD-28 now states that row ordering is presentation and may change freely but must never change how a batch is derived, because a range is an anchor and a target over the rendered order.
- (event) Currency reconciler returned: 158 checkable claims examined, 145 verified exactly, 8 FALSE, 5 imprecise, 0 unverified. 3 CRITICAL / 3 HIGH / 4 MEDIUM / 3 LOW. Its headline is the useful part: the TECHNICAL half of revision 10 is clean - every Stack row resolves against the lockfiles, every inline code citation in AD-28 and AD-29 matches the tree verbatim, and every AD-26 external quotation matches the raw tauri.app source word for word. All eight FALSE claims were about epics.md, and most were in the residuals row I had already rewritten after reading the validation report, so its three CRITICALs and two of its HIGHs were pre-fixed. That overlap is itself the lesson: the lens read the file at 12:58 and the fixes landed after, so the same defect was found twice by two routes.
- (constraint) HIGH surviving from the currency lens, and it is the FIFTH instance of the run folder's positional-reference failure: six of the seven commit hashes the spine cites do not resolve on origin/main. Verified with git merge-base --is-ancestor against both refs - 22ed41e, be1f0e6, 419dc32, 7cc7b5f and 8d36cdf exist as objects but are ancestors of NEITHER origin/main nor HEAD, because they were squash-merged as PRs (#35, #36); a201fb0 and 5c8996f are ancestors of HEAD but not of origin/main, because this branch is unmerged. So a reader on main cannot resolve any of them and a gc would drop five. Fixed with a new Citations convention row rather than by deleting the hashes, because they are still useful locally: cite by decision id, story id plus quoted criterion, row title, or AD id plus subject - never a line number, a rule ordinal, a bare count, or a pre-squash SHA. The row names all five prior instances so the lesson stops being re-learned.
- (decision) Four more currency findings applied, three of which were bare-count or false-negative claims of my own or revision 9's. (1) The epics.md divergence-batch row still said AD-27 'cannot yet cite it' - false, grep -c returns 32, so it now records that exception as cleared too. (2) The design-token row said 'All 22 focus-visible sites'; the tree has 31 after the outline conversion, so the count is gone and the claim is stated without one - the same fix the row's own history argues for. (3) My residuals row claimed Story 3.5 carries a Cmd+U limb; grep -c over epics.md returns 0, so only docs/SPEC.md 4.11 carries it and the clause was corrected rather than dropped. (4) My NFR-3 quotation had silently dropped its leading 'The' - trivial in effect, but the verbatim-quotation rule is the one this run folder has broken most often, so it is fixed and cited to both FR-6 and NFR-3.
- (decision) The currency lens's M3 replaced my residual with a SHARPER and more dangerous version, and it is the best single finding of the gate. I had written that UX-PB.1d 'needs' AD-16's corrected inertness rule. It already has it - epics.md:613 reads 'it uses aria-disabled="true" rather than native disabled, keeps focus, announces its persistent reason as an accessible description, stays inert on activation'. The trap is that the whole criterion is gated on a Given/When about a keyboard or VoiceOver user. D37 removes that trigger, so a correct-course run stripping the keyboard and VoiceOver limbs would delete the only stated trigger for the one rule AD-16 now REQUIRES - turning a D37 cleanup into a silent violation of a fresh invariant. Residual restated: re-gate the criterion on pointer interaction, do not delete it, and give the story title the same treatment. D37 already says UX-PB.1d is not to be deleted; this is why.
- (event) DECISIONS reconciler returned: 0 CRITICAL / 2 HIGH / 6 MEDIUM / 3 LOW. Verdict: D37 is applied to the spine correctly and completely - all four revision-9 sites carrying a removed obligation are gone, all three protected items survive by name, and the scoping instruction sits where a later pass will hit it. D36 applied correctly and every automation claim it rests on is true at HEAD. It also noted the target changed seven times under it and re-anchored every citation by AD id and row title rather than line number, which is the convention this revision just wrote down.
- (gap) HIGH from the DECISIONS lens, and it is the one place revision 10 overrides a LIVE decision: docs/DECISIONS.md D15 specifies pinned formulae as 'Disabled checkbox + tooltip with the brew unpin command; excluded from every plan', and AD-16's corrected inertness rule forbids native disabled. Verified: grep -c D15 over DECISIONS.md returns 1 - its own heading - so nothing supersedes it, and grep -c D15 over the spine returned 0, so the spine never named it. The shipping row is not drift, it is D15 implemented literally (PackageRow.tsx carries the brew unpin title alongside disabled and disabled:opacity-40). Handled the only honest way: AD-16 now names D15, states that D15's SUBSTANCE stands untouched - a pinned formula is never upgradable in-app and is excluded from every plan - and that only its MECHANISM clause is overridden by the later requirements authority. A spine cannot supersede a decision, so recording it is a maintainer edit to DECISIONS.md, and until that happens a reviewer checking UX-PB.1d against D15 will read the removal of disabled as a regression.
- (constraint) HIGH from the DECISIONS lens and it was MY over-claim, written this revision: AD-11 said the release checklist 'does still carry Cmd-X/C/V/A and the application accelerator map'. Verified false - grep -niE for the accelerator glyphs and for 'accelerat' over docs/RELEASE-CHECKLIST.md returns NOTHING. Step 9 covers only Cmd-X/C/V/A in the search field and every CopyableCommand. The accelerators Cmd-R, Cmd-Shift-R, Cmd-Shift-U, Cmd-L, Cmd-F and Cmd-1..9 appear nowhere on it, while prd.md RP-2 enumerates them and prd.md 4.6 says RP-1 and RP-2 'are validated through docs/RELEASE-CHECKLIST.md'. So RP-2's stated validation route is incomplete for its own accelerator half. AD-11 narrowed to what the checklist actually contains, with the gap named and a release forbidden from claiming those accelerators were verified.
- (decision) Remaining DECISIONS findings applied or routed. APPLIED: the EXPERIENCE.md Checkbox citation now carries the staleness qualifier the spine uses elsewhere - its native-disabled clause only, the announcement limbs in the same sentence being D37-removed - because a reader following the inline pointer would otherwise land in text carrying removed obligations; and AD-27's sample is renamed from 'the package-row plan-membership checkbox' to 'the package-row checkbox - membership under AD-28, still the selection toggle until UX-PB.1a lands', since the old wording put 'today' in front of a name that is only true tomorrow. ROUTED into a new 'Maintainer edits this spine cannot make' row, because the spine had rows for workflow-owned divergences and nowhere for workflow-unowned ones: D15's missing supersession, D37's unmeasured counts, D36's title over-claiming plural consumers while --color-on-success still has none, RELEASE-CHECKLIST step 5 describing D27 target state as verifiable today, RP-2's missing accelerator validation, and SPEC's F5 and 4.11 omissions.
- (event) Reviewer gate, currency lens: 0 CRITICAL, 0 HIGH. 94 claims examined, 86 verified exactly, 1 FALSE, 6 imprecise, 1 unverifiable. Its verdict: 'Revision 10 is the most reality-checked revision of this spine so far' - every Stack row resolves against the lockfiles, all seven cited commits exist and say what the spine says, the two engine claims revision 9 merely asserted now verify against the installed compiler and Tailwind's own upgrade guide, and the AD-26 quotations revision 8 FABRICATED are this time genuine text from tauri.app/llms-full.txt. I re-fetched the raw source myself and confirmed the two sentences at issue rather than taking that on report.
- (decision) Currency lens MEDIUM-2 taken, and it makes AD-26's premise honest rather than merely narrower. The rule said 'the macOS route puts an automation surface inside the application' - a route-ENUMERATION claim that enumerated incompletely. The source names two macOS-capable providers under @wdio/tauri-service, and I verified the disqualifying sentence in the raw file myself: 'You can skip it if you want to use the external or crabnebula provider instead' (grep -c returns 1 against the 2430050-byte llms-full.txt). So the embedded provider is the DEFAULT and does embed the plugin; the crabnebula provider works 'on all platforms (a paid API key is required for macOS)' and does not. AD-26 now names both, scopes the trust-boundary consequence to any route that embeds a surface, and records that the paid alternative trades money for not widening the boundary. Same failure class as revision 8's fabricated quote - derived rather than read - caught this time at MEDIUM because the direction was right.
- (decision) Currency lens LOW-4 taken and it is a real correctness point, not typography. The Verified Brownfield Baseline said release.yml 'blocks publication on two checks'. Verified in the workflow: the minisign verification at release.yml:319 runs BEFORE 'gh release upload' at :376 and does gate, but 'Verify published updater endpoint' at :383 runs AFTER it, and must - the workflow's own comment says 'latest.json is written before the asset it references is uploaded, so nothing until now has confirmed the published endpoint is reachable'. So the second check cannot block publication; it fails the run beside an already-published release. Both the baseline and AD-11 now say only the first gates and the second is a DETECTOR, with a prohibition on either being reordered into the other's role. This matters because NFR-8 and AD-11's 'silent and simultaneous across every installed client' failure mode both lean on these two checks, and a reader who believes both gate will not ask what happens to a release that is already out.
- (decision) Three more currency fixes applied. The macos-14 currency caveat was FALSE - it named docs/index.md, which is clean, and missed docs/deployment-guide.md, docs/architecture.md and docs/contribution-guide.md, which are not. Rather than correct the list, the caveat now gives the COMMAND (grep -rln macos-14 docs/ _bmad-output/*.md) and says why it is not enumerated, which is the Citations convention applied to itself. AD-27's named sample was 'a toolbar button' and the actual sample is the Refresh All button, which is not in a toolbar - renamed by accessible name, since the sample is chosen for the appearance discriminator rather than for its position in the layout. And AD-29's journal.rs quotation had silently lowercased 'One line at op start' - fixed, because the verbatim-quotation rule is the one this folder has broken most often, and a lens that checks apostrophes is exactly the lens that should catch it.
- (event) Reviewer gate, rubric walker: READY WITH FIXES. 1 CRITICAL / 4 HIGH / 5 MEDIUM / 2 LOW. Its verdict separates the two halves cleanly and the split is the useful part: 'The invariants themselves are sound... AD-28 and AD-29 are correctly shaped: each names a pair of units that obey every prior AD and still build incompatibly, states rules that reach that divergence, and ratifies a shipping referent rather than inventing one.' It checked every Prevents clause in the new and changed material against its own Rule list and found ZERO instances of the revision-5 defect where something is prevented that no rule forbids. What it found defective was the HANDOFF RECORD - and since epics.md is deliberately unedited, the residuals row IS the entire mechanism by which revision 10's decisions reach the stories they govern.
- (gap) Rubric CRITICAL, and it was my error in the worst possible place. I attributed the Cancelling divergence to UX-PB.2f and UX-PB.4c, in BOTH places that record it - AD-16's new rule and the residuals row. Verified: grep -n Cancelling over epics.md returns exactly two lines, 740 and 903, and awk against the preceding story headings puts them in UX-PB.2e ('Plan-level cancellation that skips unstarted work and escalates running process groups') and UX-PB.3g ('Two labeled cancellation scopes'). UX-PB.2f and UX-PB.4c contain the word nowhere. I had quoted UX-PB.3g's text verbatim and filed it under the wrong story id. The spine also contradicted itself: AD-29's own Prevents clause names UX-PB.2e and UX-PB.3g correctly. Because the residuals row instructs the next run to scope 'by named heading', a correct-course run following it literally would have opened two clean stories, found nothing, closed - and left both defective ones carrying a criterion that requires an OpStatus variant the requirements authority forbids by name. Fixed in both places, with the wrong ids recorded so the miss is visible rather than silently corrected.
- (decision) Four rubric HIGHs applied, all completeness failures in the residuals row rather than in the invariants. (H1) Stories 3.4 and 6.5 gained AD-27 in this revision's Binds and epics.md still omits it, and the row's own 'grep -c AD-27 returns 32' is clearance evidence for the OLD list and the wrong measure for the new one - it was already true before the widening, so it read as done while the two newly bound stories are exactly the two that do not read the rule. That is VALIDATION-REPORT F8 reproduced one level down. (H2) Two epics.md passages now assert the opposite of D36 - both say automated 4.5:1 contrast 'does not exist' and name it as an outstanding obligation - and a builder reading either schedules shipped work, the error AD-1's second rule forbids by name. (H3) Story UX-PB.5d is built almost entirely on D37-removed scope and was named nowhere; restate it rather than delete it, as UX-PB.1d is restated. Two more of the same class also added: UX-PB.3a's announcement obligation, now optional under AD-17, and epics.md's surviving claim that a manual VoiceOver pass joins the release checklist. (H4) handled separately.
- (decision) Rubric H4 answered with a GATE rather than a decision, which keeps the owner's reservation intact. Two lenses have now called the Cmd+L / Esc-second-rung row ungated with an unblocked story, and the validation report ruled the analogous shape CRITICAL a revision ago. I did not decide where the keys point - that is the owner's, and it is outside this run's scope. What I added is the half that IS this spine's: no story may retire the ActivityDrawer surface until both sinks are named. Retiring it is precisely what strands the keys, the shipping handler still routes both to it (useKeyboard.ts toggles the drawer for Cmd+L and closes it on Esc's last rung), and nothing on the release checklist would catch a dead accelerator. So the divergence cannot be foreclosed while the decision is pending, without the spine pre-empting the decision.
- (decision) Rubric MEDIUMs and LOWs applied. M1: the revision-9 gate tail counts were copied from the memlog rather than measured, and were wrong by one CRITICAL - re-summed from each v9 lens's own tally line (divergence 6/8/9/4, rubric 2/5/8/5, reconcile 2/5/7/4, currency 0/2/5/2) to 10/20/29/15, 74 findings. The row exists BECAUSE of the validation report's F11 finding that unpromoted findings are tracked nowhere, so an undercount there is an undercount of unscheduled work. M3: AD-28's Binds widened from three UX-PB stories to UX-PB.1a-1e to match its own Capability Map row, with UX-PB.1e's inclusion justified inline - it renders the Manager Remove affordance, and whether that removal writes a tombstone is decided by AD-23 and AD-28's inverse-removal rule, so it must read both. This is the Binds-versus-map split that was revision 9's CRITICAL, caught in my own new AD. M4: Story 3.2's pinned-row criterion still says 'disabled' and AD-16 binds it, so it needs UX-PB.1d's correction too. L1: AD-12's rule now points at the table row that calls it unimplementable, because Binds is 'release, all commits' and every commit author reads the rule rather than the table. L2: UX-PB.2c's persist-failure wording recorded as a clarification.
- (event) Reviewer gate, adversarial divergence lens: 2 CRITICAL / 5 HIGH / 3 MEDIUM / 4 LOW, and BOTH CRITICALs plus three of the five HIGHs are in AD-28 and AD-29 - this revision's own new material, matching the pattern every revision since 7. The lens also recorded that the spine was rewritten three times under it, pinned a snapshot blob, and cited every finding by AD id and subject with the line number as a locator only, explicitly because the Citations convention this revision wrote demands it. Two findings it had drafted were closed by my concurrent edits and it recorded them as closed rather than reporting them open.
- (gap) Divergence C-1: AD-28's removal taxonomy was NOT TOTAL, and the hole was mine. I wrote two classes - 'only an individual single-ref removal writes one' tombstone, and 'a filter-wide or Manager-wide remove clears them'. Four removal shapes the bound stories actually build fall outside both: a shift-range uncheck (one batch, not an individual removal; filed Explicit with no scope token, so 'the refs in its scope' is undefined for it), Clear (draft-wide, neither filter- nor Manager-wide), an Everything-scope removal (AD-23's enum has three tokens and my rule enumerated two - and undoing an Update Everything seed is exactly UX-PB.1c's Given), and Cmd+A on an all-staged view where the header is checked so the next press is a removal. Concretely: Story 3.5's builder writes ZERO tombstones for a 50-row shift-range uncheck and the next Update Everything silently reverses the removal; UX-PB.1c's builder writes 50 and the next Update Everything stages nothing - which is verbatim the failure my own rule said it existed to prevent. Both lawful.
- (decision) AD-28's removal rule replaced with a CLOSED THREE-WAY TAXONOMY, and the governing principle is the one my two-way version was missing: batching is a transport requirement and never changes a removal's taxonomy class. (1) single-ref removal writes a tombstone; (2) scope-wide removal carries one of AD-23's three tokens and clears membership plus the tombstones ONLY of refs whose membership it actually cleared; (3) a range removal is a batch of single-ref removals for tombstone purposes, one tombstone per member, on the same ground a range is filed Explicit - it names concrete rows rather than a predicate. A shape not on the list is a new decision. The lens also caught a sub-case I had left ambiguous: 'clears their tombstones' read widely lets a user who deliberately removed foo, then unchecked a header covering foo, get foo re-staged by the next Update Everything without ever re-staging it - and my own justification pointed the wrong way there, because both acts were removals so the newer intent was to have LESS. Scoped to what the act actually removed.
- (gap) Divergence C-2: my all-or-none rule and AD-16's narrowing-rebuild rule give opposite answers for one input, because no rule said which side of the IPC boundary owns the one predicate. 500-row filtered view, refresh lands between render and click, Rust finds 3 now pinned. Story 3.5's builder reads all-or-none and rejects the whole batch; UX-PB.1a's builder reads AD-16's 'may remove or invalidate membership' plus its own criterion and stages 497. Three things break: the tri-state control WEDGES permanently at mixed - the denominator is the full eligible set, the projection holds 497, and my own rule forbids storing the state, so there is nowhere to record that 497 is all that can be staged and the header can never reach checked; the count reported to the user differs for the identical act; and the batch verb ships two different success types for one fixture-backed model, which is what AD-3 exists to prevent. My 'ineligible rows are simply not in the batch' silently assumed the frontend already knew - the exact case my other rule says cannot be assumed.
- (decision) AD-28 rewritten on C-2: a batch is ALL-OR-NONE IN APPLICATION AND NARROWING IN RESOLUTION, and the two are not in tension - the rebuild applies every member it still finds eligible and drops the rest as one transaction, reporting the dropped refs and reasons; outright rejection is reserved for a rebuild ERROR. That is the batch form of AD-16's never-add-but-may-remove rule, so batching does not make a lawful direction unlawful. And the one predicate now has an owner: it is RUST's, projected to the frontend with the snapshot it was computed against, the frontend submits concrete identities plus the snapshot token it read, and Rust rejects a batch whose token is stale. Without that the numerator is canonical in Rust while the denominator is computed in TypeScript. The row, header, Cmd+A, batch payload and tri-state denominator all read one projected result, so a narrowed batch narrows the denominator with it and the header can reach checked.
- (decision) Divergence H-1 REVERSED my own earlier reversal, and the lens is right. Earlier this run I drafted AD-29 with UX-PB.4a as a reader, then found epics.md:849 assigning it the terminal write and reformulated to 'ratifies rather than overrides'. H-1 shows that assignment is itself the defect: UX-PB.4a depends on all of UX-PB.3a-3g while UX-PB.2e ships in wave 2 and each UX-PB story is a shippable slice, so from UX-PB.2e landing until UX-PB.4a lands, every attempt that terminates persists ADMISSION-ONLY - and AD-29's own fold rule is then required to read each as Interrupted. A user who watched an attempt verify and dismissed Results finds it Interrupted in History once History arrives, and both repairs are already forbidden. AD-29 now overrides the terminal half: the append fires on the terminal transition inside the store, in the same critical section that makes it, owned by whichever story first makes an attempt reachable terminal (UX-PB.2e); UX-PB.4a is reader and folder only. Independently, H-1's steady-state half is also right and is now a rule: no window may exist between a terminal transition and its append in which a force-quit loses the outcome.
- (decision) Divergence H-2 and H-3 applied to AD-29 and AD-16. H-2: Story 6.5 asserts each exported plan-attempt record carries scope, exact commands, verification facts AND results, which AD-29's admission/terminal split makes impossible for any single record. Resolved by authorising the shape rather than the story: the archive carries the RAW journal lines per AD-18, an attempt's fields are carried BETWEEN its two records, and a folded view may be an additional entry marked as derived but never replaces the raw lines and is never written back (AD-19). H-3: PlanAttempt.state's four values had no durable home - AD-16 declared it a field, AD-29 provided no record where running or verifying can appear, and my liveness rule said 'whatever state it names', presuming a record that does name one. Now: state is a DERIVED read-model value, never persisted, admitted/terminal implied by which records exist, running/verifying memory-only by design - which is exactly why a relaunch reads Interrupted. The normative domain block changed with it, and the liveness rule's referent is narrowed to the Operation journal's per-step states.
- (decision) Divergence H-4 is the sharpest interaction finding of the gate, because it falsified a rule I had just written as a fix. My new AD-17 rule said a safety-critical state 'reaches the user through a visible surface'. Below 720 usable CSS pixels that surface may not be on screen: the region becomes a full-workspace surface and Activity replay is also a full-workspace ActiveView destination, so UX-PB.4c ('the live sidecar stays visibly live') and UX-PB.5d ('no overlapping panes') pull opposite ways at a width NFR-3 declares supported. Under UX-PB.5d's build the live attempt goes behind the replay and takes the stall handoff and Interaction required with it - while the announcement channel that used to reach a user looking elsewhere is now OPTIONAL because I made it optional under D37 - so the attempt sits to the 30-minute hard cap invisibly with nothing narrating it. Exactly the failure the rule was written to close. Resolved in two rules: layout resolves in favour of no overlap, and a safety-critical state is reachable WITHOUT NAVIGATION - when its owning surface is off screen it surfaces in a persistent non-occludable indicator that routes to it, and that indicator is what satisfies 'stays visibly live' at this width rather than a second pane.
- (decision) Divergence H-5 and the M/L tail dispositioned. H-5 made the Cancelling residual ORDERING-CRITICAL rather than merely present: UX-PB.2e ships in wave 2 and adding the variant is an atomic AD-3 change across the Rust enum, types.ts, the guards and the committed fixtures, so it must be corrected before UX-PB.2e is built or the correct-course run is undoing shipped wire surface instead of editing story text. APPLIED in the spine: M-3, where 'that same rendered order' was wrong under virtualization - the product ships TanStack React Virtual, so at 500 rows about twenty are rendered and a 400-row range would mean two things; a range now reads the ordered filtered set the projection holds, the same set as the tri-state denominator. M-2, where AD-22's 'surfaced inline' named no surface and its own ordering guarantees the confirmation dialog is gone by then - now the surface that owns the attempt. L-1, UX-PB.1b into AD-28's Binds because it renders the counts AD-28 defines. L-3, UX-PB.4a into AD-5's Binds and the crash/relaunch row. RECORDED as residuals: M-1 (Story 3.5's Given is a row list, so the Cmd+A native-default defect has no owner on the Dashboard, History or Settings), L-2 (UX-PB.1d blocks Story 3.2 but Story 3.2's Dependencies do not agree), L-4 (UX-PB.2d's 'where applicable' undefined for verification refreshes).
- (event) spine finalized at revision 10. Deterministic lint zero findings after every edit batch including the final one; both mermaid diagrams re-rendered valid SVG with @mermaid-js/mermaid-cli 11.16.0 (RetryScope, RetryPreview and Verifying all present in the rendered output). AD inventory 1-5, 11, 12, 16-29 - 21 live ids, zero duplicates, AD-6..AD-10 and AD-13..AD-15 still retired and unreused. Two OPEN rows closed as RESOLVED by new invariants: AD-28 (a Package checkbox IS membership; batched range operations) and AD-29 (one append authority, two records per attempt, a stated fold). D37 retired as a release-criteria row with the three protected items named; D36 folded into AD-27, the Styling convention and the design-token row; the PRD recorded as requirements authority. Reviewer gate: five independent lenses plus the revision-9 VALIDATION-REPORT found in-folder mid-run. Counts from each lens's own tally: reconcile-prd 1/4/2/3, reconcile-decisions 0/2/6/3, reconcile-currency 3/3/4/3, rubric 1/4/5/2, gate-currency 0/0/4/3, divergence 2/5/3/4. Rubric verdict READY WITH FIXES; gate-currency 0 CRITICAL and 0 HIGH with 86 of 94 claims verified exactly; DECISIONS lens confirmed D37 applied correctly and completely. Every CRITICAL was in revision 10's own new material, the fourth consecutive revision with that pattern. Four rows recorded rather than decided and each names its fix - the Cmd+L/Esc accelerator sinks (now GATED: no story retires the ActivityDrawer until both are named), AD-12's file-scoped never-hand-edited rule, the quit guard's enforcement point, and the post-publish operational envelope - plus a new 'Maintainer edits this spine cannot make' row for the workflow-unowned set. epics.md untouched throughout per owner instruction; nothing committed or staged.
- (constraint) Post-finalize completeness check on my own reporting, prompted by the owner asking whether the report was complete. It was not, and two of the gaps were real. (1) Revision 10 RECREATED the F11 gap for its own gate: the tail row covered revisions 6, 8, 9 and the revision-9 validation pass and said nothing about revision 10's own six lenses - the exact failure that row exists to record. Now given its own row, with each lens's own tally and the per-finding disposition pushed to reviews/review-completeness-rev10.md rather than counted in the row, because a count in a status row is what this folder keeps getting wrong. (2) One gate-currency LOW was neither applied nor recorded - the claim that Tailwind v3's outline-none was a 'no-op'. Verified against the installed Tailwind 4.3.3 rather than from memory: 'outline-hidden' emits outline-style:none plus a (forced-colors: active) fallback of 'outline: 2px solid transparent', so what v3 called outline-none set a TRANSPARENT outline that kept the control visible under forced colours - not a no-op - and v4 renamed that utility. AD-27 corrected. Verified clean in the same sweep: zero [ASSUMPTION] tags, zero placeholders, no TODO/TBD. Spine grew 1052 to 1541 lines; memlog 156 to 224 entries.
- (decision) Owner decisions applied, all five, and every code claim in them re-verified before acting. AD-12 NARROWED and the owner is right that it was in scope: release-please-config.json extra-files pins exactly $.version in tauri.conf.json, $.package.version in Cargo.toml and the pack-manager lock entry, and the updater pubkey appears in no extra-files path, so release-please never reads or writes it. Under the old file-scoped rule the minisign key was unrotatable and AD-11's minimumSystemVersion unmaintainable. AD-12 now owns three version fields plus CHANGELOG.md and .release-please-manifest.json; everything else in those files is maintainer-owned, and a hand edit to a non-version field there is ordinary work.
- (decision) AD-30 (new) - a quit that would orphan a child process is guarded, at one enforcement point. Binds UX-PB.2f, UX-PB.1b, UX-PB.4e, Story 6.5 and any window-close, Cmd+Q or relaunch path. Owner decision taken as given, with every citation verified: the guard fires on any quit that would orphan a live child and onCloseRequested and Cmd+Q reach the same point the app-update path uses at src/components/shell/UpdateStatusItem.tsx:34-38, which opens the quitGuard dialog with reason 'update'. Queued counts as running is NOT open - docs/RELEASE-CHECKLIST.md:94-95 already records the identical reasoning, 'Queued counts as running - admission has already committed to the operation, and a restart would drop it unstarted', so the guard's active set is Queued union Running and the two guards may not drift. OS shutdown gets no dialog, best-effort only: queue.rs's cancel_all doc says outright 'Only flips the tokens - the runner tasks perform the SIGTERM to grace to SIGKILL work, so the quit path must ALSO await Queue::wait_until_idle before the process exits or the kill tasks may never be polled', which is why the rule names both halves. And commands.rs:816 already sets the surviving invariant verbatim: 'Same kill hook as a normal quit: children must never outlive the app.' Added a no-rollback rule so the guard does not imply one.
- (decision) Cmd+L and Esc settled by the owner, and NOT the way the row's own suggestion pointed - my proposed reading was wrong and I did not defend it. Cmd+L moves FOCUS to the sidecar region: it does not toggle, because a layout region has no toggled state to own, and it does not navigate. EXPERIENCE.md already gives the region a keyboard identity - F6 cycles primary navigation, the main grid and the sidecar regions - so Cmd+L is the direct jump to the third, and a hidden region makes it a no-op. Esc collapses to close-dialog ALONE, and the owner's reason for refusing to hand it the sidecar is the good one: admission is what empties the draft (AD-24), so a dismissing Esc would orphan a draft the user still holds, and Done already owns dismissing Results. Both rules land in AD-17, which is where the region's identity lives. Creates one PRD divergence, recorded: prd.md RP-2 calls Cmd+L '(toggle the activity surface)', which is the retiring behaviour.
- (question) DISAGREEMENT RECORDED rather than silently absorbed: the owner's Cmd+L decision is sound and applied, but one of its stated premises is contradicted by the spine, D30 and the PRD, and I did not encode it. The owner wrote that 'EXPERIENCE.md:153 makes Activity a state the sidecar transforms into, not a destination' and that routing Cmd+L to an Activity ActiveView 'would create a fourth home for activity data and reintroduce exactly the ambiguity AD-17 forecloses'. AD-17 does not foreclose it - it MANDATES it, verbatim: 'Activity is a first-class destination in the existing discriminated ActiveView state - for the active attempt and for replaying a completed History entry - not a drawer and not a sidecar mode', citing D30. And prd.md names 'Activity as a first-class destination' or 'first-class navigation destination' in four places (:339, :348, :439, :638), all Planned under D29/D30. EXPERIENCE.md:153 is also real - 'It persists across Manager changes and transforms into Activity and Results' - so both models coexist as AD-17 already writes them: the region transforms in place for the live attempt, AND Activity is a route for replay. The DECISION does not depend on the premise, which is why it was applied: Cmd+L as a focus jump creates no fourth home because it routes nowhere. If the owner does want AD-17's Activity rule changed, that is a separate call contradicting D30 and the PRD and rippling into every UX-PB.3x and 4x story, and my recommendation is no.
- (constraint) Minor correction to an owner premise, verified: 'grep finds zero mentions of a drawer anywhere in EXPERIENCE.md' is wrong - there are two, at :256 'Remove any Auto-open Activity drawer preference; the sidecar always transforms automatically after confirmation' and :351 'A permanently visible empty right drawer' inside an anti-pattern list. Both are NEGATIONS, so the owner's conclusion that EXPERIENCE.md nowhere endorses a drawer holds; only the count was wrong. Recorded because this run folder's whole discipline is that a count is checked rather than asserted, and that applies to the owner's counts as much as to mine.
- (constraint) CORRECTION to the revision-10 finalize event above, from a completeness audit. Three claims in it were wrong and two of them were repeated across all three deliverables. (1) 'five independent lenses' - there were SIX: three reconcilers and three gate lenses. The DRIFT-NOTE sentence even listed six while saying five. (2) gate-currency's tally is 0 CRITICAL / 0 HIGH / 4 MEDIUM / 4 LOW, not 0/0/4/3 - verified by counting its own finding headings, which are MEDIUM-1..4 and LOW-1..4. The total across the six lenses is 68 findings, not the figure implied. (3) The spine cited MAINTAINER-EDITS-2026-07-25.md before that file existed; it exists now, so the citation resolves, but it was written ahead of its referent. All three corrected in the spine and DRIFT-NOTE. The audit's substantive result is the good one and is worth recording as the counter-weight: 54 findings APPLIED, 14 RECORDED in a row, 0 NEITHER - every finding from all six lenses lands somewhere, checked against the spine rather than against my account of it. Full audit at reviews/review-completeness-rev10.md.
- (event) Update run opened 2026-07-25, resumed from this memlog. Scope is two RECORDED defects and nothing else - no regeneration, no new invariants: (1) the Decision Status row 'epics.md residuals for the next bmad-correct-course run' still reads OPEN after commit 0960aab applied the batch; (2) AD-28's Esc bullet contradicts itself inside one bullet. Both were recorded by other runs rather than found here - (2) came from the PRD run's memlog, which routed it to bmad-architecture on the owner's instruction that the spine is workflow output and gets fixed here. AD ids stay stable; no AD is created, retired, or renumbered.
- (constraint) Defect 1 verified item by item against the COMMITTED epics.md (git status returns zero lines for that path, so working tree = HEAD = 0960aab), not against the proposal's account of itself - the discipline this row's own text demands. All 17 residual items landed. Counts from grep -c on epics.md: AD-28 0 to 14, AD-29 0 to 17, AD-18 7 to 15, AD-27 32 to 39, spine line-number citations 0, Cmd+U 1 (the negation). Cancelling survives at SIX lines and every one is a negation or the ordering warning - :89 'is not a state, durable or otherwise', :799 and :972 in Dependencies as 'there is no Cancelling state', :802 the ordering-critical note ending 'A builder must not add the variant', :813 and :982 'no Cancelling state is introduced'. Story 3.5 restated on AD-28 at :1356 and gained the non-list-views criterion for the Dashboard/History/Settings preventDefault defect; Story 3.2 now reads aria-disabled=true not native disabled and its Dependencies name UX-PB.1d; Stories 3.4 and 6.5 both gained AD-27; UX-PB.1d and UX-PB.5d restated per D37 and NOT deleted, titles included; UX-PB.2c carries AD-29's override of the admission-record contents and its persist-failure criterion now says explicitly 'not that admission is blocked and not that a line is deleted from an append-only file'; UX-PB.2d defines 'where applicable' at :793; UX-PB.3a negates the announce-plan-start obligation at :853; UX-PB.3d's 'several append-only records' returns 0 and the authoritative-record gap is answered by AD-29's fold at :926; both D36 passages (:320, :365) now say the contrast guard landed in a201fb0 and that no story owes one; the manual VoiceOver pass appears twice and both state it is deleted by D37.
- (decision) Defect 1 CLOSED. The 'epics.md residuals' row goes OPEN -> RESOLVED, citing 0960aab and the proposal by filename. The re-verification was worth doing as a check rather than a rubber stamp because it caught TWO errors in the row's own text. (1) The row compressed 'a Clear action and an Esc rung AD-28 deletes', joining two things that are not symmetrical: AD-28's own rule text names Clear inside the closed scope-wide removal taxonomy - '(2) A scope-wide removal - the header checkbox, CmdA on an all-staged view, a Manager-wide remove, Clear, or undoing an Update Everything seed' - so Clear survives with a CHANGED MEANING (a membership removal, not a selection clear) and deleting it would have left a named shape in a closed taxonomy with no story owning it. The correct-course run caught this and followed the rule text over the row, which is the right precedence and is now stated in the row: the rule text governs. Esc is genuinely deleted and carries no replacement sink. (2) The row's claim 'Those are the only two occurrences in epics.md' was wrong - there were three. It measured backticked capital-C and FR-13's inventory entry carried the word lowercase. Both errors are recorded IN the resolved row rather than quietly dropped with the OPEN status, because the row is spine-owned and a later reader would trust it over the file.
- (decision) Defect 2 CLOSED. AD-28's Esc bullet no longer contradicts itself. It opened with 'the cascade drops from three rungs to two and keeps close-dialog' - prd.md FR-6's PRE-AD-17 wording - and corrected itself four lines later with 'There is no surviving second rung ... so the cascade is close-dialog alone'. Two builders could obey the same bullet and ship different cascades, which is exactly what an AD exists to prevent. Rewritten to state the end state once (close-dialog alone) while keeping every load-bearing clause: the third rung loses its sink when the ActivityDrawer retires under AD-17 so removing the middle rung leaves one; deleting rather than re-pointing is what stops a single Esc mass-writing tombstones and poisoning a draft against Update Everything; and FR-6's rule that Esc is NARROWED, NOT DELETED, so no story author removes the surviving rung along with the two that go. Agrees with AD-17's own Esc rule, which was already correct and needed no edit.
- (constraint) THIRD defect, found while fixing defect 2 and not in the commissioned scope: the AD-28 bullet closed by recording prd.md RP-2's '(toggle the activity surface)' parenthetical as a live PRD divergence, and the 'Maintainer edits this spine cannot make' row asserted the same thing. Both FALSE as of this revision - verified, not assumed: grep -c 'toggle the activity surface' on prd.md returns 0, and RP-2 now reads 'CmdL is a focus move, not a toggle. Planned - D27-D30 for the behavior; the registration ships.' A bmad-prd Update corrected it after revision 10 was written. Fixed in BOTH places. The bullet was non-negotiable - carrying a verifiably false claim forward inside the rule I was fixing would have been a defect of my own making. The maintainer-edits listing was also a ROUTING error independent of staleness: prd.md is bmad-prd output, not a workflow-unowned file, so it never belonged in a row whose premise is 'files no workflow owns'.
- (question) RECORDED, NOT FIXED - the rest of the maintainer-edits row was not re-verified and at least one more claim in it is stale. It says D15's mechanism clause is overridden by AD-16 and 'nothing records the supersession - only a decision can supersede a decision'. docs/DECISIONS.md D38 does exactly that at :583, 'D15's disabled-checkbox mechanism is superseded; its substance is not', landed in commit d7a7696 - and epics.md Story 3.2 already cites D38 for it. Its other claims (D36's plural-consumers title, RELEASE-CHECKLIST step 5's staging description, SPEC F5 and 4.11) were NOT checked this run. A full re-verification of that row is its own pass; this run was scoped to two defects and already extended once for a claim it was directly carrying. Revisit condition: the next bmad-architecture run, or whenever a maintainer works that list.
- (override) Reviewer Gate NOT run for revision 11, deliberately and recorded rather than quietly skipped. The session forbids spawning subagents unless the user asks, so the rubric walker and both finalize_reviewers lenses were skipped rather than run degraded. What DID run: lint_spine.py returns total_findings 0; AD inventory unchanged at 22 live ids (1-5, 11, 12, 16-30) with none created, retired, or renumbered; zero [ASSUMPTION], TODO, TBD, or template comments; the replaced table row has exactly 4 pipes so the three-column table is intact; neither mermaid diagram was touched; and every factual claim in both edits was verified against the cited file this session. THE LAST FULL GATE REMAINS REVISION 10'S SIX LENSES, which predate these three corrections. Offered to the owner rather than assumed.
- (event) spine finalized at revision 11. Frontmatter updated to artifact_revision 11, updated 2026-08-06, status stays final. Revision-11 note added above revision 10's; DRIFT-NOTE.md gains section 2h and its title goes 'revision 3 -> 10' to 'revision 3 -> 11'. Spine 1601 to 1616 lines; the resolved register row is 3927 chars against the OPEN row's 8242, because a closed row records what happened and no longer has to carry seventeen instructions. Files changed this run: ARCHITECTURE-SPINE.md, DRIFT-NOTE.md, this memlog. No epics.md, EXPERIENCE.md, SPEC.md, or product-code edit - each is another workflow's or the maintainer's to make.
